[1mdiff --git a/export/rspec-core-RSpec-Core-Formatters-ConsoleCodes.html.current b/export/rspec-core-RSpec-Core-Formatters-ConsoleCodes.html.new[m
[1mindex 190a3f9..c9c0c31 100644[m
[1m--- a/export/rspec-core-RSpec-Core-Formatters-ConsoleCodes.html.current[m
[1m+++ b/export/rspec-core-RSpec-Core-Formatters-ConsoleCodes.html.new[m
[36m@@ -48,21 +48,20 @@[m [mModule: RSpec::Core::Formatters::ConsoleCodes[m
 [m
 OverviewOverview[m
   [m
[31m-    ConsoleCodes provides helpers for formatting console output[m
[31m-with ANSI codes, e.g. color's and bold.ConsoleCodes provides helpers for formatting console output[m
[31m-with ANSI codes, e.g. color's and bold.[m
     [m
[32m+[m[32mConsoleCodes provides helpers for formatting console output with ANSI codes, e.g. color's and bold.ConsoleCodes provides helpers for formatting console output with ANSI codes, e.g. color's and bold.[m
 [m
 [m
[31m-    ConsoleCodes provides helpers for formatting console output[m
[31m-with ANSI codes, e.g. color's and bold.[m
   [m
     [m
[32m+[m[32mConsoleCodes provides helpers for formatting console output with ANSI codes, e.g. color's and bold.[m
 [m
 [m
   [m
[31m-    ConsoleCodes provides helpers for formatting console output[m
[31m-with ANSI codes, e.g. color's and bold.[m
[32m+[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mConsoleCodes provides helpers for formatting console output with ANSI codes, e.g. color's and bold.[m
 [m
 [m
   [m
[36m@@ -205,21 +214,20 @@[m [mWraps a piece of text in ANSI codes with the supplied code.[m
       [m
   [m
   [m
[31m-    Fetches the correct code for the supplied symbol, or checks[m
[31m-that a code is valid. Defaults to white (37).Fetches the correct code for the supplied symbol, or checks[m
[31m-that a code is valid. Defaults to white (37).[m
     [m
[32m+[m[32mFetches the correct code for the supplied symbol, or checks that a code is valid. Defaults to white (37).Fetches the correct code for the supplied symbol, or checks that a code is valid. Defaults to white (37).[m
 [m
 [m
[31m-    Fetches the correct code for the supplied symbol, or checks[m
[31m-that a code is valid. Defaults to white (37).[m
   [m
     [m
[32m+[m[32mFetches the correct code for the supplied symbol, or checks that a code is valid. Defaults to white (37).[m
 [m
 [m
   [m
[31m-    Fetches the correct code for the supplied symbol, or checks[m
[31m-that a code is valid. Defaults to white (37).[m
[32m+[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mFetches the correct code for the supplied symbol, or checks that a code is valid. Defaults to white (37).[m
 [m
 [m
   [m
[36m@@ -518,8 +536,8 @@[m [mend[m
 [m
   [m
   [m
[31m-    Fetches the correct code for the supplied symbol, or checks[m
[31m-that a code is valid. Defaults to white (37).[m
[32m+[m[41m    [m
[32m+[m[32mFetches the correct code for the supplied symbol, or checks that a code is valid. Defaults to white (37).[m
 [m
 [m
   [m
[36m@@ -603,25 +623,20 @@[m [mend[m
       [m
   [m
   [m
[31m-    Wraps a piece of text in ANSI codes with the supplied code. Will[m
[31m-only apply the control code if RSpec.configuration.color_enabled?RSpec.configuration.color_enabled?[m
[31m-returns true.Wraps a piece of text in ANSI codes with the supplied code. Will[m
[31m-only apply the control code if RSpec.configuration.color_enabled?[m
[31m-returns true.[m
[32m+[m[41m    [m
[32m+[m[32mWraps a piece of text in ANSI codes with the supplied code. Will only apply the control code if RSpec.configuration.color_enabled?RSpec.configuration.color_enabled? returns true.Wraps a piece of text in ANSI codes with the supplied code. Will only apply the control code if RSpec.configuration.color_enabled? returns true.[m
 [m
 [m
   [m
[31m-    Wraps a piece of text in ANSI codes with the supplied code. Will[m
[31m-only apply the control code if RSpec.configuration.color_enabled?[m
[31m-returns true.[m
     [m
[32m+[m[32mWraps a piece of text in ANSI codes with the supplied code. Will only apply the control code if RSpec.configuration.color_enabled? returns true.[m
 [m
 [m
   [m
 [m
[31m-    Wraps a piece of text in ANSI codes with the supplied code. Will[m
[31m-only apply the control code if RSpec.configuration.color_enabled?[m
[31m-returns true.[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mWraps a piece of text in ANSI codes with the supplied code. Will only apply the control code if RSpec.configuration.color_enabled? returns true.[m
 [m
 [m
   [m
[36m@@ -939,9 +969,8 @@[m [mend[m
 [m
   [m
   [m
[31m-    Wraps a piece of text in ANSI codes with the supplied code. Will[m
[31m-only apply the control code if RSpec.configuration.color_enabled?[m
[31m-returns true.[m
[32m+[m[41m    [m
[32m+[m[32mWraps a piece of text in ANSI codes with the supplied code. Will only apply the control code if RSpec.configuration.color_enabled? returns true.[m
 [m
 [m
   [m
[36m@@ -1036,8 +1068,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Fetches the correct code for the supplied symbol, or checks[m
[31m-that a code is valid. Defaults to white (37).[m
[32m+[m[41m    [m
[32m+[m[32mFetches the correct code for the supplied symbol, or checks that a code is valid. Defaults to white (37).[m
 [m
 [m
   [m
[36m@@ -1121,9 +1155,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Wraps a piece of text in ANSI codes with the supplied code. Will[m
[31m-only apply the control code if RSpec.configuration.color_enabled?[m
[31m-returns true.[m
[32m+[m[41m    [m
[32m+[m[32mWraps a piece of text in ANSI codes with the supplied code. Will only apply the control code if RSpec.configuration.color_enabled? returns true.[m
 [m
 [m
   [m
[36m@@ -1239,8 +1275,8 @@[m [mModule: RSpec::Core::Formatters::ConsoleCodes[m
 [m
 Overview[m
   [m
[31m-    ConsoleCodes provides helpers for formatting console output[m
[31m-with ANSI codes, e.g. color's and bold.[m
[32m+[m[41m    [m
[32m+[m[32mConsoleCodes provides helpers for formatting console output with ANSI codes, e.g. color's and bold.[m
 [m
 [m
   [m
[36m@@ -1309,8 +1347,8 @@[m [mwith ANSI codes, e.g. color's and bold.[m
       [m
   [m
   [m
[31m-    Fetches the correct code for the supplied symbol, or checks[m
[31m-that a code is valid. Defaults to white (37).[m
[32m+[m[41m    [m
[32m+[m[32mFetches the correct code for the supplied symbol, or checks that a code is valid. Defaults to white (37).[m
 [m
 [m
   [m
[36m@@ -1394,9 +1434,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Wraps a piece of text in ANSI codes with the supplied code. Will[m
[31m-only apply the control code if RSpec.configuration.color_enabled?[m
[31m-returns true.[m
[32m+[m[41m    [m
[32m+[m[32mWraps a piece of text in ANSI codes with the supplied code. Will only apply the control code if RSpec.configuration.color_enabled? returns true.[m
 [m
 [m
   [m
