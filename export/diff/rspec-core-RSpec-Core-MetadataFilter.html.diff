[1mdiff --git a/export/rspec-core-RSpec-Core-MetadataFilter.html.current b/export/rspec-core-RSpec-Core-MetadataFilter.html.new[m
[1mindex f963b76..a14d977 100644[m
[1m--- a/export/rspec-core-RSpec-Core-MetadataFilter.html.current[m
[1m+++ b/export/rspec-core-RSpec-Core-MetadataFilter.html.new[m
[36m@@ -48,33 +48,20 @@[m [mModule: RSpec::Core::MetadataFilter[m
 [m
 OverviewOverview[m
   [m
[31m-    Contains metadata filtering logic. This has been extracted from[m
[31m-the metadata classes because it operates ON a metadata hash but[m
[31m-does not manage any of the state in the hash. We're moving towards[m
[31m-having metadata be a raw hash (not a custom subclass), so externalizing[m
[31m-this filtering logic helps us move in that direction.Contains metadata filtering logic. This has been extracted from[m
[31m-the metadata classes because it operates ON a metadata hash but[m
[31m-does not manage any of the state in the hash. We're moving towards[m
[31m-having metadata be a raw hash (not a custom subclass), so externalizing[m
[31m-this filtering logic helps us move in that direction.[m
     [m
[32m+[m[32mContains metadata filtering logic. This has been extracted from the metadata classes because it operates ON a metadata hash but does not manage any of the state in the hash. We're moving towards having metadata be a raw hash (not a custom subclass), so externalizing this filtering logic helps us move in that direction.Contains metadata filtering logic. This has been extracted from the metadata classes because it operates ON a metadata hash but does not manage any of the state in the hash. We're moving towards having metadata be a raw hash (not a custom subclass), so externalizing this filtering logic helps us move in that direction.[m
 [m
 [m
[31m-    Contains metadata filtering logic. This has been extracted from[m
[31m-the metadata classes because it operates ON a metadata hash but[m
[31m-does not manage any of the state in the hash. We're moving towards[m
[31m-having metadata be a raw hash (not a custom subclass), so externalizing[m
[31m-this filtering logic helps us move in that direction.[m
   [m
     [m
[32m+[m[32mContains metadata filtering logic. This has been extracted from the metadata classes because it operates ON a metadata hash but does not manage any of the state in the hash. We're moving towards having metadata be a raw hash (not a custom subclass), so externalizing this filtering logic helps us move in that direction.[m
 [m
 [m
   [m
[31m-    Contains metadata filtering logic. This has been extracted from[m
[31m-the metadata classes because it operates ON a metadata hash but[m
[31m-does not manage any of the state in the hash. We're moving towards[m
[31m-having metadata be a raw hash (not a custom subclass), so externalizing[m
[31m-this filtering logic helps us move in that direction.[m
[32m+[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mContains metadata filtering logic. This has been extracted from the metadata classes because it operates ON a metadata hash but does not manage any of the state in the hash. We're moving towards having metadata be a raw hash (not a custom subclass), so externalizing this filtering logic helps us move in that direction.[m
 [m
 [m
   [m
[36m@@ -120,11 +107,8 @@[m [mModule: RSpec::Core::MetadataFilter[m
 [m
 Overview[m
   [m
[31m-    Contains metadata filtering logic. This has been extracted from[m
[31m-the metadata classes because it operates ON a metadata hash but[m
[31m-does not manage any of the state in the hash. We're moving towards[m
[31m-having metadata be a raw hash (not a custom subclass), so externalizing[m
[31m-this filtering logic helps us move in that direction.[m
[32m+[m[41m    [m
[32m+[m[32mContains metadata filtering logic. This has been extracted from the metadata classes because it operates ON a metadata hash but does not manage any of the state in the hash. We're moving towards having metadata be a raw hash (not a custom subclass), so externalizing this filtering logic helps us move in that direction.[m
 [m
 [m
   [m
