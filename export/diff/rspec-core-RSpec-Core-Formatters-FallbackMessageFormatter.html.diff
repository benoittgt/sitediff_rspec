[1mdiff --git a/export/rspec-core-RSpec-Core-Formatters-FallbackMessageFormatter.html.current b/export/rspec-core-RSpec-Core-Formatters-FallbackMessageFormatter.html.new[m
[1mindex 17093ef..2e59eb4 100644[m
[1m--- a/export/rspec-core-RSpec-Core-Formatters-FallbackMessageFormatter.html.current[m
[1m+++ b/export/rspec-core-RSpec-Core-Formatters-FallbackMessageFormatter.html.new[m
[36m@@ -117,9 +117,8 @@[m [mOverviewOverview[m
   This class is part of a private API.[m
   You should avoid using this class if possible, as it may be removed or be changed in the future.[m
 [m
[31m-Formatter for providing message output as a fallback when no other[m
[31m-profiler implements #message.Formatter for providing message output as a fallback when no other[m
[31m-profiler implements #message.[m
[32m+[m
[32m+[m[32mFormatter for providing message output as a fallback when no other profiler implements #message.Formatter for providing message output as a fallback when no other profiler implements #message.[m
 [m
 [m
   [m
[36m@@ -127,8 +126,8 @@[m [mprofiler implements #message.[m
   This class is part of a private API.[m
   You should avoid using this class if possible, as it may be removed or be changed in the future.[m
 [m
[31m-Formatter for providing message output as a fallback when no other[m
[31m-profiler implements #message.[m
[32m+[m
[32m+[m[32mFormatter for providing message output as a fallback when no other profiler implements #message.[m
 [m
 [m
   [m
[36m@@ -138,8 +137,8 @@[m [mprofiler implements #message.[m
   This class is part of a private API.[m
   You should avoid using this class if possible, as it may be removed or be changed in the future.[m
 [m
[31m-Formatter for providing message output as a fallback when no other[m
[31m-profiler implements #message.[m
[32m+[m
[32m+[m[32mFormatter for providing message output as a fallback when no other profiler implements #message.[m
 [m
 [m
   [m
[36m@@ -800,8 +826,8 @@[m [mOverview[m
   This class is part of a private API.[m
   You should avoid using this class if possible, as it may be removed or be changed in the future.[m
 [m
[31m-Formatter for providing message output as a fallback when no other[m
[31m-profiler implements #message.[m
[32m+[m
[32m+[m[32mFormatter for providing message output as a fallback when no other profiler implements #message.[m
 [m
 [m
   [m
