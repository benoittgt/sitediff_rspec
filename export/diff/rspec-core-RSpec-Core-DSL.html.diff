[1mdiff --git a/export/rspec-core-RSpec-Core-DSL.html.current b/export/rspec-core-RSpec-Core-DSL.html.new[m
[1mindex 303f01d..f40482c 100644[m
[1m--- a/export/rspec-core-RSpec-Core-DSL.html.current[m
[1m+++ b/export/rspec-core-RSpec-Core-DSL.html.new[m
[36m@@ -48,21 +48,10 @@[m [mModule: RSpec::Core::DSL[m
 [m
 OverviewOverview[m
   [m
[31m-    DSL defines methods to group examples, most notably describedescribe,[m
[31m-and exposes them as class methods of RSpec. They can also be[m
[31m-exposed globally (on mainmain and instances of ModuleModule) through[m
[31m-the Configuration option expose_dsl_globallyexpose_dsl_globally.DSL defines methods to group examples, most notably describe,[m
[31m-and exposes them as class methods of RSpec. They can also be[m
[31m-exposed globally (on main and instances of Module) through[m
[31m-the Configuration option expose_dsl_globally.[m
[31m-[m
[31m-By default the methods describedescribe, contextcontext and example_groupexample_group[m
[31m-are exposed. These methods define a named context for one or[m
[31m-more examples. The given block is evaluated in the context of[m
[31m-a generated subclass of ExampleGroup.By default the methods describe, context and example_group[m
[31m-are exposed. These methods define a named context for one or[m
[31m-more examples. The given block is evaluated in the context of[m
[31m-a generated subclass of ExampleGroup.[m
[32m+[m[41m    [m
[32m+[m[32mDSL defines methods to group examples, most notably describedescribe, and exposes them as class methods of RSpec. They can also be exposed globally (on mainmain and instances of ModuleModule) through the Configuration option expose_dsl_globallyexpose_dsl_globally.DSL defines methods to group examples, most notably describe, and exposes them as class methods of RSpec. They can also be exposed globally (on main and instances of Module) through the Configuration option expose_dsl_globally.[m
[32m+[m
[32m+[m[32mBy default the methods describedescribe, contextcontext and example_groupexample_group are exposed. These methods define a named context for one or more examples. The given block is evaluated in the context of a generated subclass of ExampleGroup.By default the methods describe, context and example_group are exposed. These methods define a named context for one or more examples. The given block is evaluated in the context of a generated subclass of ExampleGroup.[m
 [m
 Examples:Examples:[m
 [m
[36m@@ -92,15 +81,10 @@[m [mend[m
 [m
 [m
   [m
[31m-    DSL defines methods to group examples, most notably describe,[m
[31m-and exposes them as class methods of RSpec. They can also be[m
[31m-exposed globally (on main and instances of Module) through[m
[31m-the Configuration option expose_dsl_globally.[m
     [m
[31m-By default the methods describe, context and example_group[m
[31m-are exposed. These methods define a named context for one or[m
[31m-more examples. The given block is evaluated in the context of[m
[31m-a generated subclass of ExampleGroup.[m
[32m+[m[32mDSL defines methods to group examples, most notably describe, and exposes them as class methods of RSpec. They can also be exposed globally (on main and instances of Module) through the Configuration option expose_dsl_globally.[m
[32m+[m
[32m+[m[32mBy default the methods describe, context and example_group are exposed. These methods define a named context for one or more examples. The given block is evaluated in the context of a generated subclass of ExampleGroup.[m
 [m
 Examples:[m
 [m
[36m@@ -117,15 +101,10 @@[m [mend[m
   [m
 [m
   [m
[31m-    DSL defines methods to group examples, most notably describe,[m
[31m-and exposes them as class methods of RSpec. They can also be[m
[31m-exposed globally (on main and instances of Module) through[m
[31m-the Configuration option expose_dsl_globally.[m
     [m
[31m-By default the methods describe, context and example_group[m
[31m-are exposed. These methods define a named context for one or[m
[31m-more examples. The given block is evaluated in the context of[m
[31m-a generated subclass of ExampleGroup.[m
[32m+[m[32mDSL defines methods to group examples, most notably describe, and exposes them as class methods of RSpec. They can also be exposed globally (on main and instances of Module) through the Configuration option expose_dsl_globally.[m
[32m+[m
[32m+[m[32mBy default the methods describe, context and example_group are exposed. These methods define a named context for one or more examples. The given block is evaluated in the context of a generated subclass of ExampleGroup.[m
 [m
 Examples:[m
 [m
[36m@@ -872,15 +871,10 @@[m [mModule: RSpec::Core::DSL[m
 [m
 Overview[m
   [m
[31m-    DSL defines methods to group examples, most notably describe,[m
[31m-and exposes them as class methods of RSpec. They can also be[m
[31m-exposed globally (on main and instances of Module) through[m
[31m-the Configuration option expose_dsl_globally.[m
     [m
[31m-By default the methods describe, context and example_group[m
[31m-are exposed. These methods define a named context for one or[m
[31m-more examples. The given block is evaluated in the context of[m
[31m-a generated subclass of ExampleGroup.[m
[32m+[m[32mDSL defines methods to group examples, most notably describe, and exposes them as class methods of RSpec. They can also be exposed globally (on main and instances of Module) through the Configuration option expose_dsl_globally.[m
[32m+[m
[32m+[m[32mBy default the methods describe, context and example_group are exposed. These methods define a named context for one or more examples. The given block is evaluated in the context of a generated subclass of ExampleGroup.[m
 [m
 Examples:[m
 [m
