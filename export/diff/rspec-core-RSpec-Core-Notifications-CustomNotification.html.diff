[1mdiff --git a/export/rspec-core-RSpec-Core-Notifications-CustomNotification.html.current b/export/rspec-core-RSpec-Core-Notifications-CustomNotification.html.new[m
[1mindex 7f56fcb..be76550 100644[m
[1m--- a/export/rspec-core-RSpec-Core-Notifications-CustomNotification.html.current[m
[1m+++ b/export/rspec-core-RSpec-Core-Notifications-CustomNotification.html.new[m
[36m@@ -120,25 +120,20 @@[m [mClass: RSpec::Core::Notifications::CustomNotification[m
 [m
 OverviewOverview[m
   [m
[31m-    CustomNotificationCustomNotification is used when sending custom events to formatters /[m
[31m-other registered listeners, it creates attributes based on supplied hash[m
[31m-of options.CustomNotification is used when sending custom events to formatters /[m
[31m-other registered listeners, it creates attributes based on supplied hash[m
[31m-of options.[m
     [m
[32m+[m[32mCustomNotificationCustomNotification is used when sending custom events to formatters / other registered listeners, it creates attributes based on supplied hash of options.CustomNotification is used when sending custom events to formatters / other registered listeners, it creates attributes based on supplied hash of options.[m
 [m
 [m
[31m-    CustomNotification is used when sending custom events to formatters /[m
[31m-other registered listeners, it creates attributes based on supplied hash[m
[31m-of options.[m
   [m
     [m
[32m+[m[32mCustomNotification is used when sending custom events to formatters / other registered listeners, it creates attributes based on supplied hash of options.[m
 [m
 [m
   [m
[31m-    CustomNotification is used when sending custom events to formatters /[m
[31m-other registered listeners, it creates attributes based on supplied hash[m
[31m-of options.[m
[32m+[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mCustomNotification is used when sending custom events to formatters / other registered listeners, it creates attributes based on supplied hash of options.[m
 [m
 [m
   [m
[36m@@ -645,9 +657,8 @@[m [mClass: RSpec::Core::Notifications::CustomNotification[m
 [m
 Overview[m
   [m
[31m-    CustomNotification is used when sending custom events to formatters /[m
[31m-other registered listeners, it creates attributes based on supplied hash[m
[31m-of options.[m
[32m+[m[41m    [m
[32m+[m[32mCustomNotification is used when sending custom events to formatters / other registered listeners, it creates attributes based on supplied hash of options.[m
 [m
 [m
   [m
