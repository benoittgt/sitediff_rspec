[1mdiff --git a/export/rspec-mocks-RSpec-Mocks-Syntax.html.current b/export/rspec-mocks-RSpec-Mocks-Syntax.html.new[m
[1mindex 5ac3aa3..a5eea29 100644[m
[1m--- a/export/rspec-mocks-RSpec-Mocks-Syntax.html.current[m
[1m+++ b/export/rspec-mocks-RSpec-Mocks-Syntax.html.new[m
[36m@@ -554,8 +554,7 @@[m [mDetermines where the methods like should_receive, and stub are added.[m
     [m
       # File 'lib/rspec/mocks/syntax.rb', line 181# File 'lib/rspec/mocks/syntax.rb', line 181[m
 [m
[31m-defdef selfself..default_should_syntax_hostdefault_should_syntax_host[m
[31m-  # JRuby 1.7.4 introduces a regression whereby `defined?(::BasicObject) => nil`[m
[32m+[m[32mdefdef selfself..default_should_syntax_hostdefault_should_syntax_host  # JRuby 1.7.4 introduces a regression whereby `defined?(::BasicObject) => nil`[m
 # JRuby 1.7.4 introduces a regression whereby `defined?(::BasicObject) => nil`[m
   # yet `BasicObject` still exists and patching onto ::Object breaks things[m
 # yet `BasicObject` still exists and patching onto ::Object breaks things[m
[36m@@ -584,8 +584,7 @@[m [mdefdef selfself..default_should_syntax_hostdefault_should_syntax_host[m
   ::::BasicObjectBasicObject[m
 endend# File 'lib/rspec/mocks/syntax.rb', line 181[m
 [m
[31m-def self.default_should_syntax_host[m
[31m-  # JRuby 1.7.4 introduces a regression whereby `defined?(::BasicObject) => nil`[m
[32m+[m[32mdef self.default_should_syntax_host  # JRuby 1.7.4 introduces a regression whereby `defined?(::BasicObject) => nil`[m
   # yet `BasicObject` still exists and patching onto ::Object breaks things[m
   # e.g. SimpleDelegator expectations won't work[m
   #[m
[36m@@ -607,8 +607,7 @@[m [mend[m
     [m
       # File 'lib/rspec/mocks/syntax.rb', line 181[m
 [m
[31m-def self.default_should_syntax_host[m
[31m-  # JRuby 1.7.4 introduces a regression whereby `defined?(::BasicObject) => nil`[m
[32m+[m[32mdef self.default_should_syntax_host  # JRuby 1.7.4 introduces a regression whereby `defined?(::BasicObject) => nil`[m
   # yet `BasicObject` still exists and patching onto ::Object breaks things[m
   # e.g. SimpleDelegator expectations won't work[m
   #[m
[36m@@ -657,8 +657,7 @@[m [mend[m
     [m
       # File 'lib/rspec/mocks/syntax.rb', line 181[m
 [m
[31m-def self.default_should_syntax_host[m
[31m-  # JRuby 1.7.4 introduces a regression whereby `defined?(::BasicObject) => nil`[m
[32m+[m[32mdef self.default_should_syntax_host  # JRuby 1.7.4 introduces a regression whereby `defined?(::BasicObject) => nil`[m
   # yet `BasicObject` still exists and patching onto ::Object breaks things[m
   # e.g. SimpleDelegator expectations won't work[m
   #[m
[36m@@ -709,8 +709,7 @@[m [mend[m
     [m
       # File 'lib/rspec/mocks/syntax.rb', line 181[m
 [m
[31m-def self.default_should_syntax_host[m
[31m-  # JRuby 1.7.4 introduces a regression whereby `defined?(::BasicObject) => nil`[m
[32m+[m[32mdef self.default_should_syntax_host  # JRuby 1.7.4 introduces a regression whereby `defined?(::BasicObject) => nil`[m
   # yet `BasicObject` still exists and patching onto ::Object breaks things[m
   # e.g. SimpleDelegator expectations won't work[m
   #[m
[36m@@ -774,8 +774,7 @@[m [mDetermines where the methods like should_receive, and stub are added.[m
     [m
       # File 'lib/rspec/mocks/syntax.rb', line 181[m
 [m
[31m-def self.default_should_syntax_host[m
[31m-  # JRuby 1.7.4 introduces a regression whereby `defined?(::BasicObject) => nil`[m
[32m+[m[32mdef self.default_should_syntax_host  # JRuby 1.7.4 introduces a regression whereby `defined?(::BasicObject) => nil`[m
   # yet `BasicObject` still exists and patching onto ::Object breaks things[m
   # e.g. SimpleDelegator expectations won't work[m
   #[m
[36m@@ -3197,8 +3197,7 @@[m [mDetermines where the methods like should_receive, and stub are added.[m
     [m
       # File 'lib/rspec/mocks/syntax.rb', line 181[m
 [m
[31m-def self.default_should_syntax_host[m
[31m-  # JRuby 1.7.4 introduces a regression whereby `defined?(::BasicObject) => nil`[m
[32m+[m[32mdef self.default_should_syntax_host  # JRuby 1.7.4 introduces a regression whereby `defined?(::BasicObject) => nil`[m
   # yet `BasicObject` still exists and patching onto ::Object breaks things[m
   # e.g. SimpleDelegator expectations won't work[m
   #[m
[36m@@ -3906,8 +3906,7 @@[m [mDetermines where the methods like should_receive, and stub are added.[m
     [m
       # File 'lib/rspec/mocks/syntax.rb', line 181[m
 [m
[31m-def self.default_should_syntax_host[m
[31m-  # JRuby 1.7.4 introduces a regression whereby `defined?(::BasicObject) => nil`[m
[32m+[m[32mdef self.default_should_syntax_host  # JRuby 1.7.4 introduces a regression whereby `defined?(::BasicObject) => nil`[m
   # yet `BasicObject` still exists and patching onto ::Object breaks things[m
   # e.g. SimpleDelegator expectations won't work[m
   #[m
