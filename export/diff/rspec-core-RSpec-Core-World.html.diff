[1mdiff --git a/export/rspec-core-RSpec-Core-World.html.current b/export/rspec-core-RSpec-Core-World.html.new[m
[1mindex 58fd5d2..e6f85cc 100644[m
[1m--- a/export/rspec-core-RSpec-Core-World.html.current[m
[1m+++ b/export/rspec-core-RSpec-Core-World.html.new[m
[36m@@ -2541,21 +2633,18 @@[m [mend[m
       [m
   [m
   [m
[32m+[m[41m    [m
 Prepares filters so that they apply to example groups when they run.Prepares filters so that they apply to example groups when they run.[m
 [m
[31m-This is a separate method so that filters can be modified/replaced and[m
[31m-examples refiltered during a process's lifetime, which can be useful for[m
[31m-a custom runner.This is a separate method so that filters can be modified/replaced and[m
[31m-examples refiltered during a process's lifetime, which can be useful for[m
[31m-a custom runner.[m
[32m+[m[32mThis is a separate method so that filters can be modified/replaced and examples refiltered during a process's lifetime, which can be useful for a custom runner.This is a separate method so that filters can be modified/replaced and examples refiltered during a process's lifetime, which can be useful for a custom runner.[m
[32m+[m
 [m
   [m
     [m
 Prepares filters so that they apply to example groups when they run.[m
 [m
[31m-This is a separate method so that filters can be modified/replaced and[m
[31m-examples refiltered during a process's lifetime, which can be useful for[m
[31m-a custom runner.[m
[32m+[m[32mThis is a separate method so that filters can be modified/replaced and examples refiltered during a process's lifetime, which can be useful for a custom runner.[m
[32m+[m
 [m
   [m
 [m
[36m@@ -2563,9 +2652,7 @@[m [ma custom runner.[m
     [m
 Prepares filters so that they apply to example groups when they run.[m
 [m
[31m-This is a separate method so that filters can be modified/replaced and[m
[31m-examples refiltered during a process's lifetime, which can be useful for[m
[31m-a custom runner.[m
[32m+[m[32mThis is a separate method so that filters can be modified/replaced and examples refiltered during a process's lifetime, which can be useful for a custom runner.[m
 [m
 [m
   [m
[36m@@ -2674,11 +2761,10 @@[m [mend[m
 [m
   [m
   [m
[32m+[m[41m    [m
 Prepares filters so that they apply to example groups when they run.[m
 [m
[31m-This is a separate method so that filters can be modified/replaced and[m
[31m-examples refiltered during a process's lifetime, which can be useful for[m
[31m-a custom runner.[m
[32m+[m[32mThis is a separate method so that filters can be modified/replaced and examples refiltered during a process's lifetime, which can be useful for a custom runner.[m
 [m
 [m
   [m
[36m@@ -3356,11 +3456,10 @@[m [mend[m
       [m
   [m
   [m
[32m+[m[41m    [m
 Prepares filters so that they apply to example groups when they run.[m
 [m
[31m-This is a separate method so that filters can be modified/replaced and[m
[31m-examples refiltered during a process's lifetime, which can be useful for[m
[31m-a custom runner.[m
[32m+[m[32mThis is a separate method so that filters can be modified/replaced and examples refiltered during a process's lifetime, which can be useful for a custom runner.[m
 [m
 [m
   [m
[36m@@ -4134,11 +4255,10 @@[m [mend[m
       [m
   [m
   [m
[32m+[m[41m    [m
 Prepares filters so that they apply to example groups when they run.[m
 [m
[31m-This is a separate method so that filters can be modified/replaced and[m
[31m-examples refiltered during a process's lifetime, which can be useful for[m
[31m-a custom runner.[m
[32m+[m[32mThis is a separate method so that filters can be modified/replaced and examples refiltered during a process's lifetime, which can be useful for a custom runner.[m
 [m
 [m
   [m
