[1mdiff --git a/export/rspec-core-RSpec-Core-Configuration.html.current b/export/rspec-core-RSpec-Core-Configuration.html.new[m
[1mindex f9579a4..ec51cfe 100644[m
[1m--- a/export/rspec-core-RSpec-Core-Configuration.html.current[m
[1m+++ b/export/rspec-core-RSpec-Core-Configuration.html.new[m
[36m@@ -123,65 +123,72 @@[m [mClass: RSpec::Core::Configuration[m
 [m
 OverviewOverview[m
   [m
[32m+[m[41m    [m
 Stores runtime configuration information.Stores runtime configuration information.[m
 [m
[31m-Configuration options are loaded from multiple files and joined together[m
[31m-with command-line switches and the SPEC_OPTSSPEC_OPTS environment variable.Configuration options are loaded from multiple files and joined together[m
[31m-with command-line switches and the SPEC_OPTS environment variable.[m
[32m+[m[32mConfiguration options are loaded from multiple files and joined together with command-line switches and the SPEC_OPTSSPEC_OPTS environment variable.Configuration options are loaded from multiple files and joined together with command-line switches and the SPEC_OPTS environment variable.[m
[32m+[m
[32m+[m[32mPrecedence order (where later entries overwrite earlier entries on conflicts):.Precedence order (where later entries overwrite earlier entries on conflicts):.[m
[32m+[m
[32m+[m[32mGlobal ($XDG_CONFIG_HOME/rspec/options, or ~/.rspec if it does not exist).Global ($XDG_CONFIG_HOME/rspec/options, or ~/.rspec if it does not exist).[m
[32m+[m
[32m+[m[32mGlobal ($XDG_CONFIG_HOME/rspec/options, or ~/.rspec if it does not exist).[m
[32m+[m
[32m+[m[32mProject-specific (./.rspec).Project-specific (./.rspec).[m
[32m+[m
[32m+[m[32mProject-specific (./.rspec).[m
[32m+[m
[32m+[m[32mLocal (./.rspec-local).Local (./.rspec-local).[m
[32m+[m
[32m+[m[32mLocal (./.rspec-local).[m
[32m+[m
[32m+[m[32mCommand-line options.Command-line options.[m
[32m+[m
[32m+[m[32mCommand-line options.[m
[32m+[m
[32m+[m[32mSPEC_OPTS.SPEC_OPTS.[m
[32m+[m
[32m+[m[32mSPEC_OPTS.[m
[32m+[m
[32m+[m[32mGlobal ($XDG_CONFIG_HOME/rspec/options, or ~/.rspec if it does not exist).[m
[32m+[m
[32m+[m[32mProject-specific (./.rspec).[m
 [m
[31m-Precedence order (where later entries overwrite earlier entries on[m
[31m-conflicts):.Precedence order (where later entries overwrite earlier entries on[m
[31m-conflicts):.[m
[32m+[m[32mLocal (./.rspec-local).[m
 [m
[32m+[m[32mCommand-line options.[m
 [m
[31m-Global ($XDG_CONFIG_HOME/rspec/options$XDG_CONFIG_HOME/rspec/options, or ~/.rspec~/.rspec if it does[m
[31m-not exist)Global ($XDG_CONFIG_HOME/rspec/options, or ~/.rspec if it does[m
[31m-not exist)[m
[31m-Project-specific (./.rspec./.rspec)Project-specific (./.rspec)[m
[31m-Local (./.rspec-local./.rspec-local)Local (./.rspec-local)[m
[31m-Command-line optionsCommand-line options[m
[31m-SPEC_OPTSSPEC_OPTSSPEC_OPTS[m
[32m+[m[32mSPEC_OPTS.[m
 [m
[31m-Global ($XDG_CONFIG_HOME/rspec/options, or ~/.rspec if it does[m
[31m-not exist)[m
[31m-Project-specific (./.rspec)[m
[31m-Local (./.rspec-local)[m
[31m-Command-line options[m
[31m-SPEC_OPTS[m
 [m
[32m+[m[32mFor example, an option set in the local file will override an option set in your global file.For example, an option set in the local file will override an option set in your global file.[m
 [m
[31m-For example, an option set in the local file will override an option set[m
[31m-in your global file.For example, an option set in the local file will override an option set[m
[31m-in your global file.[m
[32m+[m[32mThe global, project-specific and local files can all be overridden with a separate custom file using the –options command-line parameter.The global, project-specific and local files can all be overridden with a separate custom file using the –options command-line parameter.[m
 [m
[31m-The global, project-specific and local files can all be overridden with a[m
[31m-separate custom file using the --options command-line parameter.The global, project-specific and local files can all be overridden with a[m
[31m-separate custom file using the --options command-line parameter.[m
 [m
   [m
     [m
 Stores runtime configuration information.[m
 [m
[31m-Configuration options are loaded from multiple files and joined together[m
[31m-with command-line switches and the SPEC_OPTS environment variable.[m
[32m+[m[32mConfiguration options are loaded from multiple files and joined together with command-line switches and the SPEC_OPTS environment variable.[m
 [m
[31m-Precedence order (where later entries overwrite earlier entries on[m
[31m-conflicts):.[m
[32m+[m[32mPrecedence order (where later entries overwrite earlier entries on conflicts):.[m
 [m
[32m+[m[32mGlobal ($XDG_CONFIG_HOME/rspec/options, or ~/.rspec if it does not exist).[m
 [m
[31m-Global ($XDG_CONFIG_HOME/rspec/options, or ~/.rspec if it does[m
[31m-not exist)[m
[31m-Project-specific (./.rspec)[m
[31m-Local (./.rspec-local)[m
[31m-Command-line options[m
[31m-SPEC_OPTS[m
[32m+[m[32mProject-specific (./.rspec).[m
 [m
[32m+[m[32mLocal (./.rspec-local).[m
 [m
[31m-For example, an option set in the local file will override an option set[m
[31m-in your global file.[m
[32m+[m[32mCommand-line options.[m
[32m+[m
[32m+[m[32mSPEC_OPTS.[m
[32m+[m
[32m+[m
[32m+[m[32mFor example, an option set in the local file will override an option set in your global file.[m
[32m+[m
[32m+[m[32mThe global, project-specific and local files can all be overridden with a separate custom file using the –options command-line parameter.[m
 [m
[31m-The global, project-specific and local files can all be overridden with a[m
[31m-separate custom file using the --options command-line parameter.[m
 [m
   [m
 [m
[36m@@ -189,26 +196,24 @@[m [mseparate custom file using the --options command-line parameter.[m
     [m
 Stores runtime configuration information.[m
 [m
[31m-Configuration options are loaded from multiple files and joined together[m
[31m-with command-line switches and the SPEC_OPTS environment variable.[m
[32m+[m[32mConfiguration options are loaded from multiple files and joined together with command-line switches and the SPEC_OPTS environment variable.[m
[32m+[m
[32m+[m[32mPrecedence order (where later entries overwrite earlier entries on conflicts):.[m
[32m+[m
[32m+[m[32mGlobal ($XDG_CONFIG_HOME/rspec/options, or ~/.rspec if it does not exist).[m
[32m+[m
[32m+[m[32mProject-specific (./.rspec).[m
 [m
[31m-Precedence order (where later entries overwrite earlier entries on[m
[31m-conflicts):.[m
[32m+[m[32mLocal (./.rspec-local).[m
 [m
[32m+[m[32mCommand-line options.[m
 [m
[31m-Global ($XDG_CONFIG_HOME/rspec/options, or ~/.rspec if it does[m
[31m-not exist)[m
[31m-Project-specific (./.rspec)[m
[31m-Local (./.rspec-local)[m
[31m-Command-line options[m
[31m-SPEC_OPTS[m
[32m+[m[32mSPEC_OPTS.[m
 [m
 [m
[31m-For example, an option set in the local file will override an option set[m
[31m-in your global file.[m
[32m+[m[32mFor example, an option set in the local file will override an option set in your global file.[m
 [m
[31m-The global, project-specific and local files can all be overridden with a[m
[31m-separate custom file using the --options command-line parameter.[m
[32m+[m[32mThe global, project-specific and local files can all be overridden with a separate custom file using the –options command-line parameter.[m
 [m
 [m
   [m
[36m@@ -394,17 +411,12 @@[m [mDetermines which bisect runner implementation gets used to run subsets of the su[m
   [m
 [m
   [m
[31m-    Deprecated.Deprecated. No longer recommended because of complex behavior. Instead,[m
[31m-rely on the fact that TTYs will display color by default, or set[m
[31m-#color_mode to :on to display color on a non-TTY output.No longer recommended because of complex behavior. Instead,[m
[31m-rely on the fact that TTYs will display color by default, or set[m
[31m-#color_mode to :on to display color on a non-TTY output.[m
[31m-No longer recommended because of complex behavior. Instead,[m
[31m-rely on the fact that TTYs will display color by default, or set[m
[31m-#color_mode to :on to display color on a non-TTY output.[m
[31m-Deprecated. No longer recommended because of complex behavior. Instead,[m
[31m-rely on the fact that TTYs will display color by default, or set[m
[31m-#color_mode to :on to display color on a non-TTY output.[m
[32m+[m[32m    Deprecated.Deprecated.[m[41m [m
[32m+[m[32mNo longer recommended because of complex behavior. Instead, rely on the fact that TTYs will display color by default, or set #color_mode to :on to display color on a non-TTY output.No longer recommended because of complex behavior. Instead, rely on the fact that TTYs will display color by default, or set #color_mode to :on to display color on a non-TTY output.[m
[32m+[m
[32m+[m[32mNo longer recommended because of complex behavior. Instead, rely on the fact that TTYs will display color by default, or set #color_mode to :on to display color on a non-TTY output.[m
[32m+[m[32mDeprecated.[m[41m [m
[32m+[m[32mNo longer recommended because of complex behavior. Instead, rely on the fact that TTYs will display color by default, or set #color_mode to :on to display color on a non-TTY output.[m
 [m
   [m
 [m
[36m@@ -421,9 +433,9 @@[m [mrely on the fact that TTYs will display color by default, or set[m
   [m
 [m
   [m
[31m-    Deprecated. No longer recommended because of complex behavior. Instead,[m
[31m-rely on the fact that TTYs will display color by default, or set[m
[31m-#color_mode to :on to display color on a non-TTY output.[m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mNo longer recommended because of complex behavior. Instead, rely on the fact that TTYs will display color by default, or set #color_mode to :on to display color on a non-TTY output.[m
[32m+[m
   [m
 [m
 [m
[36m@@ -1235,12 +1326,12 @@[m [mDetermines where RSpec will send its output.[m
   [m
 [m
   [m
[31m-    Load files matching this pattern (default: '**{,/*/**}/*_spec.rb''**{,/*/**}/*_spec.rb').Load files matching this pattern (default: '**{,/*/**}/*_spec.rb').[m
[31m-Load files matching this pattern (default: '**{,/*/**}/*_spec.rb').[m
[31m-Load files matching this pattern (default: '**{,/*/**}/*_spec.rb').[m
     [m
[32m+[m[32mLoad files matching this pattern (default: '**{,//*/*}/*_spec.rb''**{,//*}/*_spec.rb').Load files matching this pattern (default: '**{,//*}/*_spec.rb').[m
 [m
[32m+[m[32mLoad files matching this pattern (default: '**{,//*}/*_spec.rb').[m
 [m
[32m+[m[32mLoad files matching this pattern (default: '**{,//*}/*_spec.rb').[m
 [m
   [m
 [m
[36m@@ -1254,7 +1345,12 @@[m [mLoad files matching this pattern (default: '**{,/*/**}/*_spec.rb').[m
   [m
   [m
   [m
[31m-    Load files matching this pattern (default: '**{,/*/**}/*_spec.rb').[m
[32m+[m[41m  [m
[32m+[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mLoad files matching this pattern (default: '**{,//*}/*_spec.rb').[m
[32m+[m
   [m
 [m
 [m
[36m@@ -1434,13 +1545,12 @@[m [mIndicates files configured to be required.[m
   [m
 [m
   [m
[31m-    Deprecated.Deprecated. Use #filter_run_when_matching instead for the specific[m
[31m-filters that you want to be ignored if none match.Use #filter_run_when_matching instead for the specific[m
[31m-filters that you want to be ignored if none match.[m
[31m-Use #filter_run_when_matching instead for the specific[m
[31m-filters that you want to be ignored if none match.[m
[31m-Deprecated. Use #filter_run_when_matching instead for the specific[m
[31m-filters that you want to be ignored if none match.[m
[32m+[m[32m    Deprecated.Deprecated.[m[41m [m
[32m+[m[32mUse #filter_run_when_matching instead for the specific filters that you want to be ignored if none match.Use #filter_run_when_matching instead for the specific filters that you want to be ignored if none match.[m
[32m+[m
[32m+[m[32mUse #filter_run_when_matching instead for the specific filters that you want to be ignored if none match.[m
[32m+[m[32mDeprecated.[m[41m [m
[32m+[m[32mUse #filter_run_when_matching instead for the specific filters that you want to be ignored if none match.[m
 [m
   [m
 [m
[36m@@ -1457,8 +1567,9 @@[m [mfilters that you want to be ignored if none match.[m
   [m
 [m
   [m
[31m-    Deprecated. Use #filter_run_when_matching instead for the specific[m
[31m-filters that you want to be ignored if none match.[m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mUse #filter_run_when_matching instead for the specific filters that you want to be ignored if none match.[m
[32m+[m
   [m
 [m
 [m
[36m@@ -1656,9 +1783,9 @@[m [mUse threadsafe options where available.[m
   [m
 [m
   [m
[31m-    Deprecated. No longer recommended because of complex behavior. Instead,[m
[31m-rely on the fact that TTYs will display color by default, or set[m
[31m-#color_mode to :on to display color on a non-TTY output.[m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mNo longer recommended because of complex behavior. Instead, rely on the fact that TTYs will display color by default, or set #color_mode to :on to display color on a non-TTY output.[m
[32m+[m
   [m
 [m
 [m
[36m@@ -2084,7 +2230,9 @@[m [mrely on the fact that TTYs will display color by default, or set[m
   [m
 [m
   [m
[31m-    Load files matching this pattern (default: '**{,/*/**}/*_spec.rb').[m
[32m+[m[41m    [m
[32m+[m[32mLoad files matching this pattern (default: '**{,//*}/*_spec.rb').[m
[32m+[m
   [m
 [m
 [m
[36m@@ -2186,8 +2337,9 @@[m [mrely on the fact that TTYs will display color by default, or set[m
   [m
 [m
   [m
[31m-    Deprecated. Use #filter_run_when_matching instead for the specific[m
[31m-filters that you want to be ignored if none match.[m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mUse #filter_run_when_matching instead for the specific filters that you want to be ignored if none match.[m
[32m+[m
   [m
 [m
 [m
[36m@@ -2465,9 +2640,12 @@[m [mCreates a method that delegates to example including the submitted args.[m
   [m
 [m
   [m
[31m-    Define an alias for it_should_behave_like that allows different language (like "it_has_behavior" or "it_behaves_like") to be employed when including shared examples.Define an alias for it_should_behave_like that allows different language (like "it_has_behavior" or "it_behaves_like") to be employed when including shared examples.[m
[31m-Define an alias for it_should_behave_like that allows different language (like "it_has_behavior" or "it_behaves_like") to be employed when including shared examples.[m
[31m-Define an alias for it_should_behave_like that allows different language (like "it_has_behavior" or "it_behaves_like") to be employed when including shared examples.[m
[32m+[m[41m    [m
[32m+[m[32mDefine an alias for it_should_behave_like that allows different language (like “it_has_behavior” or “it_behaves_like”) to be employed when including shared examples.Define an alias for it_should_behave_like that allows different language (like “it_has_behavior” or “it_behaves_like”) to be employed when including shared examples.[m
[32m+[m
[32m+[m[32mDefine an alias for it_should_behave_like that allows different language (like “it_has_behavior” or “it_behaves_like”) to be employed when including shared examples.[m
[32m+[m
[32m+[m[32mDefine an alias for it_should_behave_like that allows different language (like “it_has_behavior” or “it_behaves_like”) to be employed when including shared examples.[m
 [m
   [m
 [m
[36m@@ -2481,7 +2659,9 @@[m [mDefine an alias for it_should_behave_like that allows different language (like "[m
   [m
 [m
   [m
[31m-    Define an alias for it_should_behave_like that allows different language (like "it_has_behavior" or "it_behaves_like") to be employed when including shared examples.[m
[32m+[m[41m    [m
[32m+[m[32mDefine an alias for it_should_behave_like that allows different language (like “it_has_behavior” or “it_behaves_like”) to be employed when including shared examples.[m
[32m+[m
   [m
 [m
 [m
[36m@@ -4218,17 +4605,12 @@[m [mSets the seed value and sets the default global ordering to random.[m
   [m
 [m
   [m
[31m-    Deprecated.Deprecated. This config option was added in RSpec 2 to pave the way[m
[31m-for this being the default behavior in RSpec 3. Now this option is[m
[31m-a no-op.This config option was added in RSpec 2 to pave the way[m
[31m-for this being the default behavior in RSpec 3. Now this option is[m
[31m-a no-op.[m
[31m-This config option was added in RSpec 2 to pave the way[m
[31m-for this being the default behavior in RSpec 3. Now this option is[m
[31m-a no-op.[m
[31m-Deprecated. This config option was added in RSpec 2 to pave the way[m
[31m-for this being the default behavior in RSpec 3. Now this option is[m
[31m-a no-op.[m
[32m+[m[32m    Deprecated.Deprecated.[m[41m [m
[32m+[m[32mThis config option was added in RSpec 2 to pave the way for this being the default behavior in RSpec 3. Now this option is a no-op.This config option was added in RSpec 2 to pave the way for this being the default behavior in RSpec 3. Now this option is a no-op.[m
[32m+[m
[32m+[m[32mThis config option was added in RSpec 2 to pave the way for this being the default behavior in RSpec 3. Now this option is a no-op.[m
[32m+[m[32mDeprecated.[m[41m [m
[32m+[m[32mThis config option was added in RSpec 2 to pave the way for this being the default behavior in RSpec 3. Now this option is a no-op.[m
 [m
   [m
 [m
[36m@@ -4242,9 +4624,9 @@[m [ma no-op.[m
   [m
 [m
   [m
[31m-    Deprecated. This config option was added in RSpec 2 to pave the way[m
[31m-for this being the default behavior in RSpec 3. Now this option is[m
[31m-a no-op.[m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mThis config option was added in RSpec 2 to pave the way for this being the default behavior in RSpec 3. Now this option is a no-op.[m
[32m+[m
   [m
 [m
 [m
[36m@@ -4447,7 +4845,9 @@[m [mDefines a callback that runs after the first example with matching metadata is d[m
   [m
 [m
   [m
[31m-    Define an alias for it_should_behave_like that allows different language (like "it_has_behavior" or "it_behaves_like") to be employed when including shared examples.[m
[32m+[m[41m    [m
[32m+[m[32mDefine an alias for it_should_behave_like that allows different language (like “it_has_behavior” or “it_behaves_like”) to be employed when including shared examples.[m
[32m+[m
   [m
 [m
 [m
[36m@@ -5350,9 +5801,9 @@[m [mDefines a callback that runs after the first example with matching metadata is d[m
   [m
 [m
   [m
[31m-    Deprecated. This config option was added in RSpec 2 to pave the way[m
[31m-for this being the default behavior in RSpec 3. Now this option is[m
[31m-a no-op.[m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mThis config option was added in RSpec 2 to pave the way for this being the default behavior in RSpec 3. Now this option is a no-op.[m
[32m+[m
   [m
 [m
 [m
[36m@@ -5627,12 +6083,12 @@[m [ma no-op.[m
     [m
       # File 'lib/rspec/core/configuration.rb', line 500# File 'lib/rspec/core/configuration.rb', line 500[m
 [m
[31m-defdef initializeinitialize[m
[32m+[m[32mdefdef initializeinitialize  # rubocop:disable Style/GlobalVars[m
 # rubocop:disable Style/GlobalVars[m
[31m-# rubocop:disable Style/GlobalVars[m
[31m-  @start_time@start_time == $_rspec_core_load_started_at$_rspec_core_load_started_at |||| ::::RSpecRSpec::::CoreCore::::TimeTime..nownow[m
[31m-  # rubocop:enable Style/GlobalVars[m
[32m+[m
[32m+[m[32m  @start_time@start_time == $_rspec_core_load_started_at$_rspec_core_load_started_at |||| ::::RSpecRSpec::::CoreCore::::TimeTime..nownow  # rubocop:enable Style/GlobalVars[m
 # rubocop:enable Style/GlobalVars[m
[32m+[m
   @expectation_frameworks@expectation_frameworks == [[]][m
   @include_modules@include_modules == FilterableItemRepositoryFilterableItemRepository::::QueryOptimizedQueryOptimized..newnew((:any?:any?))[m
   @extend_modules@extend_modules  == FilterableItemRepositoryFilterableItemRepository::::QueryOptimizedQueryOptimized..newnew((:any?:any?))[m
[36m@@ -5685,10 +6141,10 @@[m [mdefdef initializeinitialize[m
   define_built_in_hooksdefine_built_in_hooks[m
 endend# File 'lib/rspec/core/configuration.rb', line 500[m
 [m
[31m-def initialize[m
[31m-  # rubocop:disable Style/GlobalVars[m
[31m-  @start_time = $_rspec_core_load_started_at || ::RSpec::Core::Time.now[m
[31m-  # rubocop:enable Style/GlobalVars[m
[32m+[m[32mdef initialize  # rubocop:disable Style/GlobalVars[m
[32m+[m
[32m+[m[32m  @start_time = $_rspec_core_load_started_at || ::RSpec::Core::Time.now  # rubocop:enable Style/GlobalVars[m
[32m+[m
   @expectation_frameworks = [][m
   @include_modules = FilterableItemRepository::QueryOptimized.new(:any?)[m
   @extend_modules  = FilterableItemRepository::QueryOptimized.new(:any?)[m
[36m@@ -5743,10 +6199,10 @@[m [mend[m
     [m
       # File 'lib/rspec/core/configuration.rb', line 500[m
 [m
[31m-def initialize[m
[31m-  # rubocop:disable Style/GlobalVars[m
[31m-  @start_time = $_rspec_core_load_started_at || ::RSpec::Core::Time.now[m
[31m-  # rubocop:enable Style/GlobalVars[m
[32m+[m[32mdef initialize  # rubocop:disable Style/GlobalVars[m
[32m+[m
[32m+[m[32m  @start_time = $_rspec_core_load_started_at || ::RSpec::Core::Time.now  # rubocop:enable Style/GlobalVars[m
[32m+[m
   @expectation_frameworks = [][m
   @include_modules = FilterableItemRepository::QueryOptimized.new(:any?)[m
   @extend_modules  = FilterableItemRepository::QueryOptimized.new(:any?)[m
[36m@@ -5863,10 +6319,10 @@[m [mend[m
     [m
       # File 'lib/rspec/core/configuration.rb', line 500[m
 [m
[31m-def initialize[m
[31m-  # rubocop:disable Style/GlobalVars[m
[31m-  @start_time = $_rspec_core_load_started_at || ::RSpec::Core::Time.now[m
[31m-  # rubocop:enable Style/GlobalVars[m
[32m+[m[32mdef initialize  # rubocop:disable Style/GlobalVars[m
[32m+[m
[32m+[m[32m  @start_time = $_rspec_core_load_started_at || ::RSpec::Core::Time.now  # rubocop:enable Style/GlobalVars[m
[32m+[m
   @expectation_frameworks = [][m
   @include_modules = FilterableItemRepository::QueryOptimized.new(:any?)[m
   @extend_modules  = FilterableItemRepository::QueryOptimized.new(:any?)[m
[36m@@ -5985,10 +6441,10 @@[m [mend[m
     [m
       # File 'lib/rspec/core/configuration.rb', line 500[m
 [m
[31m-def initialize[m
[31m-  # rubocop:disable Style/GlobalVars[m
[31m-  @start_time = $_rspec_core_load_started_at || ::RSpec::Core::Time.now[m
[31m-  # rubocop:enable Style/GlobalVars[m
[32m+[m[32mdef initialize  # rubocop:disable Style/GlobalVars[m
[32m+[m
[32m+[m[32m  @start_time = $_rspec_core_load_started_at || ::RSpec::Core::Time.now  # rubocop:enable Style/GlobalVars[m
[32m+[m
   @expectation_frameworks = [][m
   @include_modules = FilterableItemRepository::QueryOptimized.new(:any?)[m
   @extend_modules  = FilterableItemRepository::QueryOptimized.new(:any?)[m
[36m@@ -6119,10 +6576,10 @@[m [mend[m
     [m
       # File 'lib/rspec/core/configuration.rb', line 500[m
 [m
[31m-def initialize[m
[31m-  # rubocop:disable Style/GlobalVars[m
[31m-  @start_time = $_rspec_core_load_started_at || ::RSpec::Core::Time.now[m
[31m-  # rubocop:enable Style/GlobalVars[m
[32m+[m[32mdef initialize  # rubocop:disable Style/GlobalVars[m
[32m+[m
[32m+[m[32m  @start_time = $_rspec_core_load_started_at || ::RSpec::Core::Time.now  # rubocop:enable Style/GlobalVars[m
[32m+[m
   @expectation_frameworks = [][m
   @include_modules = FilterableItemRepository::QueryOptimized.new(:any?)[m
   @extend_modules  = FilterableItemRepository::QueryOptimized.new(:any?)[m
[36m@@ -6258,10 +6716,10 @@[m [mend[m
     [m
       # File 'lib/rspec/core/configuration.rb', line 500[m
 [m
[31m-def initialize[m
[31m-  # rubocop:disable Style/GlobalVars[m
[31m-  @start_time = $_rspec_core_load_started_at || ::RSpec::Core::Time.now[m
[31m-  # rubocop:enable Style/GlobalVars[m
[32m+[m[32mdef initialize  # rubocop:disable Style/GlobalVars[m
[32m+[m
[32m+[m[32m  @start_time = $_rspec_core_load_started_at || ::RSpec::Core::Time.now  # rubocop:enable Style/GlobalVars[m
[32m+[m
   @expectation_frameworks = [][m
   @include_modules = FilterableItemRepository::QueryOptimized.new(:any?)[m
   @extend_modules  = FilterableItemRepository::QueryOptimized.new(:any?)[m
[36m@@ -6331,56 +6789,32 @@[m [mend[m
     [m
   [m
     Note:Note:[m
[31m-    This option will only be used by --bisect--bisect if you set it in a file[m
[31m-loaded via --require--require.This option will only be used by --bisect if you set it in a file[m
[31m-loaded via --require.[m
[31m-This option will only be used by --bisect if you set it in a file[m
[31m-loaded via --require.[m
[32m+[m[41m    [m
[32m+[m[32mThis option will only be used by --bisect--bisect if you set it in a file loaded via --require--require.This option will only be used by --bisect if you set it in a file loaded via --require.[m
[32m+[m
[32m+[m[32mThis option will only be used by --bisect if you set it in a file loaded via --require.[m
 [m
   [m
     Note:[m
[31m-    This option will only be used by --bisect if you set it in a file[m
[31m-loaded via --require.[m
     [m
[32m+[m[32mThis option will only be used by --bisect if you set it in a file loaded via --require.[m
[32m+[m
[32m+[m[41m  [m
[32m+[m
[32m+[m
[32m+[m[32mDetermines which bisect runner implementation gets used to run subsets of the suite during a bisection. Your choices are:.Determines which bisect runner implementation gets used to run subsets of the suite during a bisection. Your choices are:.[m
 [m
[32m+[m[32m:shell:shell: Performs a spec run by shelling out, booting RSpec and your application environment each time. This runner is the most widely compatible runner, but is not as fast. On platforms that do not support forking, this is the default.:shell: Performs a spec run by shelling out, booting RSpec and your application environment each time. This runner is the most widely compatible runner, but is not as fast. On platforms that do not support forking, this is the default.[m
 [m
[31m-Determines which bisect runner implementation gets used to run subsets[m
[31m-of the suite during a bisection. Your choices are:.Determines which bisect runner implementation gets used to run subsets[m
[31m-of the suite during a bisection. Your choices are:.[m
[32m+[m[32m:shell: Performs a spec run by shelling out, booting RSpec and your application environment each time. This runner is the most widely compatible runner, but is not as fast. On platforms that do not support forking, this is the default.[m
 [m
[32m+[m[32m:fork:fork: Pre-boots RSpec and your application environment in a parent process, and then forks a child process for each spec run. This runner tends to be significantly faster than the :shell:shell runner but cannot be used in some situations. On platforms that support forking, this is the default. If you use this runner, you should ensure that all of your one-time setup logic goes in a before(:suite)before(:suite) hook instead of getting run at the top-level of a file loaded by --require--require.:fork: Pre-boots RSpec and your application environment in a parent process, and then forks a child process for each spec run. This runner tends to be significantly faster than the :shell runner but cannot be used in some situations. On platforms that support forking, this is the default. If you use this runner, you should ensure that all of your one-time setup logic goes in a before(:suite) hook instead of getting run at the top-level of a file loaded by --require.[m
 [m
[31m-:shell:shell: Performs a spec run by shelling out, booting RSpec and your[m
[31m-application environment each time. This runner is the most widely[m
[31m-compatible runner, but is not as fast. On platforms that do not[m
[31m-support forking, this is the default.:shell: Performs a spec run by shelling out, booting RSpec and your[m
[31m-application environment each time. This runner is the most widely[m
[31m-compatible runner, but is not as fast. On platforms that do not[m
[31m-support forking, this is the default.[m
[31m-:fork:fork: Pre-boots RSpec and your application environment in a parent[m
[31m-process, and then forks a child process for each spec run. This runner[m
[31m-tends to be significantly faster than the :shell:shell runner but cannot[m
[31m-be used in some situations. On platforms that support forking, this[m
[31m-is the default. If you use this runner, you should ensure that all[m
[31m-of your one-time setup logic goes in a before(:suite)before(:suite) hook instead[m
[31m-of getting run at the top-level of a file loaded by --require--require.:fork: Pre-boots RSpec and your application environment in a parent[m
[31m-process, and then forks a child process for each spec run. This runner[m
[31m-tends to be significantly faster than the :shell runner but cannot[m
[31m-be used in some situations. On platforms that support forking, this[m
[31m-is the default. If you use this runner, you should ensure that all[m
[31m-of your one-time setup logic goes in a before(:suite) hook instead[m
[31m-of getting run at the top-level of a file loaded by --require.[m
[32m+[m[32m:fork: Pre-boots RSpec and your application environment in a parent process, and then forks a child process for each spec run. This runner tends to be significantly faster than the :shell runner but cannot be used in some situations. On platforms that support forking, this is the default. If you use this runner, you should ensure that all of your one-time setup logic goes in a before(:suite) hook instead of getting run at the top-level of a file loaded by --require.[m
 [m
[31m-:shell: Performs a spec run by shelling out, booting RSpec and your[m
[31m-application environment each time. This runner is the most widely[m
[31m-compatible runner, but is not as fast. On platforms that do not[m
[31m-support forking, this is the default.[m
[31m-:fork: Pre-boots RSpec and your application environment in a parent[m
[31m-process, and then forks a child process for each spec run. This runner[m
[31m-tends to be significantly faster than the :shell runner but cannot[m
[31m-be used in some situations. On platforms that support forking, this[m
[31m-is the default. If you use this runner, you should ensure that all[m
[31m-of your one-time setup logic goes in a before(:suite) hook instead[m
[31m-of getting run at the top-level of a file loaded by --require.[m
[32m+[m[32m:shell: Performs a spec run by shelling out, booting RSpec and your application environment each time. This runner is the most widely compatible runner, but is not as fast. On platforms that do not support forking, this is the default.[m
[32m+[m
[32m+[m[32m:fork: Pre-boots RSpec and your application environment in a parent process, and then forks a child process for each spec run. This runner tends to be significantly faster than the :shell runner but cannot be used in some situations. On platforms that support forking, this is the default. If you use this runner, you should ensure that all of your one-time setup logic goes in a before(:suite) hook instead of getting run at the top-level of a file loaded by --require.[m
 [m
 [m
 [m
[36m@@ -6388,26 +6822,17 @@[m [mof getting run at the top-level of a file loaded by --require.[m
     [m
   [m
     Note:[m
[31m-    This option will only be used by --bisect if you set it in a file[m
[31m-loaded via --require.[m
[32m+[m[41m    [m
[32m+[m[32mThis option will only be used by --bisect if you set it in a file loaded via --require.[m
[32m+[m
   [m
 [m
 [m
[31m-Determines which bisect runner implementation gets used to run subsets[m
[31m-of the suite during a bisection. Your choices are:.[m
[32m+[m[32mDetermines which bisect runner implementation gets used to run subsets of the suite during a bisection. Your choices are:.[m
 [m
[32m+[m[32m:shell: Performs a spec run by shelling out, booting RSpec and your application environment each time. This runner is the most widely compatible runner, but is not as fast. On platforms that do not support forking, this is the default.[m
 [m
[31m-:shell: Performs a spec run by shelling out, booting RSpec and your[m
[31m-application environment each time. This runner is the most widely[m
[31m-compatible runner, but is not as fast. On platforms that do not[m
[31m-support forking, this is the default.[m
[31m-:fork: Pre-boots RSpec and your application environment in a parent[m
[31m-process, and then forks a child process for each spec run. This runner[m
[31m-tends to be significantly faster than the :shell runner but cannot[m
[31m-be used in some situations. On platforms that support forking, this[m
[31m-is the default. If you use this runner, you should ensure that all[m
[31m-of your one-time setup logic goes in a before(:suite) hook instead[m
[31m-of getting run at the top-level of a file loaded by --require.[m
[32m+[m[32m:fork: Pre-boots RSpec and your application environment in a parent process, and then forks a child process for each spec run. This runner tends to be significantly faster than the :shell runner but cannot be used in some situations. On platforms that support forking, this is the default. If you use this runner, you should ensure that all of your one-time setup logic goes in a before(:suite) hook instead of getting run at the top-level of a file loaded by --require.[m
 [m
 [m
 [m
[36m@@ -6417,26 +6842,17 @@[m [mof getting run at the top-level of a file loaded by --require.[m
     [m
   [m
     Note:[m
[31m-    This option will only be used by --bisect if you set it in a file[m
[31m-loaded via --require.[m
     [m
[32m+[m[32mThis option will only be used by --bisect if you set it in a file loaded via --require.[m
 [m
   [m
[31m-Determines which bisect runner implementation gets used to run subsets[m
[31m-of the suite during a bisection. Your choices are:.[m
 [m
 [m
[31m-:shell: Performs a spec run by shelling out, booting RSpec and your[m
[31m-application environment each time. This runner is the most widely[m
[31m-compatible runner, but is not as fast. On platforms that do not[m
[31m-support forking, this is the default.[m
[31m-:fork: Pre-boots RSpec and your application environment in a parent[m
[31m-process, and then forks a child process for each spec run. This runner[m
[31m-tends to be significantly faster than the :shell runner but cannot[m
[31m-be used in some situations. On platforms that support forking, this[m
[31m-is the default. If you use this runner, you should ensure that all[m
[31m-of your one-time setup logic goes in a before(:suite) hook instead[m
[31m-of getting run at the top-level of a file loaded by --require.[m
[32m+[m[32mDetermines which bisect runner implementation gets used to run subsets of the suite during a bisection. Your choices are:.[m
[32m+[m
[32m+[m[32m:shell: Performs a spec run by shelling out, booting RSpec and your application environment each time. This runner is the most widely compatible runner, but is not as fast. On platforms that do not support forking, this is the default.[m
[32m+[m
[32m+[m[32m:fork: Pre-boots RSpec and your application environment in a parent process, and then forks a child process for each spec run. This runner tends to be significantly faster than the :shell runner but cannot be used in some situations. On platforms that support forking, this is the default. If you use this runner, you should ensure that all of your one-time setup logic goes in a before(:suite) hook instead of getting run at the top-level of a file loaded by --require.[m
 [m
 [m
 [m
[36m@@ -6573,26 +6989,17 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    This option will only be used by --bisect if you set it in a file[m
[31m-loaded via --require.[m
[32m+[m[41m    [m
[32m+[m[32mThis option will only be used by --bisect if you set it in a file loaded via --require.[m
 [m
   [m
 [m
[31m-Determines which bisect runner implementation gets used to run subsets[m
[31m-of the suite during a bisection. Your choices are:.[m
 [m
[32m+[m[32mDetermines which bisect runner implementation gets used to run subsets of the suite during a bisection. Your choices are:.[m
 [m
[31m-:shell: Performs a spec run by shelling out, booting RSpec and your[m
[31m-application environment each time. This runner is the most widely[m
[31m-compatible runner, but is not as fast. On platforms that do not[m
[31m-support forking, this is the default.[m
[31m-:fork: Pre-boots RSpec and your application environment in a parent[m
[31m-process, and then forks a child process for each spec run. This runner[m
[31m-tends to be significantly faster than the :shell runner but cannot[m
[31m-be used in some situations. On platforms that support forking, this[m
[31m-is the default. If you use this runner, you should ensure that all[m
[31m-of your one-time setup logic goes in a before(:suite) hook instead[m
[31m-of getting run at the top-level of a file loaded by --require.[m
[32m+[m[32m:shell: Performs a spec run by shelling out, booting RSpec and your application environment each time. This runner is the most widely compatible runner, but is not as fast. On platforms that do not support forking, this is the default.[m
[32m+[m
[32m+[m[32m:fork: Pre-boots RSpec and your application environment in a parent process, and then forks a child process for each spec run. This runner tends to be significantly faster than the :shell runner but cannot be used in some situations. On platforms that support forking, this is the default. If you use this runner, you should ensure that all of your one-time setup logic goes in a before(:suite) hook instead of getting run at the top-level of a file loaded by --require.[m
 [m
 [m
 [m
[36m@@ -6640,45 +7047,33 @@[m [mend[m
       [m
   [m
   [m
[31m-    Deprecated.Deprecated. No longer recommended because of complex behavior. Instead,[m
[31m-rely on the fact that TTYs will display color by default, or set[m
[31m-#color_mode to :on to display color on a non-TTY output.No longer recommended because of complex behavior. Instead,[m
[31m-rely on the fact that TTYs will display color by default, or set[m
[31m-#color_mode to :on to display color on a non-TTY output.[m
[31m-No longer recommended because of complex behavior. Instead,[m
[31m-rely on the fact that TTYs will display color by default, or set[m
[31m-#color_mode to :on to display color on a non-TTY output.[m
[31m-Deprecated. No longer recommended because of complex behavior. Instead,[m
[31m-rely on the fact that TTYs will display color by default, or set[m
[31m-#color_mode to :on to display color on a non-TTY output.[m
[32m+[m[32m    Deprecated.Deprecated.[m[41m [m
[32m+[m[32mNo longer recommended because of complex behavior. Instead, rely on the fact that TTYs will display color by default, or set #color_mode to :on to display color on a non-TTY output.No longer recommended because of complex behavior. Instead, rely on the fact that TTYs will display color by default, or set #color_mode to :on to display color on a non-TTY output.[m
[32m+[m
[32m+[m[32mNo longer recommended because of complex behavior. Instead, rely on the fact that TTYs will display color by default, or set #color_mode to :on to display color on a non-TTY output.[m
[32m+[m[32mDeprecated.[m[41m [m
[32m+[m[32mNo longer recommended because of complex behavior. Instead, rely on the fact that TTYs will display color by default, or set #color_mode to :on to display color on a non-TTY output.[m
[32m+[m
[32m+[m
[32m+[m[32mEnables color output if the output is a TTY. As of RSpec 3.6, this is the default behavior and this option is retained only for backwards compatibility.Enables color output if the output is a TTY. As of RSpec 3.6, this is the default behavior and this option is retained only for backwards compatibility.[m
 [m
[31m-Enables color output if the output is a TTY.  As of RSpec 3.6, this is[m
[31m-the default behavior and this option is retained only for backwards[m
[31m-compatibility.Enables color output if the output is a TTY.  As of RSpec 3.6, this is[m
[31m-the default behavior and this option is retained only for backwards[m
[31m-compatibility.[m
 [m
   [m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mNo longer recommended because of complex behavior. Instead, rely on the fact that TTYs will display color by default, or set #color_mode to :on to display color on a non-TTY output.[m
 [m
[31m-    Deprecated. No longer recommended because of complex behavior. Instead,[m
[31m-rely on the fact that TTYs will display color by default, or set[m
[31m-#color_mode to :on to display color on a non-TTY output.[m
 [m
[31m-Enables color output if the output is a TTY.  As of RSpec 3.6, this is[m
[31m-the default behavior and this option is retained only for backwards[m
[31m-compatibility.[m
[32m+[m[32mEnables color output if the output is a TTY. As of RSpec 3.6, this is the default behavior and this option is retained only for backwards compatibility.[m
 [m
 [m
   [m
 [m
   [m
[31m-    Deprecated. No longer recommended because of complex behavior. Instead,[m
[31m-rely on the fact that TTYs will display color by default, or set[m
[31m-#color_mode to :on to display color on a non-TTY output.[m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mNo longer recommended because of complex behavior. Instead, rely on the fact that TTYs will display color by default, or set #color_mode to :on to display color on a non-TTY output.[m
 [m
[31m-Enables color output if the output is a TTY.  As of RSpec 3.6, this is[m
[31m-the default behavior and this option is retained only for backwards[m
[31m-compatibility.[m
[32m+[m
[32m+[m[32mEnables color output if the output is a TTY. As of RSpec 3.6, this is the default behavior and this option is retained only for backwards compatibility.[m
 [m
 [m
   [m
[36m@@ -6835,13 +7230,11 @@[m [mend[m
 [m
   [m
   [m
[31m-    Deprecated. No longer recommended because of complex behavior. Instead,[m
[31m-rely on the fact that TTYs will display color by default, or set[m
[31m-#color_mode to :on to display color on a non-TTY output.[m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mNo longer recommended because of complex behavior. Instead, rely on the fact that TTYs will display color by default, or set #color_mode to :on to display color on a non-TTY output.[m
[32m+[m
 [m
[31m-Enables color output if the output is a TTY.  As of RSpec 3.6, this is[m
[31m-the default behavior and this option is retained only for backwards[m
[31m-compatibility.[m
[32m+[m[32mEnables color output if the output is a TTY. As of RSpec 3.6, this is the default behavior and this option is retained only for backwards compatibility.[m
 [m
 [m
   [m
[36m@@ -6897,30 +7290,39 @@[m [mend[m
       [m
   [m
   [m
[32m+[m[41m    [m
 The mode for determining whether to display output in color. One of:.The mode for determining whether to display output in color. One of:.[m
 [m
[32m+[m[32m:automatic - the output will be in color if the output is a TTY (the  default).:automatic - the output will be in color if the output is a TTY (the  default).[m
[32m+[m
[32m+[m[32m:automatic - the output will be in color if the output is a TTY (the  default).[m
[32m+[m
[32m+[m[32m:on - the output will be in color, whether or not the output is a TTY.:on - the output will be in color, whether or not the output is a TTY.[m
[32m+[m
[32m+[m[32m:on - the output will be in color, whether or not the output is a TTY.[m
[32m+[m
[32m+[m[32m:off - the output will not be in color.:off - the output will not be in color.[m
[32m+[m
[32m+[m[32m:off - the output will not be in color.[m
[32m+[m
[32m+[m[32m:automatic - the output will be in color if the output is a TTY (the  default).[m
[32m+[m
[32m+[m[32m:on - the output will be in color, whether or not the output is a TTY.[m
 [m
[31m-:automatic - the output will be in color if the output is a TTY (the[m
[31m-default):automatic - the output will be in color if the output is a TTY (the[m
[31m-default)[m
[31m-:on - the output will be in color, whether or not the output is a TTY:on - the output will be in color, whether or not the output is a TTY[m
[31m-:off - the output will not be in color:off - the output will not be in color[m
[32m+[m[32m:off - the output will not be in color.[m
 [m
[31m-:automatic - the output will be in color if the output is a TTY (the[m
[31m-default)[m
[31m-:on - the output will be in color, whether or not the output is a TTY[m
[31m-:off - the output will not be in color[m
 [m
 [m
   [m
     [m
 The mode for determining whether to display output in color. One of:.[m
 [m
[32m+[m[32m:automatic - the output will be in color if the output is a TTY (the  default).[m
[32m+[m
[32m+[m[32m:on - the output will be in color, whether or not the output is a TTY.[m
[32m+[m
[32m+[m[32m:off - the output will not be in color.[m
 [m
[31m-:automatic - the output will be in color if the output is a TTY (the[m
[31m-default)[m
[31m-:on - the output will be in color, whether or not the output is a TTY[m
[31m-:off - the output will not be in color[m
 [m
 [m
   [m
[36m@@ -6929,11 +7331,11 @@[m [mdefault)[m
     [m
 The mode for determining whether to display output in color. One of:.[m
 [m
[32m+[m[32m:automatic - the output will be in color if the output is a TTY (the  default).[m
 [m
[31m-:automatic - the output will be in color if the output is a TTY (the[m
[31m-default)[m
[31m-:on - the output will be in color, whether or not the output is a TTY[m
[31m-:off - the output will not be in color[m
[32m+[m[32m:on - the output will be in color, whether or not the output is a TTY.[m
[32m+[m
[32m+[m[32m:off - the output will not be in color.[m
 [m
 [m
 [m
[36m@@ -7085,13 +7487,14 @@[m [mend[m
 [m
   [m
   [m
[32m+[m[41m    [m
 The mode for determining whether to display output in color. One of:.[m
 [m
[32m+[m[32m:automatic - the output will be in color if the output is a TTY (the  default).[m
 [m
[31m-:automatic - the output will be in color if the output is a TTY (the[m
[31m-default)[m
[31m-:on - the output will be in color, whether or not the output is a TTY[m
[31m-:off - the output will not be in color[m
[32m+[m[32m:on - the output will be in color, whether or not the output is a TTY.[m
[32m+[m
[32m+[m[32m:off - the output will not be in color.[m
 [m
 [m
 [m
[36m@@ -7146,25 +7549,20 @@[m [mend[m
       [m
   [m
   [m
[31m-    The default output color. Defaults to :white but can be set to one of[m
[31m-the following: [:black, :white, :red, :green, :yellow, :blue,[m
[31m-:magenta, :cyan].The default output color. Defaults to :white but can be set to one of[m
[31m-the following: [:black, :white, :red, :green, :yellow, :blue,[m
[31m-:magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mThe default output color. Defaults to :white but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].The default output color. Defaults to :white but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
[32m+[m
 [m
   [m
     [m
[31m-    The default output color. Defaults to :white but can be set to one of[m
[31m-the following: [:black, :white, :red, :green, :yellow, :blue,[m
[31m-:magenta, :cyan].[m
[32m+[m[32mThe default output color. Defaults to :white but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
 [m
   [m
[31m-    The default output color. Defaults to :white but can be set to one of[m
[31m-the following: [:black, :white, :red, :green, :yellow, :blue,[m
[31m-:magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mThe default output color. Defaults to :white but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -7277,9 +7675,8 @@[m [madd_setting :default_color[m
 [m
   [m
   [m
[31m-    The default output color. Defaults to :white but can be set to one of[m
[31m-the following: [:black, :white, :red, :green, :yellow, :blue,[m
[31m-:magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mThe default output color. Defaults to :white but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -7325,38 +7722,33 @@[m [madd_setting :default_color[m
     [m
   [m
     Note:Note:[m
[31m-    Other scripts invoking rspecrspec indirectly will ignore this[m
[31m-setting.Other scripts invoking rspec indirectly will ignore this[m
[31m-setting.[m
[31m-Other scripts invoking rspec indirectly will ignore this[m
[31m-setting.[m
[32m+[m[41m    [m
[32m+[m[32mOther scripts invoking rspecrspec indirectly will ignore this setting.Other scripts invoking rspec indirectly will ignore this setting.[m
[32m+[m
[32m+[m[32mOther scripts invoking rspec indirectly will ignore this setting.[m
 [m
   [m
     Note:[m
[31m-    Other scripts invoking rspec indirectly will ignore this[m
[31m-setting.[m
[32m+[m[41m    [m
[32m+[m[32mOther scripts invoking rspec indirectly will ignore this setting.[m
[32m+[m
   [m
 [m
 [m
[31m-Path to use if no path is provided to the rspecrspec command (default:[m
[31m-"spec""spec"). Allows you to just type rspecrspec instead of rspec specrspec spec to[m
[31m-run all the examples in the specspec directory.Path to use if no path is provided to the rspec command (default:[m
[31m-"spec"). Allows you to just type rspec instead of rspec spec to[m
[31m-run all the examples in the spec directory.[m
[32m+[m[32mPath to use if no path is provided to the rspecrspec command (default: "spec""spec"). Allows you to just type rspecrspec instead of rspec specrspec spec to run all the examples in the specspec directory.Path to use if no path is provided to the rspec command (default: "spec"). Allows you to just type rspec instead of rspec spec to run all the examples in the spec directory.[m
 [m
 [m
   [m
     [m
   [m
     Note:[m
[31m-    Other scripts invoking rspec indirectly will ignore this[m
[31m-setting.[m
     [m
[32m+[m[32mOther scripts invoking rspec indirectly will ignore this setting.[m
 [m
   [m
[31m-Path to use if no path is provided to the rspec command (default:[m
[31m-"spec"). Allows you to just type rspec instead of rspec spec to[m
[31m-run all the examples in the spec directory.[m
[32m+[m
[32m+[m
[32m+[m[32mPath to use if no path is provided to the rspec command (default: "spec"). Allows you to just type rspec instead of rspec spec to run all the examples in the spec directory.[m
 [m
 [m
   [m
[36m@@ -7365,14 +7757,13 @@[m [mrun all the examples in the spec directory.[m
     [m
   [m
     Note:[m
[31m-    Other scripts invoking rspec indirectly will ignore this[m
[31m-setting.[m
[32m+[m[41m    [m
[32m+[m[32mOther scripts invoking rspec indirectly will ignore this setting.[m
[32m+[m
   [m
 [m
 [m
[31m-Path to use if no path is provided to the rspec command (default:[m
[31m-"spec"). Allows you to just type rspec instead of rspec spec to[m
[31m-run all the examples in the spec directory.[m
[32m+[m[32mPath to use if no path is provided to the rspec command (default: "spec"). Allows you to just type rspec instead of rspec spec to run all the examples in the spec directory.[m
 [m
 [m
   [m
[36m@@ -7488,14 +7879,13 @@[m [madd_read_only_setting :default_path[m
     [m
   [m
     Note:[m
[31m-    Other scripts invoking rspec indirectly will ignore this[m
[31m-setting.[m
     [m
[32m+[m[32mOther scripts invoking rspec indirectly will ignore this setting.[m
 [m
   [m
[31m-Path to use if no path is provided to the rspec command (default:[m
[31m-"spec"). Allows you to just type rspec instead of rspec spec to[m
[31m-run all the examples in the spec directory.[m
[32m+[m
[32m+[m
[32m+[m[32mPath to use if no path is provided to the rspec command (default: "spec"). Allows you to just type rspec instead of rspec spec to run all the examples in the spec directory.[m
 [m
 [m
   [m
[36m@@ -7538,25 +7928,20 @@[m [madd_read_only_setting :default_path[m
       [m
   [m
   [m
[31m-    Color used to print details.  Defaults to :cyan but can be set to one[m
[31m-of the following: [:black, :white, :red, :green, :yellow, :blue,[m
[31m-:magenta, :cyan].Color used to print details.  Defaults to :cyan but can be set to one[m
[31m-of the following: [:black, :white, :red, :green, :yellow, :blue,[m
[31m-:magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mColor used to print details. Defaults to :cyan but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].Color used to print details. Defaults to :cyan but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[31m-    Color used to print details.  Defaults to :cyan but can be set to one[m
[31m-of the following: [:black, :white, :red, :green, :yellow, :blue,[m
[31m-:magenta, :cyan].[m
     [m
[32m+[m[32mColor used to print details. Defaults to :cyan but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
 [m
[31m-    Color used to print details.  Defaults to :cyan but can be set to one[m
[31m-of the following: [:black, :white, :red, :green, :yellow, :blue,[m
[31m-:magenta, :cyan].[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mColor used to print details. Defaults to :cyan but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -7669,9 +8054,8 @@[m [madd_setting :detail_color[m
 [m
   [m
   [m
[31m-    Color used to print details.  Defaults to :cyan but can be set to one[m
[31m-of the following: [:black, :white, :red, :green, :yellow, :blue,[m
[31m-:magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mColor used to print details. Defaults to :cyan but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -7714,21 +8098,20 @@[m [madd_setting :detail_color[m
       [m
   [m
   [m
[31m-    Run examples over DRb (default: falsefalse). RSpec doesn't supply the DRb[m
[31m-server, but you can use tools like spork.Run examples over DRb (default: false). RSpec doesn't supply the DRb[m
[31m-server, but you can use tools like spork.[m
[32m+[m[41m    [m
[32m+[m[32mRun examples over DRb (default: falsefalse). RSpec doesn't supply the DRb server, but you can use tools like spork.Run examples over DRb (default: false). RSpec doesn't supply the DRb server, but you can use tools like spork.[m
[32m+[m
[32m+[m
   [m
     [m
[32m+[m[32mRun examples over DRb (default: false). RSpec doesn't supply the DRb server, but you can use tools like spork.[m
 [m
[31m-    Run examples over DRb (default: false). RSpec doesn't supply the DRb[m
[31m-server, but you can use tools like spork.[m
 [m
   [m
 [m
   [m
     [m
[31m-    Run examples over DRb (default: false). RSpec doesn't supply the DRb[m
[31m-server, but you can use tools like spork.[m
[32m+[m[32mRun examples over DRb (default: false). RSpec doesn't supply the DRb server, but you can use tools like spork.[m
 [m
 [m
   [m
[36m@@ -7841,8 +8224,8 @@[m [madd_setting :drb[m
 [m
   [m
   [m
[31m-    Run examples over DRb (default: false). RSpec doesn't supply the DRb[m
[31m-server, but you can use tools like spork.[m
[32m+[m[41m    [m
[32m+[m[32mRun examples over DRb (default: false). RSpec doesn't supply the DRb server, but you can use tools like spork.[m
 [m
 [m
   [m
[36m@@ -7994,21 +8381,20 @@[m [madd_setting :drb_port[m
       [m
   [m
   [m
[31m-    Prints the formatter output of your suite without running any[m
[31m-examples or hooks.Prints the formatter output of your suite without running any[m
[31m-examples or hooks.[m
     [m
[32m+[m[32mPrints the formatter output of your suite without running any examples or hooks.Prints the formatter output of your suite without running any examples or hooks.[m
 [m
 [m
[31m-    Prints the formatter output of your suite without running any[m
[31m-examples or hooks.[m
   [m
     [m
[32m+[m[32mPrints the formatter output of your suite without running any examples or hooks.[m
 [m
 [m
   [m
[31m-    Prints the formatter output of your suite without running any[m
[31m-examples or hooks.[m
[32m+[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mPrints the formatter output of your suite without running any examples or hooks.[m
 [m
 [m
   [m
[36m@@ -8077,8 +8463,8 @@[m [madd_setting :dry_run[m
 [m
   [m
   [m
[31m-    Prints the formatter output of your suite without running any[m
[31m-examples or hooks.[m
[32m+[m[41m    [m
[32m+[m[32mPrints the formatter output of your suite without running any examples or hooks.[m
 [m
 [m
   [m
[36m@@ -8217,21 +8607,20 @@[m [madd_setting :error_stream[m
       [m
   [m
   [m
[31m-    The file path to use for persisting example statuses. Necessary for the[m
[31m---only-failures--only-failures and --next-failure--next-failure CLI options.The file path to use for persisting example statuses. Necessary for the[m
[31m---only-failures and --next-failure CLI options.[m
[32m+[m[41m    [m
[32m+[m[32mThe file path to use for persisting example statuses. Necessary for the --only-failures--only-failures and --next-failure--next-failure CLI options.The file path to use for persisting example statuses. Necessary for the --only-failures and --next-failure CLI options.[m
[32m+[m
[32m+[m
   [m
     [m
[32m+[m[32mThe file path to use for persisting example statuses. Necessary for the --only-failures and --next-failure CLI options.[m
 [m
[31m-    The file path to use for persisting example statuses. Necessary for the[m
[31m---only-failures and --next-failure CLI options.[m
 [m
   [m
 [m
   [m
     [m
[31m-    The file path to use for persisting example statuses. Necessary for the[m
[31m---only-failures and --next-failure CLI options.[m
[32m+[m[32mThe file path to use for persisting example statuses. Necessary for the --only-failures and --next-failure CLI options.[m
 [m
 [m
   [m
[36m@@ -8692,8 +9103,8 @@[m [mdefine_reader :example_status_persistence_file_path[m
 [m
   [m
   [m
[31m-    The file path to use for persisting example statuses. Necessary for the[m
[31m---only-failures and --next-failure CLI options.[m
[32m+[m[41m    [m
[32m+[m[32mThe file path to use for persisting example statuses. Necessary for the --only-failures and --next-failure CLI options.[m
 [m
 [m
   [m
[36m@@ -8961,25 +9379,20 @@[m [mdefine_reader :exclude_pattern[m
       [m
   [m
   [m
[31m-    If specified, indicates the number of failures required before cleaning[m
[31m-up and exit (default: nil). Can also be true to fail and exit on first[m
[31m-failure.If specified, indicates the number of failures required before cleaning[m
[31m-up and exit (default: nil). Can also be true to fail and exit on first[m
[31m-failure.[m
[32m+[m[41m    [m
[32m+[m[32mIf specified, indicates the number of failures required before cleaning up and exit (default: nil). Can also be true to fail and exit on first failure.If specified, indicates the number of failures required before cleaning up and exit (default: nil). Can also be true to fail and exit on first failure.[m
[32m+[m
[32m+[m
   [m
     [m
[32m+[m[32mIf specified, indicates the number of failures required before cleaning up and exit (default: nil). Can also be true to fail and exit on first failure.[m
 [m
[31m-    If specified, indicates the number of failures required before cleaning[m
[31m-up and exit (default: nil). Can also be true to fail and exit on first[m
[31m-failure.[m
 [m
   [m
 [m
   [m
     [m
[31m-    If specified, indicates the number of failures required before cleaning[m
[31m-up and exit (default: nil). Can also be true to fail and exit on first[m
[31m-failure.[m
[32m+[m[32mIf specified, indicates the number of failures required before cleaning up and exit (default: nil). Can also be true to fail and exit on first failure.[m
 [m
 [m
   [m
[36m@@ -9048,9 +9461,8 @@[m [mdefine_reader :fail_fast[m
 [m
   [m
   [m
[31m-    If specified, indicates the number of failures required before cleaning[m
[31m-up and exit (default: nil). Can also be true to fail and exit on first[m
[31m-failure.[m
[32m+[m[41m    [m
[32m+[m[32mIf specified, indicates the number of failures required before cleaning up and exit (default: nil). Can also be true to fail and exit on first failure.[m
 [m
 [m
   [m
[36m@@ -9246,25 +9662,20 @@[m [madd_setting :fail_if_no_examples[m
       [m
   [m
   [m
[31m-    Color to use to indicate failure.  Defaults to :red but can be set to[m
[31m-one of the following: [:black, :white, :red, :green, :yellow, :blue,[m
[31m-:magenta, :cyan].Color to use to indicate failure.  Defaults to :red but can be set to[m
[31m-one of the following: [:black, :white, :red, :green, :yellow, :blue,[m
[31m-:magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mColor to use to indicate failure. Defaults to :red but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].Color to use to indicate failure. Defaults to :red but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
[32m+[m
 [m
   [m
     [m
[31m-    Color to use to indicate failure.  Defaults to :red but can be set to[m
[31m-one of the following: [:black, :white, :red, :green, :yellow, :blue,[m
[31m-:magenta, :cyan].[m
[32m+[m[32mColor to use to indicate failure. Defaults to :red but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
 [m
   [m
[31m-    Color to use to indicate failure.  Defaults to :red but can be set to[m
[31m-one of the following: [:black, :white, :red, :green, :yellow, :blue,[m
[31m-:magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mColor to use to indicate failure. Defaults to :red but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -9377,9 +9788,8 @@[m [madd_setting :failure_color[m
 [m
   [m
   [m
[31m-    Color to use to indicate failure.  Defaults to :red but can be set to[m
[31m-one of the following: [:black, :white, :red, :green, :yellow, :blue,[m
[31m-:magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mColor to use to indicate failure. Defaults to :red but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -9799,25 +10223,20 @@[m [mend[m
       [m
   [m
   [m
[31m-    Color used when a pending example is fixed. Defaults to :blue but can[m
[31m-be set to one of the following: [:black, :white, :red, :green,[m
[31m-:yellow, :blue, :magenta, :cyan].Color used when a pending example is fixed. Defaults to :blue but can[m
[31m-be set to one of the following: [:black, :white, :red, :green,[m
[31m-:yellow, :blue, :magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mColor used when a pending example is fixed. Defaults to :blue but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].Color used when a pending example is fixed. Defaults to :blue but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[31m-    Color used when a pending example is fixed. Defaults to :blue but can[m
[31m-be set to one of the following: [:black, :white, :red, :green,[m
[31m-:yellow, :blue, :magenta, :cyan].[m
     [m
[32m+[m[32mColor used when a pending example is fixed. Defaults to :blue but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
 [m
[31m-    Color used when a pending example is fixed. Defaults to :blue but can[m
[31m-be set to one of the following: [:black, :white, :red, :green,[m
[31m-:yellow, :blue, :magenta, :cyan].[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mColor used when a pending example is fixed. Defaults to :blue but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -9930,9 +10349,8 @@[m [madd_setting :fixed_color[m
 [m
   [m
   [m
[31m-    Color used when a pending example is fixed. Defaults to :blue but can[m
[31m-be set to one of the following: [:black, :white, :red, :green,[m
[31m-:yellow, :blue, :magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mColor used when a pending example is fixed. Defaults to :blue but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -9975,21 +10393,20 @@[m [madd_setting :fixed_color[m
       [m
   [m
   [m
[31m-    Returns dirs that have been prepended to the load path by the -I-I[m
[31m-command line option.Returns dirs that have been prepended to the load path by the -I[m
[31m-command line option.[m
[32m+[m[41m    [m
[32m+[m[32mReturns dirs that have been prepended to the load path by the -I-I command line option.Returns dirs that have been prepended to the load path by the -I command line option.[m
 [m
 [m
   [m
[31m-    Returns dirs that have been prepended to the load path by the -I[m
[31m-command line option.[m
     [m
[32m+[m[32mReturns dirs that have been prepended to the load path by the -I command line option.[m
 [m
 [m
   [m
 [m
[31m-    Returns dirs that have been prepended to the load path by the -I[m
[31m-command line option.[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mReturns dirs that have been prepended to the load path by the -I command line option.[m
 [m
 [m
   [m
[36m@@ -10102,8 +10519,8 @@[m [mdefine_reader :libs[m
 [m
   [m
   [m
[31m-    Returns dirs that have been prepended to the load path by the -I[m
[31m-command line option.[m
[32m+[m[41m    [m
[32m+[m[32mReturns dirs that have been prepended to the load path by the -I command line option.[m
 [m
 [m
   [m
[36m@@ -10146,25 +10563,20 @@[m [mdefine_reader :libs[m
       [m
   [m
   [m
[31m-    Maximum count of failed source lines to display in the failure reports.[m
[31m-(default 10).[m
[31m-return [Integer].Maximum count of failed source lines to display in the failure reports.[m
[31m-(default 10).[m
[31m-return [Integer].[m
     [m
[32m+[m[32mMaximum count of failed source lines to display in the failure reports. (default 10). return [Integer].Maximum count of failed source lines to display in the failure reports. (default 10). return [Integer].[m
 [m
 [m
[31m-    Maximum count of failed source lines to display in the failure reports.[m
[31m-(default 10).[m
[31m-return [Integer].[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mMaximum count of failed source lines to display in the failure reports. (default 10). return [Integer].[m
 [m
 [m
   [m
 [m
   [m
[31m-    Maximum count of failed source lines to display in the failure reports.[m
[31m-(default 10).[m
[31m-return [Integer].[m
[32m+[m[41m    [m
[32m+[m[32mMaximum count of failed source lines to display in the failure reports. (default 10). return [Integer].[m
 [m
 [m
   [m
[36m@@ -10233,9 +10645,8 @@[m [madd_setting :max_displayed_failure_line_count[m
 [m
   [m
   [m
[31m-    Maximum count of failed source lines to display in the failure reports.[m
[31m-(default 10).[m
[31m-return [Integer].[m
[32m+[m[41m    [m
[32m+[m[32mMaximum count of failed source lines to display in the failure reports. (default 10). return [Integer].[m
 [m
 [m
   [m
[36m@@ -10374,21 +10789,20 @@[m [mdefine_reader :only_failures[m
       [m
   [m
   [m
[31m-    Determines where RSpec will send its output.[m
[31m-Default: $stdout$stdout.Determines where RSpec will send its output.[m
[31m-Default: $stdout.[m
     [m
[32m+[m[32mDetermines where RSpec will send its output. Default: $stdout$stdout.Determines where RSpec will send its output. Default: $stdout.[m
 [m
 [m
[31m-    Determines where RSpec will send its output.[m
[31m-Default: $stdout.[m
   [m
     [m
[32m+[m[32mDetermines where RSpec will send its output. Default: $stdout.[m
 [m
 [m
   [m
[31m-    Determines where RSpec will send its output.[m
[31m-Default: $stdout.[m
[32m+[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mDetermines where RSpec will send its output. Default: $stdout.[m
 [m
 [m
   [m
[36m@@ -10501,8 +10915,8 @@[m [mdefine_reader :output_stream[m
 [m
   [m
   [m
[31m-    Determines where RSpec will send its output.[m
[31m-Default: $stdout.[m
[32m+[m[41m    [m
[32m+[m[32mDetermines where RSpec will send its output. Default: $stdout.[m
 [m
 [m
   [m
[36m@@ -10545,17 +10959,20 @@[m [mdefine_reader :output_stream[m
       [m
   [m
   [m
[31m-    Load files matching this pattern (default: '**{,/*/**}/*_spec.rb''**{,/*/**}/*_spec.rb').Load files matching this pattern (default: '**{,/*/**}/*_spec.rb').[m
[32m+[m[41m    [m
[32m+[m[32mLoad files matching this pattern (default: '**{,//*/*}/*_spec.rb''**{,//*}/*_spec.rb').Load files matching this pattern (default: '**{,//*}/*_spec.rb').[m
[32m+[m
 [m
   [m
     [m
[31m-    Load files matching this pattern (default: '**{,/*/**}/*_spec.rb').[m
[32m+[m[32mLoad files matching this pattern (default: '**{,//*}/*_spec.rb').[m
 [m
 [m
   [m
 [m
   [m
[31m-    Load files matching this pattern (default: '**{,/*/**}/*_spec.rb').[m
[32m+[m[41m    [m
[32m+[m[32mLoad files matching this pattern (default: '**{,//*}/*_spec.rb').[m
 [m
 [m
   [m
[36m@@ -10668,7 +11085,8 @@[m [mdefine_reader :pattern[m
 [m
   [m
   [m
[31m-    Load files matching this pattern (default: '**{,/*/**}/*_spec.rb').[m
[32m+[m[41m    [m
[32m+[m[32mLoad files matching this pattern (default: '**{,//*}/*_spec.rb').[m
 [m
 [m
   [m
[36m@@ -10711,25 +11129,20 @@[m [mdefine_reader :pattern[m
       [m
   [m
   [m
[31m-    Color to use to print pending examples.  Defaults to :yellow but can[m
[31m-be set to one of the following: [:black, :white, :red, :green,[m
[31m-:yellow, :blue, :magenta, :cyan].Color to use to print pending examples.  Defaults to :yellow but can[m
[31m-be set to one of the following: [:black, :white, :red, :green,[m
[31m-:yellow, :blue, :magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mColor to use to print pending examples. Defaults to :yellow but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].Color to use to print pending examples. Defaults to :yellow but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[31m-    Color to use to print pending examples.  Defaults to :yellow but can[m
[31m-be set to one of the following: [:black, :white, :red, :green,[m
[31m-:yellow, :blue, :magenta, :cyan].[m
     [m
[32m+[m[32mColor to use to print pending examples. Defaults to :yellow but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
 [m
[31m-    Color to use to print pending examples.  Defaults to :yellow but can[m
[31m-be set to one of the following: [:black, :white, :red, :green,[m
[31m-:yellow, :blue, :magenta, :cyan].[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mColor to use to print pending examples. Defaults to :yellow but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -10842,9 +11255,8 @@[m [madd_setting :pending_color[m
 [m
   [m
   [m
[31m-    Color to use to print pending examples.  Defaults to :yellow but can[m
[31m-be set to one of the following: [:black, :white, :red, :green,[m
[31m-:yellow, :blue, :magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mColor to use to print pending examples. Defaults to :yellow but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -10894,9 +11306,8 @@[m [madd_setting :pending_color[m
   This method is part of a private API.[m
   You should avoid using this method if possible, as it may be removed or be changed in the future.[m
 [m
[31m-Defaults profile_examplesprofile_examples to 10 examples when @profile_examples@profile_examples is[m
[31m-truetrue.Defaults profile_examples to 10 examples when @profile_examples is[m
[31m-true.[m
[32m+[m
[32m+[m[32mDefaults profile_examplesprofile_examples to 10 examples when @profile_examples@profile_examples is truetrue.Defaults profile_examples to 10 examples when @profile_examples is true.[m
 [m
 [m
   [m
[36m@@ -10904,8 +11315,8 @@[m [mtrue.[m
   This method is part of a private API.[m
   You should avoid using this method if possible, as it may be removed or be changed in the future.[m
 [m
[31m-Defaults profile_examples to 10 examples when @profile_examples is[m
[31m-true.[m
[32m+[m
[32m+[m[32mDefaults profile_examples to 10 examples when @profile_examples is true.[m
 [m
 [m
   [m
[36m@@ -10915,8 +11326,8 @@[m [mtrue.[m
   This method is part of a private API.[m
   You should avoid using this method if possible, as it may be removed or be changed in the future.[m
 [m
[31m-Defaults profile_examples to 10 examples when @profile_examples is[m
[31m-true.[m
[32m+[m
[32m+[m[32mDefaults profile_examples to 10 examples when @profile_examples is true.[m
 [m
 [m
   [m
[36m@@ -10989,8 +11400,8 @@[m [madd_setting :profile_examples[m
   This method is part of a private API.[m
   You should avoid using this method if possible, as it may be removed or be changed in the future.[m
 [m
[31m-Defaults profile_examples to 10 examples when @profile_examples is[m
[31m-true.[m
[32m+[m
[32m+[m[32mDefaults profile_examples to 10 examples when @profile_examples is true.[m
 [m
 [m
   [m
[36m@@ -11020,37 +11431,20 @@[m [madd_setting :profile_examples[m
       [m
   [m
   [m
[31m-    Specifies which directories contain the source code for your project.[m
[31m-When a failure occurs, RSpec looks through the backtrace to find a[m
[31m-a line of source to print. It first looks for a line coming from[m
[31m-one of the project source directories so that, for example, it prints[m
[31m-the expectation or assertion call rather than the source code from[m
[31m-the expectation or assertion framework.Specifies which directories contain the source code for your project.[m
[31m-When a failure occurs, RSpec looks through the backtrace to find a[m
[31m-a line of source to print. It first looks for a line coming from[m
[31m-one of the project source directories so that, for example, it prints[m
[31m-the expectation or assertion call rather than the source code from[m
[31m-the expectation or assertion framework.[m
     [m
[32m+[m[32mSpecifies which directories contain the source code for your project. When a failure occurs, RSpec looks through the backtrace to find a a line of source to print. It first looks for a line coming from one of the project source directories so that, for example, it prints the expectation or assertion call rather than the source code from the expectation or assertion framework.Specifies which directories contain the source code for your project. When a failure occurs, RSpec looks through the backtrace to find a a line of source to print. It first looks for a line coming from one of the project source directories so that, for example, it prints the expectation or assertion call rather than the source code from the expectation or assertion framework.[m
 [m
 [m
[31m-    Specifies which directories contain the source code for your project.[m
[31m-When a failure occurs, RSpec looks through the backtrace to find a[m
[31m-a line of source to print. It first looks for a line coming from[m
[31m-one of the project source directories so that, for example, it prints[m
[31m-the expectation or assertion call rather than the source code from[m
[31m-the expectation or assertion framework.[m
   [m
     [m
[32m+[m[32mSpecifies which directories contain the source code for your project. When a failure occurs, RSpec looks through the backtrace to find a a line of source to print. It first looks for a line coming from one of the project source directories so that, for example, it prints the expectation or assertion call rather than the source code from the expectation or assertion framework.[m
 [m
 [m
   [m
[31m-    Specifies which directories contain the source code for your project.[m
[31m-When a failure occurs, RSpec looks through the backtrace to find a[m
[31m-a line of source to print. It first looks for a line coming from[m
[31m-one of the project source directories so that, for example, it prints[m
[31m-the expectation or assertion call rather than the source code from[m
[31m-the expectation or assertion framework.[m
[32m+[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mSpecifies which directories contain the source code for your project. When a failure occurs, RSpec looks through the backtrace to find a a line of source to print. It first looks for a line coming from one of the project source directories so that, for example, it prints the expectation or assertion call rather than the source code from the expectation or assertion framework.[m
 [m
 [m
   [m
[36m@@ -11163,12 +11557,8 @@[m [madd_setting :project_source_dirs[m
 [m
   [m
   [m
[31m-    Specifies which directories contain the source code for your project.[m
[31m-When a failure occurs, RSpec looks through the backtrace to find a[m
[31m-a line of source to print. It first looks for a line coming from[m
[31m-one of the project source directories so that, for example, it prints[m
[31m-the expectation or assertion call rather than the source code from[m
[31m-the expectation or assertion framework.[m
[32m+[m[41m    [m
[32m+[m[32mSpecifies which directories contain the source code for your project. When a failure occurs, RSpec looks through the backtrace to find a a line of source to print. It first looks for a line coming from one of the project source directories so that, for example, it prints the expectation or assertion call rather than the source code from the expectation or assertion framework.[m
 [m
 [m
   [m
[36m@@ -11377,35 +11771,33 @@[m [mdefine_reader :requires[m
       [m
   [m
   [m
[31m-    Deprecated.Deprecated. Use #filter_run_when_matching instead for the specific[m
[31m-filters that you want to be ignored if none match.Use #filter_run_when_matching instead for the specific[m
[31m-filters that you want to be ignored if none match.[m
[31m-Use #filter_run_when_matching instead for the specific[m
[31m-filters that you want to be ignored if none match.[m
[31m-Deprecated. Use #filter_run_when_matching instead for the specific[m
[31m-filters that you want to be ignored if none match.[m
[32m+[m[32m    Deprecated.Deprecated.[m[41m [m
[32m+[m[32mUse #filter_run_when_matching instead for the specific filters that you want to be ignored if none match.Use #filter_run_when_matching instead for the specific filters that you want to be ignored if none match.[m
[32m+[m
[32m+[m[32mUse #filter_run_when_matching instead for the specific filters that you want to be ignored if none match.[m
[32m+[m[32mDeprecated.[m[41m [m
[32m+[m[32mUse #filter_run_when_matching instead for the specific filters that you want to be ignored if none match.[m
[32m+[m
[32m+[m
[32m+[m[32mRun all examples if none match the configured filters (default: falsefalse).Run all examples if none match the configured filters (default: false).[m
[32m+[m
 [m
[31m-Run all examples if none match the configured filters[m
[31m-(default: falsefalse).Run all examples if none match the configured filters[m
[31m-(default: false).[m
   [m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mUse #filter_run_when_matching instead for the specific filters that you want to be ignored if none match.[m
 [m
 [m
[31m-    Deprecated. Use #filter_run_when_matching instead for the specific[m
[31m-filters that you want to be ignored if none match.[m
[32m+[m[32mRun all examples if none match the configured filters (default: false).[m
 [m
[31m-Run all examples if none match the configured filters[m
[31m-(default: false).[m
 [m
   [m
 [m
   [m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mUse #filter_run_when_matching instead for the specific filters that you want to be ignored if none match.[m
 [m
[31m-    Deprecated. Use #filter_run_when_matching instead for the specific[m
[31m-filters that you want to be ignored if none match.[m
 [m
[31m-Run all examples if none match the configured filters[m
[31m-(default: false).[m
[32m+[m[32mRun all examples if none match the configured filters (default: false).[m
 [m
 [m
   [m
[36m@@ -11474,11 +11866,11 @@[m [madd_setting :run_all_when_everything_filtered[m
 [m
   [m
   [m
[31m-    Deprecated. Use #filter_run_when_matching instead for the specific[m
[31m-filters that you want to be ignored if none match.[m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mUse #filter_run_when_matching instead for the specific filters that you want to be ignored if none match.[m
 [m
[31m-Run all examples if none match the configured filters[m
[31m-(default: false).[m
[32m+[m
[32m+[m[32mRun all examples if none match the configured filters (default: false).[m
 [m
 [m
   [m
[36m@@ -11508,9 +11900,8 @@[m [madd_setting :run_all_when_everything_filtered[m
       [m
   [m
   [m
[31m-    Configures how RSpec treats metadata passed as part of a shared example[m
[31m-group definition. For example, given this shared example group definition:.Configures how RSpec treats metadata passed as part of a shared example[m
[31m-group definition. For example, given this shared example group definition:.[m
[32m+[m[41m    [m
[32m+[m[32mConfigures how RSpec treats metadata passed as part of a shared example group definition. For example, given this shared example group definition:.Configures how RSpec treats metadata passed as part of a shared example group definition. For example, given this shared example group definition:.[m
 [m
 RSpecRSpec..shared_contextshared_context ""uses DBuses DB"""uses DB",, :db:db =>=> truetrue dodo[m
   aroundaround((:example:example)) dodo ||exex||[m
[36m@@ -11529,29 +11920,22 @@[m [mRSpec.shared_context "uses DB", :db => true do[m
 end[m
 [m
 [m
[31m-...there are two ways RSpec can treat the :db => true metadata, each[m
[31m-of which has a corresponding config option:....there are two ways RSpec can treat the :db => true metadata, each[m
[31m-of which has a corresponding config option:.[m
[32m+[m[32m…there are two ways RSpec can treat the :db => true metadata, each of which has a corresponding config option:.…there are two ways RSpec can treat the :db => true metadata, each of which has a corresponding config option:.[m
[32m+[m
[32m+[m[32m:trigger_inclusion:trigger_inclusion: this shared context will be implicitly included  in any groups (or examples) that have :db => true:db => true metadata.:trigger_inclusion: this shared context will be implicitly included  in any groups (or examples) that have :db => true metadata.[m
 [m
[32m+[m[32m:trigger_inclusion: this shared context will be implicitly included  in any groups (or examples) that have :db => true metadata.[m
 [m
[31m-:trigger_inclusion:trigger_inclusion: this shared context will be implicitly included[m
[31m-in any groups (or examples) that have :db => true:db => true metadata.:trigger_inclusion: this shared context will be implicitly included[m
[31m-in any groups (or examples) that have :db => true metadata.[m
[31m-:apply_to_host_groups:apply_to_host_groups: the metadata will be inherited by the metadata[m
[31m-hash of all host groups and examples.:apply_to_host_groups: the metadata will be inherited by the metadata[m
[31m-hash of all host groups and examples.[m
[32m+[m[32m:apply_to_host_groups:apply_to_host_groups: the metadata will be inherited by the metadata  hash of all host groups and examples.:apply_to_host_groups: the metadata will be inherited by the metadata  hash of all host groups and examples.[m
 [m
[31m-:trigger_inclusion: this shared context will be implicitly included[m
[31m-in any groups (or examples) that have :db => true metadata.[m
[31m-:apply_to_host_groups: the metadata will be inherited by the metadata[m
[31m-hash of all host groups and examples.[m
[32m+[m[32m:apply_to_host_groups: the metadata will be inherited by the metadata  hash of all host groups and examples.[m
 [m
[32m+[m[32m:trigger_inclusion: this shared context will be implicitly included  in any groups (or examples) that have :db => true metadata.[m
 [m
[31m-:trigger_inclusion is the legacy behavior from before RSpec 3.5 but should[m
[31m-be considered deprecated. Instead, you can explicitly include a group with[m
[31m-include_context:.:trigger_inclusion is the legacy behavior from before RSpec 3.5 but should[m
[31m-be considered deprecated. Instead, you can explicitly include a group with[m
[31m-include_context:.[m
[32m+[m[32m:apply_to_host_groups: the metadata will be inherited by the metadata  hash of all host groups and examples.[m
[32m+[m
[32m+[m
[32m+[m[32m:trigger_inclusion is the legacy behavior from before RSpec 3.5 but should be considered deprecated. Instead, you can explicitly include a group with include_context:.:trigger_inclusion is the legacy behavior from before RSpec 3.5 but should be considered deprecated. Instead, you can explicitly include a group with include_context:.[m
 [m
 RSpecRSpec..describedescribe ""My modelMy model"""My model" dodo[m
   include_contextinclude_context ""uses DBuses DB"""uses DB"[m
[36m@@ -11564,9 +11948,7 @@[m [mRSpec.describe "My model" do[m
 end[m
 [m
 [m
[31m-...or you can configure RSpec to include the context based on matching metadata[m
[31m-using an API that mirrors configured module inclusion:....or you can configure RSpec to include the context based on matching metadata[m
[31m-using an API that mirrors configured module inclusion:.[m
[32m+[m[32m…or you can configure RSpec to include the context based on matching metadata using an API that mirrors configured module inclusion:.…or you can configure RSpec to include the context based on matching metadata using an API that mirrors configured module inclusion:.[m
 [m
 RSpecRSpec..configureconfigure dodo ||rspecrspec||[m
   rspecrspec..include_contextinclude_context ""uses DBuses DB"""uses DB",, :db:db =>=> truetrue[m
[36m@@ -11579,14 +11961,12 @@[m [mRSpec.configure do |rspec|[m
 end[m
 [m
 [m
[31m-:apply_to_host_groups:apply_to_host_groups is a new feature of RSpec 3.5 and will be the only[m
[31m-supported behavior in RSpec 4.:apply_to_host_groups is a new feature of RSpec 3.5 and will be the only[m
[31m-supported behavior in RSpec 4.[m
[32m+[m[32m:apply_to_host_groups:apply_to_host_groups is a new feature of RSpec 3.5 and will be the only supported behavior in RSpec 4.:apply_to_host_groups is a new feature of RSpec 3.5 and will be the only supported behavior in RSpec 4.[m
[32m+[m
 [m
   [m
     [m
[31m-    Configures how RSpec treats metadata passed as part of a shared example[m
[31m-group definition. For example, given this shared example group definition:.[m
[32m+[m[32mConfigures how RSpec treats metadata passed as part of a shared example group definition. For example, given this shared example group definition:.[m
 [m
 RSpec.shared_context "uses DB", :db => true do[m
   around(:example) do |ex|[m
[36m@@ -11595,42 +11975,35 @@[m [mRSpec.shared_context "uses DB", :db => true do[m
 end[m
 [m
 [m
[31m-...there are two ways RSpec can treat the :db => true metadata, each[m
[31m-of which has a corresponding config option:.[m
[32m+[m[32m…there are two ways RSpec can treat the :db => true metadata, each of which has a corresponding config option:.[m
 [m
[32m+[m[32m:trigger_inclusion: this shared context will be implicitly included  in any groups (or examples) that have :db => true metadata.[m
 [m
[31m-:trigger_inclusion: this shared context will be implicitly included[m
[31m-in any groups (or examples) that have :db => true metadata.[m
[31m-:apply_to_host_groups: the metadata will be inherited by the metadata[m
[31m-hash of all host groups and examples.[m
[32m+[m[32m:apply_to_host_groups: the metadata will be inherited by the metadata  hash of all host groups and examples.[m
 [m
 [m
[31m-:trigger_inclusion is the legacy behavior from before RSpec 3.5 but should[m
[31m-be considered deprecated. Instead, you can explicitly include a group with[m
[31m-include_context:.[m
[32m+[m[32m:trigger_inclusion is the legacy behavior from before RSpec 3.5 but should be considered deprecated. Instead, you can explicitly include a group with include_context:.[m
 [m
 RSpec.describe "My model" do[m
   include_context "uses DB"[m
 end[m
 [m
 [m
[31m-...or you can configure RSpec to include the context based on matching metadata[m
[31m-using an API that mirrors configured module inclusion:.[m
[32m+[m[32m…or you can configure RSpec to include the context based on matching metadata using an API that mirrors configured module inclusion:.[m
 [m
 RSpec.configure do |rspec|[m
   rspec.include_context "uses DB", :db => true[m
 end[m
 [m
 [m
[31m-:apply_to_host_groups is a new feature of RSpec 3.5 and will be the only[m
[31m-supported behavior in RSpec 4.[m
[32m+[m[32m:apply_to_host_groups is a new feature of RSpec 3.5 and will be the only supported behavior in RSpec 4.[m
 [m
 [m
   [m
 [m
   [m
[31m-    Configures how RSpec treats metadata passed as part of a shared example[m
[31m-group definition. For example, given this shared example group definition:.[m
[32m+[m[41m    [m
[32m+[m[32mConfigures how RSpec treats metadata passed as part of a shared example group definition. For example, given this shared example group definition:.[m
 [m
 RSpec.shared_context "uses DB", :db => true do[m
   around(:example) do |ex|[m
[36m@@ -11639,35 +12012,28 @@[m [mRSpec.shared_context "uses DB", :db => true do[m
 end[m
 [m
 [m
[31m-...there are two ways RSpec can treat the :db => true metadata, each[m
[31m-of which has a corresponding config option:.[m
[32m+[m[32m…there are two ways RSpec can treat the :db => true metadata, each of which has a corresponding config option:.[m
 [m
[32m+[m[32m:trigger_inclusion: this shared context will be implicitly included  in any groups (or examples) that have :db => true metadata.[m
 [m
[31m-:trigger_inclusion: this shared context will be implicitly included[m
[31m-in any groups (or examples) that have :db => true metadata.[m
[31m-:apply_to_host_groups: the metadata will be inherited by the metadata[m
[31m-hash of all host groups and examples.[m
[32m+[m[32m:apply_to_host_groups: the metadata will be inherited by the metadata  hash of all host groups and examples.[m
 [m
 [m
[31m-:trigger_inclusion is the legacy behavior from before RSpec 3.5 but should[m
[31m-be considered deprecated. Instead, you can explicitly include a group with[m
[31m-include_context:.[m
[32m+[m[32m:trigger_inclusion is the legacy behavior from before RSpec 3.5 but should be considered deprecated. Instead, you can explicitly include a group with include_context:.[m
 [m
 RSpec.describe "My model" do[m
   include_context "uses DB"[m
 end[m
 [m
 [m
[31m-...or you can configure RSpec to include the context based on matching metadata[m
[31m-using an API that mirrors configured module inclusion:.[m
[32m+[m[32m…or you can configure RSpec to include the context based on matching metadata using an API that mirrors configured module inclusion:.[m
 [m
 RSpec.configure do |rspec|[m
   rspec.include_context "uses DB", :db => true[m
 end[m
 [m
 [m
[31m-:apply_to_host_groups is a new feature of RSpec 3.5 and will be the only[m
[31m-supported behavior in RSpec 4.[m
[32m+[m[32m:apply_to_host_groups is a new feature of RSpec 3.5 and will be the only supported behavior in RSpec 4.[m
 [m
 [m
   [m
[36m@@ -12128,8 +12516,8 @@[m [mdefine_reader :shared_context_metadata_behavior[m
 [m
   [m
   [m
[31m-    Configures how RSpec treats metadata passed as part of a shared example[m
[31m-group definition. For example, given this shared example group definition:.[m
[32m+[m[41m    [m
[32m+[m[32mConfigures how RSpec treats metadata passed as part of a shared example group definition. For example, given this shared example group definition:.[m
 [m
 RSpec.shared_context "uses DB", :db => true do[m
   around(:example) do |ex|[m
[36m@@ -12138,35 +12526,28 @@[m [mRSpec.shared_context "uses DB", :db => true do[m
 end[m
 [m
 [m
[31m-...there are two ways RSpec can treat the :db => true metadata, each[m
[31m-of which has a corresponding config option:.[m
[32m+[m[32m…there are two ways RSpec can treat the :db => true metadata, each of which has a corresponding config option:.[m
 [m
[32m+[m[32m:trigger_inclusion: this shared context will be implicitly included  in any groups (or examples) that have :db => true metadata.[m
 [m
[31m-:trigger_inclusion: this shared context will be implicitly included[m
[31m-in any groups (or examples) that have :db => true metadata.[m
[31m-:apply_to_host_groups: the metadata will be inherited by the metadata[m
[31m-hash of all host groups and examples.[m
[32m+[m[32m:apply_to_host_groups: the metadata will be inherited by the metadata  hash of all host groups and examples.[m
 [m
 [m
[31m-:trigger_inclusion is the legacy behavior from before RSpec 3.5 but should[m
[31m-be considered deprecated. Instead, you can explicitly include a group with[m
[31m-include_context:.[m
[32m+[m[32m:trigger_inclusion is the legacy behavior from before RSpec 3.5 but should be considered deprecated. Instead, you can explicitly include a group with include_context:.[m
 [m
 RSpec.describe "My model" do[m
   include_context "uses DB"[m
 end[m
 [m
 [m
[31m-...or you can configure RSpec to include the context based on matching metadata[m
[31m-using an API that mirrors configured module inclusion:.[m
[32m+[m[32m…or you can configure RSpec to include the context based on matching metadata using an API that mirrors configured module inclusion:.[m
 [m
 RSpec.configure do |rspec|[m
   rspec.include_context "uses DB", :db => true[m
 end[m
 [m
 [m
[31m-:apply_to_host_groups is a new feature of RSpec 3.5 and will be the only[m
[31m-supported behavior in RSpec 4.[m
[32m+[m[32m:apply_to_host_groups is a new feature of RSpec 3.5 and will be the only supported behavior in RSpec 4.[m
 [m
 [m
   [m
[36m@@ -12268,25 +12652,20 @@[m [mdefine_reader :shared_context_metadata_behavior[m
       [m
   [m
   [m
[31m-    Don't print filter info i.e. "Run options: include :focus=>true"[m
[31m-(default false).[m
[31m-return [Boolean].Don't print filter info i.e. "Run options: include :focus=>true"[m
[31m-(default false).[m
[31m-return [Boolean].[m
[32m+[m[41m    [m
[32m+[m[32mDon't print filter info i.e. “Run options: include :focus=>true” (default false). return [Boolean].Don't print filter info i.e. “Run options: include :focus=>true” (default false). return [Boolean].[m
[32m+[m
[32m+[m
   [m
     [m
[32m+[m[32mDon't print filter info i.e. “Run options: include :focus=>true” (default false). return [Boolean].[m
 [m
[31m-    Don't print filter info i.e. "Run options: include :focus=>true"[m
[31m-(default false).[m
[31m-return [Boolean].[m
 [m
   [m
 [m
   [m
     [m
[31m-    Don't print filter info i.e. "Run options: include :focus=>true"[m
[31m-(default false).[m
[31m-return [Boolean].[m
[32m+[m[32mDon't print filter info i.e. “Run options: include :focus=>true” (default false). return [Boolean].[m
 [m
 [m
   [m
[36m@@ -12355,9 +12734,8 @@[m [madd_setting :silence_filter_announcements[m
 [m
   [m
   [m
[31m-    Don't print filter info i.e. "Run options: include :focus=>true"[m
[31m-(default false).[m
[31m-return [Boolean].[m
[32m+[m[41m    [m
[32m+[m[32mDon't print filter info i.e. “Run options: include :focus=>true” (default false). return [Boolean].[m
 [m
 [m
   [m
[36m@@ -12387,25 +12765,20 @@[m [madd_setting :silence_filter_announcements[m
       [m
   [m
   [m
[31m-    Color to use to indicate success.  Defaults to :green but can be set[m
[31m-to one of the following: [:black, :white, :red, :green, :yellow,[m
[31m-:blue, :magenta, :cyan].Color to use to indicate success.  Defaults to :green but can be set[m
[31m-to one of the following: [:black, :white, :red, :green, :yellow,[m
[31m-:blue, :magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mColor to use to indicate success. Defaults to :green but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].Color to use to indicate success. Defaults to :green but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
[32m+[m
 [m
   [m
     [m
[31m-    Color to use to indicate success.  Defaults to :green but can be set[m
[31m-to one of the following: [:black, :white, :red, :green, :yellow,[m
[31m-:blue, :magenta, :cyan].[m
[32m+[m[32mColor to use to indicate success. Defaults to :green but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
 [m
   [m
[31m-    Color to use to indicate success.  Defaults to :green but can be set[m
[31m-to one of the following: [:black, :white, :red, :green, :yellow,[m
[31m-:blue, :magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mColor to use to indicate success. Defaults to :green but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -12518,9 +12891,8 @@[m [madd_setting :success_color[m
 [m
   [m
   [m
[31m-    Color to use to indicate success.  Defaults to :green but can be set[m
[31m-to one of the following: [:black, :white, :red, :green, :yellow,[m
[31m-:blue, :magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mColor to use to indicate success. Defaults to :green but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -12563,25 +12935,20 @@[m [madd_setting :success_color[m
       [m
   [m
   [m
[31m-    Use threadsafe options where available.[m
[31m-Currently this will place a mutex around memoized values such as let blocks.[m
[31m-return [Boolean].Use threadsafe options where available.[m
[31m-Currently this will place a mutex around memoized values such as let blocks.[m
[31m-return [Boolean].[m
     [m
[32m+[m[32mUse threadsafe options where available. Currently this will place a mutex around memoized values such as let blocks. return [Boolean].Use threadsafe options where available. Currently this will place a mutex around memoized values such as let blocks. return [Boolean].[m
 [m
 [m
[31m-    Use threadsafe options where available.[m
[31m-Currently this will place a mutex around memoized values such as let blocks.[m
[31m-return [Boolean].[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mUse threadsafe options where available. Currently this will place a mutex around memoized values such as let blocks. return [Boolean].[m
 [m
 [m
   [m
 [m
   [m
[31m-    Use threadsafe options where available.[m
[31m-Currently this will place a mutex around memoized values such as let blocks.[m
[31m-return [Boolean].[m
[32m+[m[41m    [m
[32m+[m[32mUse threadsafe options where available. Currently this will place a mutex around memoized values such as let blocks. return [Boolean].[m
 [m
 [m
   [m
[36m@@ -12650,9 +13017,8 @@[m [madd_setting :threadsafe[m
 [m
   [m
   [m
[31m-    Use threadsafe options where available.[m
[31m-Currently this will place a mutex around memoized values such as let blocks.[m
[31m-return [Boolean].[m
[32m+[m[41m    [m
[32m+[m[32mUse threadsafe options where available. Currently this will place a mutex around memoized values such as let blocks. return [Boolean].[m
 [m
 [m
   [m
[36m@@ -12688,26 +13054,17 @@[m [madd_setting :threadsafe[m
     [m
   [m
     Note:[m
[31m-    This option will only be used by --bisect if you set it in a file[m
[31m-loaded via --require.[m
[32m+[m[41m    [m
[32m+[m[32mThis option will only be used by --bisect if you set it in a file loaded via --require.[m
 [m
   [m
 [m
[31m-Determines which bisect runner implementation gets used to run subsets[m
[31m-of the suite during a bisection. Your choices are:.[m
 [m
[32m+[m[32mDetermines which bisect runner implementation gets used to run subsets of the suite during a bisection. Your choices are:.[m
 [m
[31m-:shell: Performs a spec run by shelling out, booting RSpec and your[m
[31m-application environment each time. This runner is the most widely[m
[31m-compatible runner, but is not as fast. On platforms that do not[m
[31m-support forking, this is the default.[m
[31m-:fork: Pre-boots RSpec and your application environment in a parent[m
[31m-process, and then forks a child process for each spec run. This runner[m
[31m-tends to be significantly faster than the :shell runner but cannot[m
[31m-be used in some situations. On platforms that support forking, this[m
[31m-is the default. If you use this runner, you should ensure that all[m
[31m-of your one-time setup logic goes in a before(:suite) hook instead[m
[31m-of getting run at the top-level of a file loaded by --require.[m
[32m+[m[32m:shell: Performs a spec run by shelling out, booting RSpec and your application environment each time. This runner is the most widely compatible runner, but is not as fast. On platforms that do not support forking, this is the default.[m
[32m+[m
[32m+[m[32m:fork: Pre-boots RSpec and your application environment in a parent process, and then forks a child process for each spec run. This runner tends to be significantly faster than the :shell runner but cannot be used in some situations. On platforms that support forking, this is the default. If you use this runner, you should ensure that all of your one-time setup logic goes in a before(:suite) hook instead of getting run at the top-level of a file loaded by --require.[m
 [m
 [m
 [m
[36m@@ -12755,13 +13112,11 @@[m [mend[m
       [m
   [m
   [m
[31m-    Deprecated. No longer recommended because of complex behavior. Instead,[m
[31m-rely on the fact that TTYs will display color by default, or set[m
[31m-#color_mode to :on to display color on a non-TTY output.[m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mNo longer recommended because of complex behavior. Instead, rely on the fact that TTYs will display color by default, or set #color_mode to :on to display color on a non-TTY output.[m
[32m+[m
 [m
[31m-Enables color output if the output is a TTY.  As of RSpec 3.6, this is[m
[31m-the default behavior and this option is retained only for backwards[m
[31m-compatibility.[m
[32m+[m[32mEnables color output if the output is a TTY. As of RSpec 3.6, this is the default behavior and this option is retained only for backwards compatibility.[m
 [m
 [m
   [m
[36m@@ -12817,13 +13172,14 @@[m [mend[m
       [m
   [m
   [m
[32m+[m[41m    [m
 The mode for determining whether to display output in color. One of:.[m
 [m
[32m+[m[32m:automatic - the output will be in color if the output is a TTY (the  default).[m
 [m
[31m-:automatic - the output will be in color if the output is a TTY (the[m
[31m-default)[m
[31m-:on - the output will be in color, whether or not the output is a TTY[m
[31m-:off - the output will not be in color[m
[32m+[m[32m:on - the output will be in color, whether or not the output is a TTY.[m
[32m+[m
[32m+[m[32m:off - the output will not be in color.[m
 [m
 [m
 [m
[36m@@ -12878,9 +13234,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    The default output color. Defaults to :white but can be set to one of[m
[31m-the following: [:black, :white, :red, :green, :yellow, :blue,[m
[31m-:magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mThe default output color. Defaults to :white but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -12926,14 +13281,13 @@[m [madd_setting :default_color[m
     [m
   [m
     Note:[m
[31m-    Other scripts invoking rspec indirectly will ignore this[m
[31m-setting.[m
[32m+[m[41m    [m
[32m+[m[32mOther scripts invoking rspec indirectly will ignore this setting.[m
 [m
   [m
 [m
[31m-Path to use if no path is provided to the rspec command (default:[m
[31m-"spec"). Allows you to just type rspec instead of rspec spec to[m
[31m-run all the examples in the spec directory.[m
[32m+[m
[32m+[m[32mPath to use if no path is provided to the rspec command (default: "spec"). Allows you to just type rspec instead of rspec spec to run all the examples in the spec directory.[m
 [m
 [m
   [m
[36m@@ -12976,9 +13330,8 @@[m [madd_read_only_setting :default_path[m
       [m
   [m
   [m
[31m-    Color used to print details.  Defaults to :cyan but can be set to one[m
[31m-of the following: [:black, :white, :red, :green, :yellow, :blue,[m
[31m-:magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mColor used to print details. Defaults to :cyan but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -13021,8 +13374,8 @@[m [madd_setting :detail_color[m
       [m
   [m
   [m
[31m-    Run examples over DRb (default: false). RSpec doesn't supply the DRb[m
[31m-server, but you can use tools like spork.[m
[32m+[m[41m    [m
[32m+[m[32mRun examples over DRb (default: false). RSpec doesn't supply the DRb server, but you can use tools like spork.[m
 [m
 [m
   [m
[36m@@ -13095,8 +13449,8 @@[m [madd_setting :drb_port[m
       [m
   [m
   [m
[31m-    Prints the formatter output of your suite without running any[m
[31m-examples or hooks.[m
[32m+[m[41m    [m
[32m+[m[32mPrints the formatter output of your suite without running any examples or hooks.[m
 [m
 [m
   [m
[36m@@ -13156,8 +13511,8 @@[m [madd_setting :error_stream[m
       [m
   [m
   [m
[31m-    The file path to use for persisting example statuses. Necessary for the[m
[31m---only-failures and --next-failure CLI options.[m
[32m+[m[41m    [m
[32m+[m[32mThe file path to use for persisting example statuses. Necessary for the --only-failures and --next-failure CLI options.[m
 [m
 [m
   [m
[36m@@ -13302,9 +13661,8 @@[m [mdefine_reader :exclude_pattern[m
       [m
   [m
   [m
[31m-    If specified, indicates the number of failures required before cleaning[m
[31m-up and exit (default: nil). Can also be true to fail and exit on first[m
[31m-failure.[m
[32m+[m[41m    [m
[32m+[m[32mIf specified, indicates the number of failures required before cleaning up and exit (default: nil). Can also be true to fail and exit on first failure.[m
 [m
 [m
   [m
[36m@@ -13377,9 +13736,8 @@[m [madd_setting :fail_if_no_examples[m
       [m
   [m
   [m
[31m-    Color to use to indicate failure.  Defaults to :red but can be set to[m
[31m-one of the following: [:black, :white, :red, :green, :yellow, :blue,[m
[31m-:magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mColor to use to indicate failure. Defaults to :red but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -13516,9 +13877,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Color used when a pending example is fixed. Defaults to :blue but can[m
[31m-be set to one of the following: [:black, :white, :red, :green,[m
[31m-:yellow, :blue, :magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mColor used when a pending example is fixed. Defaults to :blue but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -13561,8 +13921,8 @@[m [madd_setting :fixed_color[m
       [m
   [m
   [m
[31m-    Returns dirs that have been prepended to the load path by the -I[m
[31m-command line option.[m
[32m+[m[41m    [m
[32m+[m[32mReturns dirs that have been prepended to the load path by the -I command line option.[m
 [m
 [m
   [m
[36m@@ -13605,9 +13965,8 @@[m [mdefine_reader :libs[m
       [m
   [m
   [m
[31m-    Maximum count of failed source lines to display in the failure reports.[m
[31m-(default 10).[m
[31m-return [Integer].[m
[32m+[m[41m    [m
[32m+[m[32mMaximum count of failed source lines to display in the failure reports. (default 10). return [Integer].[m
 [m
 [m
   [m
[36m@@ -13667,8 +14027,8 @@[m [mdefine_reader :only_failures[m
       [m
   [m
   [m
[31m-    Determines where RSpec will send its output.[m
[31m-Default: $stdout.[m
[32m+[m[41m    [m
[32m+[m[32mDetermines where RSpec will send its output. Default: $stdout.[m
 [m
 [m
   [m
[36m@@ -13711,7 +14071,8 @@[m [mdefine_reader :output_stream[m
       [m
   [m
   [m
[31m-    Load files matching this pattern (default: '**{,/*/**}/*_spec.rb').[m
[32m+[m[41m    [m
[32m+[m[32mLoad files matching this pattern (default: '**{,//*}/*_spec.rb').[m
 [m
 [m
   [m
[36m@@ -13754,9 +14115,8 @@[m [mdefine_reader :pattern[m
       [m
   [m
   [m
[31m-    Color to use to print pending examples.  Defaults to :yellow but can[m
[31m-be set to one of the following: [:black, :white, :red, :green,[m
[31m-:yellow, :blue, :magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mColor to use to print pending examples. Defaults to :yellow but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -13803,8 +14163,8 @@[m [madd_setting :pending_color[m
   This method is part of a private API.[m
   You should avoid using this method if possible, as it may be removed or be changed in the future.[m
 [m
[31m-Defaults profile_examples to 10 examples when @profile_examples is[m
[31m-true.[m
[32m+[m
[32m+[m[32mDefaults profile_examples to 10 examples when @profile_examples is true.[m
 [m
 [m
   [m
[36m@@ -13834,12 +14194,8 @@[m [madd_setting :profile_examples[m
       [m
   [m
   [m
[31m-    Specifies which directories contain the source code for your project.[m
[31m-When a failure occurs, RSpec looks through the backtrace to find a[m
[31m-a line of source to print. It first looks for a line coming from[m
[31m-one of the project source directories so that, for example, it prints[m
[31m-the expectation or assertion call rather than the source code from[m
[31m-the expectation or assertion framework.[m
[32m+[m[41m    [m
[32m+[m[32mSpecifies which directories contain the source code for your project. When a failure occurs, RSpec looks through the backtrace to find a a line of source to print. It first looks for a line coming from one of the project source directories so that, for example, it prints the expectation or assertion call rather than the source code from the expectation or assertion framework.[m
 [m
 [m
   [m
[36m@@ -13925,11 +14282,11 @@[m [mdefine_reader :requires[m
       [m
   [m
   [m
[31m-    Deprecated. Use #filter_run_when_matching instead for the specific[m
[31m-filters that you want to be ignored if none match.[m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mUse #filter_run_when_matching instead for the specific filters that you want to be ignored if none match.[m
 [m
[31m-Run all examples if none match the configured filters[m
[31m-(default: false).[m
[32m+[m
[32m+[m[32mRun all examples if none match the configured filters (default: false).[m
 [m
 [m
   [m
[36m@@ -13959,8 +14316,8 @@[m [madd_setting :run_all_when_everything_filtered[m
       [m
   [m
   [m
[31m-    Configures how RSpec treats metadata passed as part of a shared example[m
[31m-group definition. For example, given this shared example group definition:.[m
[32m+[m[41m    [m
[32m+[m[32mConfigures how RSpec treats metadata passed as part of a shared example group definition. For example, given this shared example group definition:.[m
 [m
 RSpec.shared_context "uses DB", :db => true do[m
   around(:example) do |ex|[m
[36m@@ -13969,35 +14326,28 @@[m [mRSpec.shared_context "uses DB", :db => true do[m
 end[m
 [m
 [m
[31m-...there are two ways RSpec can treat the :db => true metadata, each[m
[31m-of which has a corresponding config option:.[m
[32m+[m[32m…there are two ways RSpec can treat the :db => true metadata, each of which has a corresponding config option:.[m
 [m
[32m+[m[32m:trigger_inclusion: this shared context will be implicitly included  in any groups (or examples) that have :db => true metadata.[m
 [m
[31m-:trigger_inclusion: this shared context will be implicitly included[m
[31m-in any groups (or examples) that have :db => true metadata.[m
[31m-:apply_to_host_groups: the metadata will be inherited by the metadata[m
[31m-hash of all host groups and examples.[m
[32m+[m[32m:apply_to_host_groups: the metadata will be inherited by the metadata  hash of all host groups and examples.[m
 [m
 [m
[31m-:trigger_inclusion is the legacy behavior from before RSpec 3.5 but should[m
[31m-be considered deprecated. Instead, you can explicitly include a group with[m
[31m-include_context:.[m
[32m+[m[32m:trigger_inclusion is the legacy behavior from before RSpec 3.5 but should be considered deprecated. Instead, you can explicitly include a group with include_context:.[m
 [m
 RSpec.describe "My model" do[m
   include_context "uses DB"[m
 end[m
 [m
 [m
[31m-...or you can configure RSpec to include the context based on matching metadata[m
[31m-using an API that mirrors configured module inclusion:.[m
[32m+[m[32m…or you can configure RSpec to include the context based on matching metadata using an API that mirrors configured module inclusion:.[m
 [m
 RSpec.configure do |rspec|[m
   rspec.include_context "uses DB", :db => true[m
 end[m
 [m
 [m
[31m-:apply_to_host_groups is a new feature of RSpec 3.5 and will be the only[m
[31m-supported behavior in RSpec 4.[m
[32m+[m[32m:apply_to_host_groups is a new feature of RSpec 3.5 and will be the only supported behavior in RSpec 4.[m
 [m
 [m
   [m
[36m@@ -14099,9 +14452,8 @@[m [mdefine_reader :shared_context_metadata_behavior[m
       [m
   [m
   [m
[31m-    Don't print filter info i.e. "Run options: include :focus=>true"[m
[31m-(default false).[m
[31m-return [Boolean].[m
[32m+[m[41m    [m
[32m+[m[32mDon't print filter info i.e. “Run options: include :focus=>true” (default false). return [Boolean].[m
 [m
 [m
   [m
[36m@@ -14131,9 +14483,8 @@[m [madd_setting :silence_filter_announcements[m
       [m
   [m
   [m
[31m-    Color to use to indicate success.  Defaults to :green but can be set[m
[31m-to one of the following: [:black, :white, :red, :green, :yellow,[m
[31m-:blue, :magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mColor to use to indicate success. Defaults to :green but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -14176,9 +14527,8 @@[m [madd_setting :success_color[m
       [m
   [m
   [m
[31m-    Use threadsafe options where available.[m
[31m-Currently this will place a mutex around memoized values such as let blocks.[m
[31m-return [Boolean].[m
[32m+[m[41m    [m
[32m+[m[32mUse threadsafe options where available. Currently this will place a mutex around memoized values such as let blocks. return [Boolean].[m
 [m
 [m
   [m
[36m@@ -14242,20 +14595,10 @@[m [madd_setting :threadsafe[m
       [m
       [m
         —[m
[31m-        formatter to use. Can be any of the[m
[31m-string values supported from the CLI (pp/progressprogress,[m
[31m-dd/docdoc/documentationdocumentation, hh/htmlhtml, or jj/jsonjson), any[m
[31m-class that implements the formatter protocol and has registered[m
[31m-itself with RSpec as a formatter, or a formatter instance.formatter to use. Can be any of the[m
[31m-string values supported from the CLI (p/progress,[m
[31m-d/doc/documentation, h/html, or j/json), any[m
[31m-class that implements the formatter protocol and has registered[m
[31m-itself with RSpec as a formatter, or a formatter instance.[m
[31m-formatter to use. Can be any of the[m
[31m-string values supported from the CLI (p/progress,[m
[31m-d/doc/documentation, h/html, or j/json), any[m
[31m-class that implements the formatter protocol and has registered[m
[31m-itself with RSpec as a formatter, or a formatter instance.[m
[32m+[m[41m        [m
[32m+[m[32mformatter to use. Can be any of the string values supported from the CLI (pp/progressprogress, dd/docdoc/documentationdocumentation, hh/htmlhtml, or jj/jsonjson), any class that implements the formatter protocol and has registered itself with RSpec as a formatter, or a formatter instance.formatter to use. Can be any of the string values supported from the CLI (p/progress, d/doc/documentation, h/html, or j/json), any class that implements the formatter protocol and has registered itself with RSpec as a formatter, or a formatter instance.[m
[32m+[m
[32m+[m[32mformatter to use. Can be any of the string values supported from the CLI (p/progress, d/doc/documentation, h/html, or j/json), any class that implements the formatter protocol and has registered itself with RSpec as a formatter, or a formatter instance.[m
 [m
       [m
     [m
[36m@@ -14268,11 +14611,8 @@[m [mitself with RSpec as a formatter, or a formatter instance.[m
       [m
       [m
         —[m
[31m-        formatter to use. Can be any of the[m
[31m-string values supported from the CLI (p/progress,[m
[31m-d/doc/documentation, h/html, or j/json), any[m
[31m-class that implements the formatter protocol and has registered[m
[31m-itself with RSpec as a formatter, or a formatter instance.[m
[32m+[m[41m        [m
[32m+[m[32mformatter to use. Can be any of the string values supported from the CLI (p/progress, d/doc/documentation, h/html, or j/json), any class that implements the formatter protocol and has registered itself with RSpec as a formatter, or a formatter instance.[m
 [m
       [m
     [m
[36m@@ -14289,14 +14629,10 @@[m [mitself with RSpec as a formatter, or a formatter instance.[m
       [m
       [m
         —[m
[31m-        where the formatter will write its output.[m
[31m-Can be an IO object or a string path to a file. If not provided,[m
[31m-the configured output_streamoutput_stream ($stdout$stdout, by default) will be used.where the formatter will write its output.[m
[31m-Can be an IO object or a string path to a file. If not provided,[m
[31m-the configured output_stream ($stdout, by default) will be used.[m
[31m-where the formatter will write its output.[m
[31m-Can be an IO object or a string path to a file. If not provided,[m
[31m-the configured output_stream ($stdout, by default) will be used.[m
[32m+[m[41m        [m
[32m+[m[32mwhere the formatter will write its output. Can be an IO object or a string path to a file. If not provided, the configured output_streamoutput_stream ($stdout$stdout, by default) will be used.where the formatter will write its output. Can be an IO object or a string path to a file. If not provided, the configured output_stream ($stdout, by default) will be used.[m
[32m+[m
[32m+[m[32mwhere the formatter will write its output. Can be an IO object or a string path to a file. If not provided, the configured output_stream ($stdout, by default) will be used.[m
 [m
       [m
     [m
[36m@@ -14311,9 +14647,8 @@[m [mthe configured output_stream ($stdout, by default) will be used.[m
       [m
       [m
         —[m
[31m-        where the formatter will write its output.[m
[31m-Can be an IO object or a string path to a file. If not provided,[m
[31m-the configured output_stream ($stdout, by default) will be used.[m
[32m+[m[41m        [m
[32m+[m[32mwhere the formatter will write its output. Can be an IO object or a string path to a file. If not provided, the configured output_stream ($stdout, by default) will be used.[m
 [m
       [m
     [m
[36m@@ -14330,11 +14665,8 @@[m [mthe configured output_stream ($stdout, by default) will be used.[m
       [m
       [m
         —[m
[31m-        formatter to use. Can be any of the[m
[31m-string values supported from the CLI (p/progress,[m
[31m-d/doc/documentation, h/html, or j/json), any[m
[31m-class that implements the formatter protocol and has registered[m
[31m-itself with RSpec as a formatter, or a formatter instance.[m
[32m+[m[41m        [m
[32m+[m[32mformatter to use. Can be any of the string values supported from the CLI (p/progress, d/doc/documentation, h/html, or j/json), any class that implements the formatter protocol and has registered itself with RSpec as a formatter, or a formatter instance.[m
 [m
       [m
     [m
[36m@@ -14351,9 +14683,8 @@[m [mitself with RSpec as a formatter, or a formatter instance.[m
       [m
       [m
         —[m
[31m-        where the formatter will write its output.[m
[31m-Can be an IO object or a string path to a file. If not provided,[m
[31m-the configured output_stream ($stdout, by default) will be used.[m
[32m+[m[41m        [m
[32m+[m[32mwhere the formatter will write its output. Can be an IO object or a string path to a file. If not provided, the configured output_stream ($stdout, by default) will be used.[m
 [m
       [m
     [m
[36m@@ -14386,11 +14717,8 @@[m [mthe configured output_stream ($stdout, by default) will be used.[m
       [m
       [m
         —[m
[31m-        formatter to use. Can be any of the[m
[31m-string values supported from the CLI (p/progress,[m
[31m-d/doc/documentation, h/html, or j/json), any[m
[31m-class that implements the formatter protocol and has registered[m
[31m-itself with RSpec as a formatter, or a formatter instance.[m
[32m+[m[41m        [m
[32m+[m[32mformatter to use. Can be any of the string values supported from the CLI (p/progress, d/doc/documentation, h/html, or j/json), any class that implements the formatter protocol and has registered itself with RSpec as a formatter, or a formatter instance.[m
 [m
       [m
     [m
[36m@@ -14407,9 +14735,8 @@[m [mitself with RSpec as a formatter, or a formatter instance.[m
       [m
       [m
         —[m
[31m-        where the formatter will write its output.[m
[31m-Can be an IO object or a string path to a file. If not provided,[m
[31m-the configured output_stream ($stdout, by default) will be used.[m
[32m+[m[41m        [m
[32m+[m[32mwhere the formatter will write its output. Can be an IO object or a string path to a file. If not provided, the configured output_stream ($stdout, by default) will be used.[m
 [m
       [m
     [m
[36m@@ -14521,11 +14849,8 @@[m [mend[m
       [m
       [m
         —[m
[31m-        formatter to use. Can be any of the[m
[31m-string values supported from the CLI (p/progress,[m
[31m-d/doc/documentation, h/html, or j/json), any[m
[31m-class that implements the formatter protocol and has registered[m
[31m-itself with RSpec as a formatter, or a formatter instance.[m
[32m+[m[41m        [m
[32m+[m[32mformatter to use. Can be any of the string values supported from the CLI (p/progress, d/doc/documentation, h/html, or j/json), any class that implements the formatter protocol and has registered itself with RSpec as a formatter, or a formatter instance.[m
 [m
       [m
     [m
[36m@@ -14542,9 +14867,8 @@[m [mitself with RSpec as a formatter, or a formatter instance.[m
       [m
       [m
         —[m
[31m-        where the formatter will write its output.[m
[31m-Can be an IO object or a string path to a file. If not provided,[m
[31m-the configured output_stream ($stdout, by default) will be used.[m
[32m+[m[41m        [m
[32m+[m[32mwhere the formatter will write its output. Can be an IO object or a string path to a file. If not provided, the configured output_stream ($stdout, by default) will be used.[m
 [m
       [m
     [m
[36m@@ -14590,9 +14915,7 @@[m [mRSpec.configuration.add_setting :foo[m
 RSpec.configuration.add_setting :foo[m
 [m
 [m
[31m-Used internally and by extension frameworks like rspec-rails, so they[m
[31m-can add config settings that are domain specific. For example:.Used internally and by extension frameworks like rspec-rails, so they[m
[31m-can add config settings that are domain specific. For example:.[m
[32m+[m[32mUsed internally and by extension frameworks like rspec-rails, so they can add config settings that are domain specific. For example:.Used internally and by extension frameworks like rspec-rails, so they can add config settings that are domain specific. For example:.[m
 [m
 RSpecRSpec..configureconfigure dodo ||cc||[m
   cc..add_settingadd_setting :use_transactional_fixtures:use_transactional_fixtures,,[m
[36m@@ -14611,9 +14934,7 @@[m [mRSpec.configure do |c|[m
 end[m
 [m
 [m
[31m-add_setting creates three methods on the configuration object, a[m
[31m-setter, a getter, and a predicate:.add_setting creates three methods on the configuration object, a[m
[31m-setter, a getter, and a predicate:.[m
[32m+[m[32madd_setting creates three methods on the configuration object, a setter, a getter, and a predicate:.add_setting creates three methods on the configuration object, a setter, a getter, and a predicate:.[m
 [m
 RSpecRSpec..configurationconfiguration..foofoo==((valuevalue))[m
 RSpecRSpec..configurationconfiguration..foofoo[m
[36m@@ -14634,8 +14956,7 @@[m [mRSpec.configuration.foo? # Returns true if foo returns anything but nil or false[m
 RSpec.configuration.add_setting :foo[m
 [m
 [m
[31m-Used internally and by extension frameworks like rspec-rails, so they[m
[31m-can add config settings that are domain specific. For example:.[m
[32m+[m[32mUsed internally and by extension frameworks like rspec-rails, so they can add config settings that are domain specific. For example:.[m
 [m
 RSpec.configure do |c|[m
   c.add_setting :use_transactional_fixtures,[m
[36m@@ -14644,8 +14965,7 @@[m [mRSpec.configure do |c|[m
 end[m
 [m
 [m
[31m-add_setting creates three methods on the configuration object, a[m
[31m-setter, a getter, and a predicate:.[m
[32m+[m[32madd_setting creates three methods on the configuration object, a setter, a getter, and a predicate:.[m
 [m
 RSpec.configuration.foo=(value)[m
 RSpec.configuration.foo[m
[36m@@ -14661,8 +14982,7 @@[m [mRSpec.configuration.foo? # Returns true if foo returns anything but nil or false[m
 RSpec.configuration.add_setting :foo[m
 [m
 [m
[31m-Used internally and by extension frameworks like rspec-rails, so they[m
[31m-can add config settings that are domain specific. For example:.[m
[32m+[m[32mUsed internally and by extension frameworks like rspec-rails, so they can add config settings that are domain specific. For example:.[m
 [m
 RSpec.configure do |c|[m
   c.add_setting :use_transactional_fixtures,[m
[36m@@ -14671,8 +14991,7 @@[m [mRSpec.configure do |c|[m
 end[m
 [m
 [m
[31m-add_setting creates three methods on the configuration object, a[m
[31m-setter, a getter, and a predicate:.[m
[32m+[m[32madd_setting creates three methods on the configuration object, a setter, a getter, and a predicate:.[m
 [m
 RSpec.configuration.foo=(value)[m
 RSpec.configuration.foo[m
[36m@@ -14757,7 +15080,8 @@[m [ma customizable set of options[m
             [m
           [m
           [m
[31m-            — Set a default value for the generated getter and predicate methods:Set a default value for the generated getter and predicate methods:[m
[32m+[m[32m            —[m[41m [m
[32m+[m[32mSet a default value for the generated getter and predicate methods:Set a default value for the generated getter and predicate methods:[m
 [m
 add_settingadd_setting((:foo:foo,, :default:default =>=> ""default valuedefault value"""default value"))[m
 add_setting(:foo, :default => "default value")[m
[36m@@ -14776,7 +15101,8 @@[m [madd_setting(:foo, :default => "default value")[m
             [m
           [m
           [m
[31m-            — Set a default value for the generated getter and predicate methods:[m
[32m+[m[32m            —[m[41m [m
[32m+[m[32mSet a default value for the generated getter and predicate methods:[m
 [m
 add_setting(:foo, :default => "default value")[m
 [m
[36m@@ -14793,9 +15119,8 @@[m [madd_setting(:foo, :default => "default value")[m
             [m
           [m
           [m
[31m-            — Use :alias_with:alias_with to alias the setter, getter, and predicate to[m
[31m-another name, or names:Use :alias_with to alias the setter, getter, and predicate to[m
[31m-another name, or names:[m
[32m+[m[32m            —[m[41m [m
[32m+[m[32mUse :alias_with:alias_with to alias the setter, getter, and predicate to another name, or names:Use :alias_with to alias the setter, getter, and predicate to another name, or names:[m
 [m
 add_settingadd_setting((:foo:foo,, :alias_with:alias_with =>=> :bar:bar))[m
 add_settingadd_setting((:foo:foo,, :alias_with:alias_with =>=> [[:bar:bar,, :baz:baz]]))[m
[36m@@ -14804,8 +15129,8 @@[m [madd_setting(:foo, :alias_with => [:bar, :baz])[m
 add_setting(:foo, :alias_with => :bar)[m
 add_setting(:foo, :alias_with => [:bar, :baz])[m
 [m
[31m-Use :alias_with to alias the setter, getter, and predicate to[m
[31m-another name, or names:[m
[32m+[m
[32m+[m[32mUse :alias_with to alias the setter, getter, and predicate to another name, or names:[m
 [m
 add_setting(:foo, :alias_with => :bar)[m
 add_setting(:foo, :alias_with => [:bar, :baz])[m
[36m@@ -14819,8 +15144,8 @@[m [madd_setting(:foo, :alias_with => [:bar, :baz])[m
             [m
           [m
           [m
[31m-            — Use :alias_with to alias the setter, getter, and predicate to[m
[31m-another name, or names:[m
[32m+[m[32m            —[m[41m [m
[32m+[m[32mUse :alias_with to alias the setter, getter, and predicate to another name, or names:[m
 [m
 add_setting(:foo, :alias_with => :bar)[m
 add_setting(:foo, :alias_with => [:bar, :baz])[m
[36m@@ -14838,7 +15163,8 @@[m [madd_setting(:foo, :alias_with => [:bar, :baz])[m
             [m
           [m
           [m
[31m-            — Set a default value for the generated getter and predicate methods:[m
[32m+[m[32m            —[m[41m [m
[32m+[m[32mSet a default value for the generated getter and predicate methods:[m
 [m
 add_setting(:foo, :default => "default value")[m
 [m
[36m@@ -14853,8 +15179,8 @@[m [madd_setting(:foo, :default => "default value")[m
             [m
           [m
           [m
[31m-            — Use :alias_with to alias the setter, getter, and predicate to[m
[31m-another name, or names:[m
[32m+[m[32m            —[m[41m [m
[32m+[m[32mUse :alias_with to alias the setter, getter, and predicate to another name, or names:[m
 [m
 add_setting(:foo, :alias_with => :bar)[m
 add_setting(:foo, :alias_with => [:bar, :baz])[m
[36m@@ -14905,7 +15232,8 @@[m [madd_setting(:foo, :alias_with => [:bar, :baz])[m
             [m
           [m
           [m
[31m-            — Set a default value for the generated getter and predicate methods:[m
[32m+[m[32m            —[m[41m [m
[32m+[m[32mSet a default value for the generated getter and predicate methods:[m
 [m
 add_setting(:foo, :default => "default value")[m
 [m
[36m@@ -14920,8 +15248,8 @@[m [madd_setting(:foo, :default => "default value")[m
             [m
           [m
           [m
[31m-            — Use :alias_with to alias the setter, getter, and predicate to[m
[31m-another name, or names:[m
[32m+[m[32m            —[m[41m [m
[32m+[m[32mUse :alias_with to alias the setter, getter, and predicate to another name, or names:[m
 [m
 add_setting(:foo, :alias_with => :bar)[m
 add_setting(:foo, :alias_with => [:bar, :baz])[m
[36m@@ -15057,8 +15386,7 @@[m [mend[m
 RSpec.configuration.add_setting :foo[m
 [m
 [m
[31m-Used internally and by extension frameworks like rspec-rails, so they[m
[31m-can add config settings that are domain specific. For example:.[m
[32m+[m[32mUsed internally and by extension frameworks like rspec-rails, so they can add config settings that are domain specific. For example:.[m
 [m
 RSpec.configure do |c|[m
   c.add_setting :use_transactional_fixtures,[m
[36m@@ -15067,8 +15395,7 @@[m [mRSpec.configure do |c|[m
 end[m
 [m
 [m
[31m-add_setting creates three methods on the configuration object, a[m
[31m-setter, a getter, and a predicate:.[m
[32m+[m[32madd_setting creates three methods on the configuration object, a setter, a getter, and a predicate:.[m
 [m
 RSpec.configuration.foo=(value)[m
 RSpec.configuration.foo[m
[36m@@ -15116,7 +15444,8 @@[m [mRSpec.configuration.foo? # Returns true if foo returns anything but nil or false[m
             [m
           [m
           [m
[31m-            — Set a default value for the generated getter and predicate methods:[m
[32m+[m[32m            —[m[41m [m
[32m+[m[32mSet a default value for the generated getter and predicate methods:[m
 [m
 add_setting(:foo, :default => "default value")[m
 [m
[36m@@ -15131,8 +15460,8 @@[m [madd_setting(:foo, :default => "default value")[m
             [m
           [m
           [m
[31m-            — Use :alias_with to alias the setter, getter, and predicate to[m
[31m-another name, or names:[m
[32m+[m[32m            —[m[41m [m
[32m+[m[32mUse :alias_with to alias the setter, getter, and predicate to another name, or names:[m
 [m
 add_setting(:foo, :alias_with => :bar)[m
 add_setting(:foo, :alias_with => [:bar, :baz])[m
[36m@@ -15177,21 +15506,18 @@[m [mend[m
       [m
   [m
   [m
[32m+[m[41m    [m
 Defines a afterafter hook. See Hooks#after for full docs.Defines a after hook. See Hooks#after for full docs.[m
 [m
[31m-This method differs from Hooks#after in only one way: it supports[m
[31m-the :suite:suite scope. Hooks with the :suite:suite scope will be run once after[m
[31m-the last example of the entire suite is executed.This method differs from Hooks#after in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once after[m
[31m-the last example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#after in only one way: it supports the :suite:suite scope. Hooks with the :suite:suite scope will be run once after the last example of the entire suite is executed.This method differs from Hooks#after in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once after the last example of the entire suite is executed.[m
[32m+[m
 [m
   [m
     [m
 Defines a after hook. See Hooks#after for full docs.[m
 [m
[31m-This method differs from Hooks#after in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once after[m
[31m-the last example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#after in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once after the last example of the entire suite is executed.[m
[32m+[m
 [m
   [m
 [m
[36m@@ -15199,9 +15525,7 @@[m [mthe last example of the entire suite is executed.[m
     [m
 Defines a after hook. See Hooks#after for full docs.[m
 [m
[31m-This method differs from Hooks#after in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once after[m
[31m-the last example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#after in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once after the last example of the entire suite is executed.[m
 [m
 [m
   [m
[36m@@ -15434,11 +15758,10 @@[m [mend[m
 [m
   [m
   [m
[32m+[m[41m    [m
 Defines a after hook. See Hooks#after for full docs.[m
 [m
[31m-This method differs from Hooks#after in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once after[m
[31m-the last example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#after in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once after the last example of the entire suite is executed.[m
 [m
 [m
   [m
[36m@@ -15506,40 +15829,33 @@[m [mend[m
     [m
   [m
     Note:Note:[m
[31m-    The defined aliased will also be added to the top level[m
[31m-(e.g. mainmain and from within modules) if[m
[31m-expose_dsl_globallyexpose_dsl_globally is set to true.The defined aliased will also be added to the top level[m
[31m-(e.g. main and from within modules) if[m
[31m-expose_dsl_globally is set to true.[m
[31m-The defined aliased will also be added to the top level[m
[31m-(e.g. main and from within modules) if[m
[31m-expose_dsl_globally is set to true.[m
[32m+[m[41m    [m
[32m+[m[32mThe defined aliased will also be added to the top level (e.g. mainmain and from within modules) if expose_dsl_globallyexpose_dsl_globally is set to true.The defined aliased will also be added to the top level (e.g. main and from within modules) if expose_dsl_globally is set to true.[m
[32m+[m
[32m+[m[32mThe defined aliased will also be added to the top level (e.g. main and from within modules) if expose_dsl_globally is set to true.[m
 [m
   [m
     Note:[m
[31m-    The defined aliased will also be added to the top level[m
[31m-(e.g. main and from within modules) if[m
[31m-expose_dsl_globally is set to true.[m
[32m+[m[41m    [m
[32m+[m[32mThe defined aliased will also be added to the top level (e.g. main and from within modules) if expose_dsl_globally is set to true.[m
 [m
   [m
 [m
[31m-Creates a method that defines an example group with the provided[m
[31m-metadata. Can be used to define example group/metadata shortcuts.Creates a method that defines an example group with the provided[m
[31m-metadata. Can be used to define example group/metadata shortcuts.[m
[32m+[m
[32m+[m[32mCreates a method that defines an example group with the provided metadata. Can be used to define example group/metadata shortcuts.Creates a method that defines an example group with the provided metadata. Can be used to define example group/metadata shortcuts.[m
 [m
 [m
   [m
     [m
   [m
     Note:[m
[31m-    The defined aliased will also be added to the top level[m
[31m-(e.g. main and from within modules) if[m
[31m-expose_dsl_globally is set to true.[m
[32m+[m[41m    [m
[32m+[m[32mThe defined aliased will also be added to the top level (e.g. main and from within modules) if expose_dsl_globally is set to true.[m
 [m
   [m
 [m
[31m-Creates a method that defines an example group with the provided[m
[31m-metadata. Can be used to define example group/metadata shortcuts.[m
[32m+[m
[32m+[m[32mCreates a method that defines an example group with the provided metadata. Can be used to define example group/metadata shortcuts.[m
 [m
 [m
   [m
[36m@@ -15548,14 +15864,13 @@[m [mmetadata. Can be used to define example group/metadata shortcuts.[m
     [m
   [m
     Note:[m
[31m-    The defined aliased will also be added to the top level[m
[31m-(e.g. main and from within modules) if[m
[31m-expose_dsl_globally is set to true.[m
[32m+[m[41m    [m
[32m+[m[32mThe defined aliased will also be added to the top level (e.g. main and from within modules) if expose_dsl_globally is set to true.[m
[32m+[m
   [m
 [m
 [m
[31m-Creates a method that defines an example group with the provided[m
[31m-metadata. Can be used to define example group/metadata shortcuts.[m
[32m+[m[32mCreates a method that defines an example group with the provided metadata. Can be used to define example group/metadata shortcuts.[m
 [m
 [m
   [m
[36m@@ -15786,14 +16101,13 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    The defined aliased will also be added to the top level[m
[31m-(e.g. main and from within modules) if[m
[31m-expose_dsl_globally is set to true.[m
     [m
[32m+[m[32mThe defined aliased will also be added to the top level (e.g. main and from within modules) if expose_dsl_globally is set to true.[m
 [m
   [m
[31m-Creates a method that defines an example group with the provided[m
[31m-metadata. Can be used to define example group/metadata shortcuts.[m
[32m+[m
[32m+[m
[32m+[m[32mCreates a method that defines an example group with the provided metadata. Can be used to define example group/metadata shortcuts.[m
 [m
 [m
   [m
[36m@@ -15863,67 +16177,54 @@[m [mend[m
     [m
   [m
     Note:Note:[m
[31m-    The specific example alias below (pendingpending) is already[m
[31m-defined for you.The specific example alias below (pending) is already[m
[31m-defined for you.[m
[31m-The specific example alias below (pending) is already[m
[31m-defined for you.[m
[32m+[m[41m    [m
[32m+[m[32mThe specific example alias below (pendingpending) is already defined for you.The specific example alias below (pending) is already defined for you.[m
[32m+[m
[32m+[m[32mThe specific example alias below (pending) is already defined for you.[m
 [m
   [m
     Note:[m
[31m-    The specific example alias below (pending) is already[m
[31m-defined for you.[m
[32m+[m[41m    [m
[32m+[m[32mThe specific example alias below (pending) is already defined for you.[m
 [m
   [m
 [m
   [m
     Note:Note:[m
[31m-    Use with caution. This extends the language used in your[m
[31m-specs, but does not add any additional documentation. We use this[m
[31m-in RSpec to define methods like focusfocus and xitxit, but we also add[m
[31m-docs for those methods.Use with caution. This extends the language used in your[m
[31m-specs, but does not add any additional documentation. We use this[m
[31m-in RSpec to define methods like focus and xit, but we also add[m
[31m-docs for those methods.[m
[31m-Use with caution. This extends the language used in your[m
[31m-specs, but does not add any additional documentation. We use this[m
[31m-in RSpec to define methods like focus and xit, but we also add[m
[31m-docs for those methods.[m
[32m+[m[41m    [m
[32m+[m[32mUse with caution. This extends the language used in your specs, but does not add any additional documentation. We use this in RSpec to define methods like focusfocus and xitxit, but we also add docs for those methods.Use with caution. This extends the language used in your specs, but does not add any additional documentation. We use this in RSpec to define methods like focus and xit, but we also add docs for those methods.[m
[32m+[m
[32m+[m[32mUse with caution. This extends the language used in your specs, but does not add any additional documentation. We use this in RSpec to define methods like focus and xit, but we also add docs for those methods.[m
 [m
   [m
     Note:[m
[31m-    Use with caution. This extends the language used in your[m
[31m-specs, but does not add any additional documentation. We use this[m
[31m-in RSpec to define methods like focus and xit, but we also add[m
[31m-docs for those methods.[m
     [m
[32m+[m[32mUse with caution. This extends the language used in your specs, but does not add any additional documentation. We use this in RSpec to define methods like focus and xit, but we also add docs for those methods.[m
 [m
   [m
[31m-Creates a method that delegates to example including the submitted[m
[31m-args. Used internally to add variants of example like pending:.Creates a method that delegates to example including the submitted[m
[31m-args. Used internally to add variants of example like pending:.[m
[32m+[m
[32m+[m
[32m+[m[32mCreates a method that delegates to example including the submitted args. Used internally to add variants of example like pending:.Creates a method that delegates to example including the submitted args. Used internally to add variants of example like pending:.[m
 [m
 [m
   [m
     [m
   [m
     Note:[m
[31m-    The specific example alias below (pending) is already[m
[31m-defined for you.[m
[32m+[m[41m    [m
[32m+[m[32mThe specific example alias below (pending) is already defined for you.[m
 [m
   [m
 [m
   [m
     Note:[m
[31m-    Use with caution. This extends the language used in your[m
[31m-specs, but does not add any additional documentation. We use this[m
[31m-in RSpec to define methods like focus and xit, but we also add[m
[31m-docs for those methods.[m
     [m
[32m+[m[32mUse with caution. This extends the language used in your specs, but does not add any additional documentation. We use this in RSpec to define methods like focus and xit, but we also add docs for those methods.[m
 [m
   [m
[31m-Creates a method that delegates to example including the submitted[m
[31m-args. Used internally to add variants of example like pending:.[m
[32m+[m
[32m+[m
[32m+[m[32mCreates a method that delegates to example including the submitted args. Used internally to add variants of example like pending:.[m
 [m
 [m
   [m
[36m@@ -15932,22 +16233,20 @@[m [margs. Used internally to add variants of example like pending:.[m
     [m
   [m
     Note:[m
[31m-    The specific example alias below (pending) is already[m
[31m-defined for you.[m
[32m+[m[41m    [m
[32m+[m[32mThe specific example alias below (pending) is already defined for you.[m
 [m
   [m
 [m
   [m
     Note:[m
[31m-    Use with caution. This extends the language used in your[m
[31m-specs, but does not add any additional documentation. We use this[m
[31m-in RSpec to define methods like focus and xit, but we also add[m
[31m-docs for those methods.[m
     [m
[32m+[m[32mUse with caution. This extends the language used in your specs, but does not add any additional documentation. We use this in RSpec to define methods like focus and xit, but we also add docs for those methods.[m
 [m
   [m
[31m-Creates a method that delegates to example including the submitted[m
[31m-args. Used internally to add variants of example like pending:.[m
[32m+[m
[32m+[m
[32m+[m[32mCreates a method that delegates to example including the submitted args. Used internally to add variants of example like pending:.[m
 [m
 [m
   [m
[36m@@ -16291,22 +16600,20 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    The specific example alias below (pending) is already[m
[31m-defined for you.[m
[32m+[m[41m    [m
[32m+[m[32mThe specific example alias below (pending) is already defined for you.[m
 [m
   [m
 [m
   [m
     Note:[m
[31m-    Use with caution. This extends the language used in your[m
[31m-specs, but does not add any additional documentation. We use this[m
[31m-in RSpec to define methods like focus and xit, but we also add[m
[31m-docs for those methods.[m
[32m+[m[41m    [m
[32m+[m[32mUse with caution. This extends the language used in your specs, but does not add any additional documentation. We use this in RSpec to define methods like focus and xit, but we also add docs for those methods.[m
 [m
   [m
 [m
[31m-Creates a method that delegates to example including the submitted[m
[31m-args. Used internally to add variants of example like pending:.[m
[32m+[m
[32m+[m[32mCreates a method that delegates to example including the submitted args. Used internally to add variants of example like pending:.[m
 [m
 [m
   [m
[36m@@ -16403,48 +16712,33 @@[m [mend[m
     [m
   [m
     Note:Note:[m
[31m-    Use with caution. This extends the language used in your[m
[31m-specs, but does not add any additional documentation. We use this[m
[31m-in RSpec to define it_should_behave_likeit_should_behave_like (for backward[m
[31m-compatibility), but we also add docs for that method.Use with caution. This extends the language used in your[m
[31m-specs, but does not add any additional documentation. We use this[m
[31m-in RSpec to define it_should_behave_like (for backward[m
[31m-compatibility), but we also add docs for that method.[m
[31m-Use with caution. This extends the language used in your[m
[31m-specs, but does not add any additional documentation. We use this[m
[31m-in RSpec to define it_should_behave_like (for backward[m
[31m-compatibility), but we also add docs for that method.[m
[32m+[m[41m    [m
[32m+[m[32mUse with caution. This extends the language used in your specs, but does not add any additional documentation. We use this in RSpec to define it_should_behave_likeit_should_behave_like (for backward compatibility), but we also add docs for that method.Use with caution. This extends the language used in your specs, but does not add any additional documentation. We use this in RSpec to define it_should_behave_like (for backward compatibility), but we also add docs for that method.[m
[32m+[m
[32m+[m[32mUse with caution. This extends the language used in your specs, but does not add any additional documentation. We use this in RSpec to define it_should_behave_like (for backward compatibility), but we also add docs for that method.[m
 [m
   [m
     Note:[m
[31m-    Use with caution. This extends the language used in your[m
[31m-specs, but does not add any additional documentation. We use this[m
[31m-in RSpec to define it_should_behave_like (for backward[m
[31m-compatibility), but we also add docs for that method.[m
[32m+[m[41m    [m
[32m+[m[32mUse with caution. This extends the language used in your specs, but does not add any additional documentation. We use this in RSpec to define it_should_behave_like (for backward compatibility), but we also add docs for that method.[m
[32m+[m
   [m
 [m
 [m
[31m-Define an alias for it_should_behave_like that allows different[m
[31m-language (like "it_has_behavior" or "it_behaves_like") to be[m
[31m-employed when including shared examples.Define an alias for it_should_behave_like that allows different[m
[31m-language (like "it_has_behavior" or "it_behaves_like") to be[m
[31m-employed when including shared examples.[m
[32m+[m[32mDefine an alias for it_should_behave_like that allows different language (like “it_has_behavior” or “it_behaves_like”) to be employed when including shared examples.Define an alias for it_should_behave_like that allows different language (like “it_has_behavior” or “it_behaves_like”) to be employed when including shared examples.[m
 [m
 [m
   [m
     [m
   [m
     Note:[m
[31m-    Use with caution. This extends the language used in your[m
[31m-specs, but does not add any additional documentation. We use this[m
[31m-in RSpec to define it_should_behave_like (for backward[m
[31m-compatibility), but we also add docs for that method.[m
     [m
[32m+[m[32mUse with caution. This extends the language used in your specs, but does not add any additional documentation. We use this in RSpec to define it_should_behave_like (for backward compatibility), but we also add docs for that method.[m
 [m
   [m
[31m-Define an alias for it_should_behave_like that allows different[m
[31m-language (like "it_has_behavior" or "it_behaves_like") to be[m
[31m-employed when including shared examples.[m
[32m+[m
[32m+[m
[32m+[m[32mDefine an alias for it_should_behave_like that allows different language (like “it_has_behavior” or “it_behaves_like”) to be employed when including shared examples.[m
 [m
 [m
   [m
[36m@@ -16453,16 +16747,13 @@[m [memployed when including shared examples.[m
     [m
   [m
     Note:[m
[31m-    Use with caution. This extends the language used in your[m
[31m-specs, but does not add any additional documentation. We use this[m
[31m-in RSpec to define it_should_behave_like (for backward[m
[31m-compatibility), but we also add docs for that method.[m
[32m+[m[41m    [m
[32m+[m[32mUse with caution. This extends the language used in your specs, but does not add any additional documentation. We use this in RSpec to define it_should_behave_like (for backward compatibility), but we also add docs for that method.[m
 [m
   [m
 [m
[31m-Define an alias for it_should_behave_like that allows different[m
[31m-language (like "it_has_behavior" or "it_behaves_like") to be[m
[31m-employed when including shared examples.[m
[32m+[m
[32m+[m[32mDefine an alias for it_should_behave_like that allows different language (like “it_has_behavior” or “it_behaves_like”) to be employed when including shared examples.[m
 [m
 [m
   [m
[36m@@ -16655,16 +16946,13 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    Use with caution. This extends the language used in your[m
[31m-specs, but does not add any additional documentation. We use this[m
[31m-in RSpec to define it_should_behave_like (for backward[m
[31m-compatibility), but we also add docs for that method.[m
[32m+[m[41m    [m
[32m+[m[32mUse with caution. This extends the language used in your specs, but does not add any additional documentation. We use this in RSpec to define it_should_behave_like (for backward compatibility), but we also add docs for that method.[m
[32m+[m
   [m
 [m
 [m
[31m-Define an alias for it_should_behave_like that allows different[m
[31m-language (like "it_has_behavior" or "it_behaves_like") to be[m
[31m-employed when including shared examples.[m
[32m+[m[32mDefine an alias for it_should_behave_like that allows different language (like “it_has_behavior” or “it_behaves_like”) to be employed when including shared examples.[m
 [m
 [m
   [m
[36m@@ -16719,45 +17007,32 @@[m [mend[m
       [m
   [m
   [m
[31m-    Adds blockblock to the end of the list of afterafter blocks in the same[m
[31m-scope (:example:example, :context:context, or :suite:suite), in contrast to #after,[m
[31m-which adds the hook to the start of the list.Adds block to the end of the list of after blocks in the same[m
[31m-scope (:example, :context, or :suite), in contrast to #after,[m
[31m-which adds the hook to the start of the list.[m
[32m+[m[41m    [m
[32m+[m[32mAdds blockblock to the end of the list of afterafter blocks in the same scope (:example:example, :context:context, or :suite:suite), in contrast to #after, which adds the hook to the start of the list.Adds block to the end of the list of after blocks in the same scope (:example, :context, or :suite), in contrast to #after, which adds the hook to the start of the list.[m
 [m
 See Hooks#after for full afterafter hook docs.See Hooks#after for full after hook docs.[m
 [m
[31m-This method differs from Hooks#append_after in only one way: it supports[m
[31m-the :suite:suite scope. Hooks with the :suite:suite scope will be run once after[m
[31m-the last example of the entire suite is executed.This method differs from Hooks#append_after in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once after[m
[31m-the last example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#append_after in only one way: it supports the :suite:suite scope. Hooks with the :suite:suite scope will be run once after the last example of the entire suite is executed.This method differs from Hooks#append_after in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once after the last example of the entire suite is executed.[m
 [m
 [m
   [m
[31m-    Adds block to the end of the list of after blocks in the same[m
[31m-scope (:example, :context, or :suite), in contrast to #after,[m
[31m-which adds the hook to the start of the list.[m
[32m+[m[41m    [m
[32m+[m[32mAdds block to the end of the list of after blocks in the same scope (:example, :context, or :suite), in contrast to #after, which adds the hook to the start of the list.[m
 [m
 See Hooks#after for full after hook docs.[m
 [m
[31m-This method differs from Hooks#append_after in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once after[m
[31m-the last example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#append_after in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once after the last example of the entire suite is executed.[m
 [m
 [m
   [m
 [m
   [m
[31m-    Adds block to the end of the list of after blocks in the same[m
[31m-scope (:example, :context, or :suite), in contrast to #after,[m
[31m-which adds the hook to the start of the list.[m
[32m+[m[41m    [m
[32m+[m[32mAdds block to the end of the list of after blocks in the same scope (:example, :context, or :suite), in contrast to #after, which adds the hook to the start of the list.[m
 [m
 See Hooks#after for full after hook docs.[m
 [m
[31m-This method differs from Hooks#append_after in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once after[m
[31m-the last example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#append_after in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once after the last example of the entire suite is executed.[m
 [m
 [m
   [m
[36m@@ -16990,15 +17265,12 @@[m [mend[m
 [m
   [m
   [m
[31m-    Adds block to the end of the list of after blocks in the same[m
[31m-scope (:example, :context, or :suite), in contrast to #after,[m
[31m-which adds the hook to the start of the list.[m
[32m+[m[41m    [m
[32m+[m[32mAdds block to the end of the list of after blocks in the same scope (:example, :context, or :suite), in contrast to #after, which adds the hook to the start of the list.[m
 [m
 See Hooks#after for full after hook docs.[m
 [m
[31m-This method differs from Hooks#append_after in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once after[m
[31m-the last example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#append_after in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once after the last example of the entire suite is executed.[m
 [m
 [m
   [m
[36m@@ -17290,35 +17566,26 @@[m [mend[m
       [m
   [m
   [m
[32m+[m[41m    [m
 Regexps used to exclude lines from backtraces.Regexps used to exclude lines from backtraces.[m
 [m
[31m-Excludes lines from ruby (and jruby) source, installed gems, anything[m
[31m-in any "bin" directory, and any of the RSpec libs (outside gem[m
[31m-installs) by default.Excludes lines from ruby (and jruby) source, installed gems, anything[m
[31m-in any "bin" directory, and any of the RSpec libs (outside gem[m
[31m-installs) by default.[m
[32m+[m[32mExcludes lines from ruby (and jruby) source, installed gems, anything in any “bin” directory, and any of the RSpec libs (outside gem installs) by default.Excludes lines from ruby (and jruby) source, installed gems, anything in any “bin” directory, and any of the RSpec libs (outside gem installs) by default.[m
 [m
 You can modify the list via the getter, or replace it with the setter.You can modify the list via the getter, or replace it with the setter.[m
 [m
[31m-To override this behaviour and display a full backtrace, use[m
[31m---backtrace--backtrace on the command line, in a .rspec.rspec file, or in the[m
[31m-rspec_optionsrspec_options attribute of RSpec's rake task.To override this behaviour and display a full backtrace, use[m
[31m---backtrace on the command line, in a .rspec file, or in the[m
[31m-rspec_options attribute of RSpec's rake task.[m
[32m+[m[32mTo override this behaviour and display a full backtrace, use --backtrace--backtrace on the command line, in a .rspec.rspec file, or in the rspec_optionsrspec_options attribute of RSpec's rake task.To override this behaviour and display a full backtrace, use --backtrace on the command line, in a .rspec file, or in the rspec_options attribute of RSpec's rake task.[m
[32m+[m
 [m
   [m
     [m
 Regexps used to exclude lines from backtraces.[m
 [m
[31m-Excludes lines from ruby (and jruby) source, installed gems, anything[m
[31m-in any "bin" directory, and any of the RSpec libs (outside gem[m
[31m-installs) by default.[m
[32m+[m[32mExcludes lines from ruby (and jruby) source, installed gems, anything in any “bin” directory, and any of the RSpec libs (outside gem installs) by default.[m
 [m
 You can modify the list via the getter, or replace it with the setter.[m
 [m
[31m-To override this behaviour and display a full backtrace, use[m
[31m---backtrace on the command line, in a .rspec file, or in the[m
[31m-rspec_options attribute of RSpec's rake task.[m
[32m+[m[32mTo override this behaviour and display a full backtrace, use --backtrace on the command line, in a .rspec file, or in the rspec_options attribute of RSpec's rake task.[m
[32m+[m
 [m
   [m
 [m
[36m@@ -17326,15 +17593,11 @@[m [mrspec_options attribute of RSpec's rake task.[m
     [m
 Regexps used to exclude lines from backtraces.[m
 [m
[31m-Excludes lines from ruby (and jruby) source, installed gems, anything[m
[31m-in any "bin" directory, and any of the RSpec libs (outside gem[m
[31m-installs) by default.[m
[32m+[m[32mExcludes lines from ruby (and jruby) source, installed gems, anything in any “bin” directory, and any of the RSpec libs (outside gem installs) by default.[m
 [m
 You can modify the list via the getter, or replace it with the setter.[m
 [m
[31m-To override this behaviour and display a full backtrace, use[m
[31m---backtrace on the command line, in a .rspec file, or in the[m
[31m-rspec_options attribute of RSpec's rake task.[m
[32m+[m[32mTo override this behaviour and display a full backtrace, use --backtrace on the command line, in a .rspec file, or in the rspec_options attribute of RSpec's rake task.[m
 [m
 [m
   [m
[36m@@ -17467,17 +17730,14 @@[m [mend[m
 [m
   [m
   [m
[32m+[m[41m    [m
 Regexps used to exclude lines from backtraces.[m
 [m
[31m-Excludes lines from ruby (and jruby) source, installed gems, anything[m
[31m-in any "bin" directory, and any of the RSpec libs (outside gem[m
[31m-installs) by default.[m
[32m+[m[32mExcludes lines from ruby (and jruby) source, installed gems, anything in any “bin” directory, and any of the RSpec libs (outside gem installs) by default.[m
 [m
 You can modify the list via the getter, or replace it with the setter.[m
 [m
[31m-To override this behaviour and display a full backtrace, use[m
[31m---backtrace on the command line, in a .rspec file, or in the[m
[31m-rspec_options attribute of RSpec's rake task.[m
[32m+[m[32mTo override this behaviour and display a full backtrace, use --backtrace on the command line, in a .rspec file, or in the rspec_options attribute of RSpec's rake task.[m
 [m
 [m
   [m
[36m@@ -17745,9 +18016,7 @@[m [mend[m
 [m
 Defaults to [Regexp.new Dir.getwd].Defaults to [Regexp.new Dir.getwd].[m
 [m
[31m-Lines that match an exclusion andand an inclusion pattern[m
[31m-will be included.Lines that match an exclusion and an inclusion pattern[m
[31m-will be included.[m
[32m+[m[32mLines that match an exclusion andand an inclusion pattern will be included.Lines that match an exclusion and an inclusion pattern will be included.[m
 [m
 You can modify the list via the getter, or replace it with the setter.You can modify the list via the getter, or replace it with the setter.[m
 [m
[36m@@ -17757,8 +18027,7 @@[m [mYou can modify the list via the getter, or replace it with the setter.You can mo[m
 [m
 Defaults to [Regexp.new Dir.getwd].[m
 [m
[31m-Lines that match an exclusion and an inclusion pattern[m
[31m-will be included.[m
[32m+[m[32mLines that match an exclusion and an inclusion pattern will be included.[m
 [m
 You can modify the list via the getter, or replace it with the setter.[m
 [m
[36m@@ -17770,8 +18040,7 @@[m [mYou can modify the list via the getter, or replace it with the setter.[m
 [m
 Defaults to [Regexp.new Dir.getwd].[m
 [m
[31m-Lines that match an exclusion and an inclusion pattern[m
[31m-will be included.[m
[32m+[m[32mLines that match an exclusion and an inclusion pattern will be included.[m
 [m
 You can modify the list via the getter, or replace it with the setter.[m
 [m
[36m@@ -17910,8 +18180,7 @@[m [mend[m
 [m
 Defaults to [Regexp.new Dir.getwd].[m
 [m
[31m-Lines that match an exclusion and an inclusion pattern[m
[31m-will be included.[m
[32m+[m[32mLines that match an exclusion and an inclusion pattern will be included.[m
 [m
 You can modify the list via the getter, or replace it with the setter.[m
 [m
[36m@@ -18089,21 +18362,18 @@[m [mend[m
       [m
   [m
   [m
[32m+[m[41m    [m
 Defines a beforebefore hook. See Hooks#before for full docs.Defines a before hook. See Hooks#before for full docs.[m
 [m
[31m-This method differs from Hooks#before in only one way: it supports[m
[31m-the :suite:suite scope. Hooks with the :suite:suite scope will be run once before[m
[31m-the first example of the entire suite is executed.This method differs from Hooks#before in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once before[m
[31m-the first example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#before in only one way: it supports the :suite:suite scope. Hooks with the :suite:suite scope will be run once before the first example of the entire suite is executed.This method differs from Hooks#before in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once before the first example of the entire suite is executed.[m
[32m+[m
 [m
   [m
     [m
 Defines a before hook. See Hooks#before for full docs.[m
 [m
[31m-This method differs from Hooks#before in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once before[m
[31m-the first example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#before in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once before the first example of the entire suite is executed.[m
[32m+[m
 [m
   [m
 [m
[36m@@ -18111,9 +18381,7 @@[m [mthe first example of the entire suite is executed.[m
     [m
 Defines a before hook. See Hooks#before for full docs.[m
 [m
[31m-This method differs from Hooks#before in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once before[m
[31m-the first example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#before in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once before the first example of the entire suite is executed.[m
 [m
 [m
   [m
[36m@@ -18346,11 +18614,10 @@[m [mend[m
 [m
   [m
   [m
[32m+[m[41m    [m
 Defines a before hook. See Hooks#before for full docs.[m
 [m
[31m-This method differs from Hooks#before in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once before[m
[31m-the first example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#before in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once before the first example of the entire suite is executed.[m
 [m
 [m
   [m
[36m@@ -18446,11 +18716,10 @@[m [mend[m
       [m
       [m
         —[m
[31m-        an output stream to use, defaults to the current[m
[31m-output_streamoutput_streaman output stream to use, defaults to the current[m
[31m-output_stream[m
[31m-an output stream to use, defaults to the current[m
[31m-output_stream[m
[32m+[m[41m        [m
[32m+[m[32man output stream to use, defaults to the current output_streamoutput_streaman output stream to use, defaults to the current output_stream[m
[32m+[m
[32m+[m[32man output stream to use, defaults to the current output_stream[m
 [m
       [m
     [m
[36m@@ -18465,8 +18734,8 @@[m [moutput_stream[m
       [m
       [m
         —[m
[31m-        an output stream to use, defaults to the current[m
[31m-output_stream[m
[32m+[m[41m        [m
[32m+[m[32man output stream to use, defaults to the current output_stream[m
 [m
       [m
     [m
[36m@@ -18485,8 +18754,8 @@[m [moutput_stream[m
       [m
       [m
         —[m
[31m-        an output stream to use, defaults to the current[m
[31m-output_stream[m
[32m+[m[41m        [m
[32m+[m[32man output stream to use, defaults to the current output_stream[m
 [m
       [m
     [m
[36m@@ -18541,8 +18810,8 @@[m [mReturns:Returns:[m
       [m
       [m
         —[m
[31m-        an output stream to use, defaults to the current[m
[31m-output_stream[m
[32m+[m[41m        [m
[32m+[m[32man output stream to use, defaults to the current output_stream[m
 [m
       [m
     [m
[36m@@ -18713,8 +18983,8 @@[m [mend[m
       [m
       [m
         —[m
[31m-        an output stream to use, defaults to the current[m
[31m-output_stream[m
[32m+[m[41m        [m
[32m+[m[32man output stream to use, defaults to the current output_stream[m
 [m
       [m
     [m
[36m@@ -18769,21 +19039,20 @@[m [mend[m
       [m
   [m
   [m
[31m-    The formatter that will be used if no formatter has been set.[m
[31m-Defaults to 'progress'.The formatter that will be used if no formatter has been set.[m
[31m-Defaults to 'progress'.[m
     [m
[32m+[m[32mThe formatter that will be used if no formatter has been set. Defaults to 'progress'.The formatter that will be used if no formatter has been set. Defaults to 'progress'.[m
 [m
 [m
[31m-    The formatter that will be used if no formatter has been set.[m
[31m-Defaults to 'progress'.[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mThe formatter that will be used if no formatter has been set. Defaults to 'progress'.[m
 [m
 [m
   [m
 [m
   [m
[31m-    The formatter that will be used if no formatter has been set.[m
[31m-Defaults to 'progress'.[m
[32m+[m[41m    [m
[32m+[m[32mThe formatter that will be used if no formatter has been set. Defaults to 'progress'.[m
 [m
 [m
   [m
[36m@@ -18872,8 +19141,8 @@[m [mend[m
 [m
   [m
   [m
[31m-    The formatter that will be used if no formatter has been set.[m
[31m-Defaults to 'progress'.[m
[32m+[m[41m    [m
[32m+[m[32mThe formatter that will be used if no formatter has been set. Defaults to 'progress'.[m
 [m
 [m
   [m
[36m@@ -19153,17 +19429,10 @@[m [mParameters:Parameters:[m
       [m
       [m
         —[m
[31m-        metadata filters that determine[m
[31m-which example or group metadata hashes the callback will be triggered[m
[31m-for. If none are given, the callback will be run against the metadata[m
[31m-hashes of all groups and examples.metadata filters that determine[m
[31m-which example or group metadata hashes the callback will be triggered[m
[31m-for. If none are given, the callback will be run against the metadata[m
[31m-hashes of all groups and examples.[m
[31m-metadata filters that determine[m
[31m-which example or group metadata hashes the callback will be triggered[m
[31m-for. If none are given, the callback will be run against the metadata[m
[31m-hashes of all groups and examples.[m
[32m+[m[41m        [m
[32m+[m[32mmetadata filters that determine which example or group metadata hashes the callback will be triggered for. If none are given, the callback will be run against the metadata hashes of all groups and examples.metadata filters that determine which example or group metadata hashes the callback will be triggered for. If none are given, the callback will be run against the metadata hashes of all groups and examples.[m
[32m+[m
[32m+[m[32mmetadata filters that determine which example or group metadata hashes the callback will be triggered for. If none are given, the callback will be run against the metadata hashes of all groups and examples.[m
 [m
       [m
     [m
[36m@@ -19176,10 +19445,8 @@[m [mhashes of all groups and examples.[m
       [m
       [m
         —[m
[31m-        metadata filters that determine[m
[31m-which example or group metadata hashes the callback will be triggered[m
[31m-for. If none are given, the callback will be run against the metadata[m
[31m-hashes of all groups and examples.[m
[32m+[m[41m        [m
[32m+[m[32mmetadata filters that determine which example or group metadata hashes the callback will be triggered for. If none are given, the callback will be run against the metadata hashes of all groups and examples.[m
 [m
       [m
     [m
[36m@@ -19196,10 +19463,8 @@[m [mhashes of all groups and examples.[m
       [m
       [m
         —[m
[31m-        metadata filters that determine[m
[31m-which example or group metadata hashes the callback will be triggered[m
[31m-for. If none are given, the callback will be run against the metadata[m
[31m-hashes of all groups and examples.[m
[32m+[m[41m        [m
[32m+[m[32mmetadata filters that determine which example or group metadata hashes the callback will be triggered for. If none are given, the callback will be run against the metadata hashes of all groups and examples.[m
 [m
       [m
     [m
[36m@@ -19219,11 +19484,10 @@[m [mYield Parameters:Yield Parameters:[m
       [m
       [m
         —[m
[31m-        original metadata hash from an example or[m
[31m-group. Mutate this in your block as needed.original metadata hash from an example or[m
[31m-group. Mutate this in your block as needed.[m
[31m-original metadata hash from an example or[m
[31m-group. Mutate this in your block as needed.[m
[32m+[m[41m        [m
[32m+[m[32moriginal metadata hash from an example or group. Mutate this in your block as needed.original metadata hash from an example or group. Mutate this in your block as needed.[m
[32m+[m
[32m+[m[32moriginal metadata hash from an example or group. Mutate this in your block as needed.[m
 [m
       [m
     [m
[36m@@ -19236,8 +19500,8 @@[m [mgroup. Mutate this in your block as needed.[m
       [m
       [m
         —[m
[31m-        original metadata hash from an example or[m
[31m-group. Mutate this in your block as needed.[m
[32m+[m[41m        [m
[32m+[m[32moriginal metadata hash from an example or group. Mutate this in your block as needed.[m
 [m
       [m
     [m
[36m@@ -19254,8 +19518,8 @@[m [mgroup. Mutate this in your block as needed.[m
       [m
       [m
         —[m
[31m-        original metadata hash from an example or[m
[31m-group. Mutate this in your block as needed.[m
[32m+[m[41m        [m
[32m+[m[32moriginal metadata hash from an example or group. Mutate this in your block as needed.[m
 [m
       [m
     [m
[36m@@ -19290,10 +19554,8 @@[m [mParameters:[m
       [m
       [m
         —[m
[31m-        metadata filters that determine[m
[31m-which example or group metadata hashes the callback will be triggered[m
[31m-for. If none are given, the callback will be run against the metadata[m
[31m-hashes of all groups and examples.[m
[32m+[m[41m        [m
[32m+[m[32mmetadata filters that determine which example or group metadata hashes the callback will be triggered for. If none are given, the callback will be run against the metadata hashes of all groups and examples.[m
 [m
       [m
     [m
[36m@@ -19313,8 +19575,8 @@[m [mYield Parameters:[m
       [m
       [m
         —[m
[31m-        original metadata hash from an example or[m
[31m-group. Mutate this in your block as needed.[m
[32m+[m[41m        [m
[32m+[m[32moriginal metadata hash from an example or group. Mutate this in your block as needed.[m
 [m
       [m
     [m
[36m@@ -19442,10 +19705,8 @@[m [mParameters:[m
       [m
       [m
         —[m
[31m-        metadata filters that determine[m
[31m-which example or group metadata hashes the callback will be triggered[m
[31m-for. If none are given, the callback will be run against the metadata[m
[31m-hashes of all groups and examples.[m
[32m+[m[41m        [m
[32m+[m[32mmetadata filters that determine which example or group metadata hashes the callback will be triggered for. If none are given, the callback will be run against the metadata hashes of all groups and examples.[m
 [m
       [m
     [m
[36m@@ -19465,8 +19726,8 @@[m [mYield Parameters:[m
       [m
       [m
         —[m
[31m-        original metadata hash from an example or[m
[31m-group. Mutate this in your block as needed.[m
[32m+[m[41m        [m
[32m+[m[32moriginal metadata hash from an example or group. Mutate this in your block as needed.[m
 [m
       [m
     [m
[36m@@ -19499,21 +19760,20 @@[m [mend[m
       [m
   [m
   [m
[31m-    Determines where deprecation warnings are printed.[m
[31m-Defaults to $stderr$stderr.Determines where deprecation warnings are printed.[m
[31m-Defaults to $stderr.[m
     [m
[32m+[m[32mDetermines where deprecation warnings are printed. Defaults to $stderr$stderr.Determines where deprecation warnings are printed. Defaults to $stderr.[m
 [m
 [m
[31m-    Determines where deprecation warnings are printed.[m
[31m-Defaults to $stderr.[m
   [m
     [m
[32m+[m[32mDetermines where deprecation warnings are printed. Defaults to $stderr.[m
[32m+[m
 [m
   [m
 [m
[31m-    Determines where deprecation warnings are printed.[m
[31m-Defaults to $stderr.[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mDetermines where deprecation warnings are printed. Defaults to $stderr.[m
 [m
 [m
   [m
[36m@@ -19643,8 +19908,8 @@[m [mdefine_reader :deprecation_stream[m
 [m
   [m
   [m
[31m-    Determines where deprecation warnings are printed.[m
[31m-Defaults to $stderr.[m
[32m+[m[41m    [m
[32m+[m[32mDetermines where deprecation warnings are printed. Defaults to $stderr.[m
 [m
 [m
   [m
[36m@@ -20007,82 +20283,54 @@[m [mend[m
     [m
   [m
     Note:Note:[m
[31m-    It configures rspec-mocks and rspec-expectations only[m
[31m-if the user is using those (either explicitly or implicitly[m
[31m-by not setting mock_withmock_with or expect_withexpect_with to anything else).It configures rspec-mocks and rspec-expectations only[m
[31m-if the user is using those (either explicitly or implicitly[m
[31m-by not setting mock_with or expect_with to anything else).[m
[31m-It configures rspec-mocks and rspec-expectations only[m
[31m-if the user is using those (either explicitly or implicitly[m
[31m-by not setting mock_with or expect_with to anything else).[m
[32m+[m[41m    [m
[32m+[m[32mIt configures rspec-mocks and rspec-expectations only if the user is using those (either explicitly or implicitly by not setting mock_withmock_with or expect_withexpect_with to anything else).It configures rspec-mocks and rspec-expectations only if the user is using those (either explicitly or implicitly by not setting mock_with or expect_with to anything else).[m
[32m+[m
[32m+[m[32mIt configures rspec-mocks and rspec-expectations only if the user is using those (either explicitly or implicitly by not setting mock_with or expect_with to anything else).[m
 [m
   [m
     Note:[m
[31m-    It configures rspec-mocks and rspec-expectations only[m
[31m-if the user is using those (either explicitly or implicitly[m
[31m-by not setting mock_with or expect_with to anything else).[m
[32m+[m[41m    [m
[32m+[m[32mIt configures rspec-mocks and rspec-expectations only if the user is using those (either explicitly or implicitly by not setting mock_with or expect_with to anything else).[m
 [m
   [m
 [m
   [m
     Note:Note:[m
[31m-    If the user uses this options with mock_with :mochamock_with :mocha[m
[31m-(or similiar) they will still have monkey patching active[m
[31m-in their test environment from mocha.If the user uses this options with mock_with :mocha[m
[31m-(or similiar) they will still have monkey patching active[m
[31m-in their test environment from mocha.[m
[31m-If the user uses this options with mock_with :mocha[m
[31m-(or similiar) they will still have monkey patching active[m
[31m-in their test environment from mocha.[m
[32m+[m[41m    [m
[32m+[m[32mIf the user uses this options with mock_with :mochamock_with :mocha (or similiar) they will still have monkey patching active in their test environment from mocha.If the user uses this options with mock_with :mocha (or similiar) they will still have monkey patching active in their test environment from mocha.[m
[32m+[m
[32m+[m[32mIf the user uses this options with mock_with :mocha (or similiar) they will still have monkey patching active in their test environment from mocha.[m
 [m
   [m
     Note:[m
[31m-    If the user uses this options with mock_with :mocha[m
[31m-(or similiar) they will still have monkey patching active[m
[31m-in their test environment from mocha.[m
[32m+[m[41m    [m
[32m+[m[32mIf the user uses this options with mock_with :mocha (or similiar) they will still have monkey patching active in their test environment from mocha.[m
 [m
   [m
 [m
[31m-Enables zero monkey patching mode for RSpec. It removes monkey[m
[31m-patching of the top-level DSL methods (describedescribe,[m
[31m-shared_examples_forshared_examples_for, etc) onto mainmain and ModuleModule, instead[m
[31m-requiring you to prefix these methods with RSpec.RSpec.. It enables[m
[31m-expect-only syntax for rspec-mocks and rspec-expectations. It[m
[31m-simply disables monkey patching on whatever pieces of RSpec[m
[31m-the user is using.Enables zero monkey patching mode for RSpec. It removes monkey[m
[31m-patching of the top-level DSL methods (describe,[m
[31m-shared_examples_for, etc) onto main and Module, instead[m
[31m-requiring you to prefix these methods with RSpec.. It enables[m
[31m-expect-only syntax for rspec-mocks and rspec-expectations. It[m
[31m-simply disables monkey patching on whatever pieces of RSpec[m
[31m-the user is using.[m
[32m+[m
[32m+[m[32mEnables zero monkey patching mode for RSpec. It removes monkey patching of the top-level DSL methods (describedescribe, shared_examples_forshared_examples_for, etc) onto mainmain and ModuleModule, instead requiring you to prefix these methods with RSpec.RSpec.. It enables expect-only syntax for rspec-mocks and rspec-expectations. It simply disables monkey patching on whatever pieces of RSpec the user is using.Enables zero monkey patching mode for RSpec. It removes monkey patching of the top-level DSL methods (describe, shared_examples_for, etc) onto main and Module, instead requiring you to prefix these methods with RSpec.. It enables expect-only syntax for rspec-mocks and rspec-expectations. It simply disables monkey patching on whatever pieces of RSpec the user is using.[m
 [m
 [m
   [m
     [m
   [m
     Note:[m
[31m-    It configures rspec-mocks and rspec-expectations only[m
[31m-if the user is using those (either explicitly or implicitly[m
[31m-by not setting mock_with or expect_with to anything else).[m
[32m+[m[41m    [m
[32m+[m[32mIt configures rspec-mocks and rspec-expectations only if the user is using those (either explicitly or implicitly by not setting mock_with or expect_with to anything else).[m
 [m
   [m
 [m
   [m
     Note:[m
[31m-    If the user uses this options with mock_with :mocha[m
[31m-(or similiar) they will still have monkey patching active[m
[31m-in their test environment from mocha.[m
[32m+[m[41m    [m
[32m+[m[32mIf the user uses this options with mock_with :mocha (or similiar) they will still have monkey patching active in their test environment from mocha.[m
[32m+[m
   [m
 [m
 [m
[31m-Enables zero monkey patching mode for RSpec. It removes monkey[m
[31m-patching of the top-level DSL methods (describe,[m
[31m-shared_examples_for, etc) onto main and Module, instead[m
[31m-requiring you to prefix these methods with RSpec.. It enables[m
[31m-expect-only syntax for rspec-mocks and rspec-expectations. It[m
[31m-simply disables monkey patching on whatever pieces of RSpec[m
[31m-the user is using.[m
[32m+[m[32mEnables zero monkey patching mode for RSpec. It removes monkey patching of the top-level DSL methods (describe, shared_examples_for, etc) onto main and Module, instead requiring you to prefix these methods with RSpec.. It enables expect-only syntax for rspec-mocks and rspec-expectations. It simply disables monkey patching on whatever pieces of RSpec the user is using.[m
 [m
 [m
   [m
[36m@@ -20091,27 +20339,20 @@[m [mthe user is using.[m
     [m
   [m
     Note:[m
[31m-    It configures rspec-mocks and rspec-expectations only[m
[31m-if the user is using those (either explicitly or implicitly[m
[31m-by not setting mock_with or expect_with to anything else).[m
[32m+[m[41m    [m
[32m+[m[32mIt configures rspec-mocks and rspec-expectations only if the user is using those (either explicitly or implicitly by not setting mock_with or expect_with to anything else).[m
 [m
   [m
 [m
   [m
     Note:[m
[31m-    If the user uses this options with mock_with :mocha[m
[31m-(or similiar) they will still have monkey patching active[m
[31m-in their test environment from mocha.[m
[32m+[m[41m    [m
[32m+[m[32mIf the user uses this options with mock_with :mocha (or similiar) they will still have monkey patching active in their test environment from mocha.[m
[32m+[m
   [m
 [m
 [m
[31m-Enables zero monkey patching mode for RSpec. It removes monkey[m
[31m-patching of the top-level DSL methods (describe,[m
[31m-shared_examples_for, etc) onto main and Module, instead[m
[31m-requiring you to prefix these methods with RSpec.. It enables[m
[31m-expect-only syntax for rspec-mocks and rspec-expectations. It[m
[31m-simply disables monkey patching on whatever pieces of RSpec[m
[31m-the user is using.[m
[32m+[m[32mEnables zero monkey patching mode for RSpec. It removes monkey patching of the top-level DSL methods (describe, shared_examples_for, etc) onto main and Module, instead requiring you to prefix these methods with RSpec.. It enables expect-only syntax for rspec-mocks and rspec-expectations. It simply disables monkey patching on whatever pieces of RSpec the user is using.[m
 [m
 [m
   [m
[36m@@ -20347,27 +20588,20 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    It configures rspec-mocks and rspec-expectations only[m
[31m-if the user is using those (either explicitly or implicitly[m
[31m-by not setting mock_with or expect_with to anything else).[m
[32m+[m[41m    [m
[32m+[m[32mIt configures rspec-mocks and rspec-expectations only if the user is using those (either explicitly or implicitly by not setting mock_with or expect_with to anything else).[m
 [m
   [m
 [m
   [m
     Note:[m
[31m-    If the user uses this options with mock_with :mocha[m
[31m-(or similiar) they will still have monkey patching active[m
[31m-in their test environment from mocha.[m
[32m+[m[41m    [m
[32m+[m[32mIf the user uses this options with mock_with :mocha (or similiar) they will still have monkey patching active in their test environment from mocha.[m
 [m
   [m
 [m
[31m-Enables zero monkey patching mode for RSpec. It removes monkey[m
[31m-patching of the top-level DSL methods (describe,[m
[31m-shared_examples_for, etc) onto main and Module, instead[m
[31m-requiring you to prefix these methods with RSpec.. It enables[m
[31m-expect-only syntax for rspec-mocks and rspec-expectations. It[m
[31m-simply disables monkey patching on whatever pieces of RSpec[m
[31m-the user is using.[m
[32m+[m
[32m+[m[32mEnables zero monkey patching mode for RSpec. It removes monkey patching of the top-level DSL methods (describe, shared_examples_for, etc) onto main and Module, instead requiring you to prefix these methods with RSpec.. It enables expect-only syntax for rspec-mocks and rspec-expectations. It simply disables monkey patching on whatever pieces of RSpec the user is using.[m
 [m
 [m
   [m
[36m@@ -20431,21 +20665,20 @@[m [mend[m
       [m
   [m
   [m
[31m-    Returns the exclusion_filterexclusion_filter. If none has been set, returns an empty[m
[31m-hash.Returns the exclusion_filter. If none has been set, returns an empty[m
[31m-hash.[m
[32m+[m[41m    [m
[32m+[m[32mReturns the exclusion_filterexclusion_filter. If none has been set, returns an empty hash.Returns the exclusion_filter. If none has been set, returns an empty hash.[m
 [m
 [m
   [m
[31m-    Returns the exclusion_filter. If none has been set, returns an empty[m
[31m-hash.[m
     [m
[32m+[m[32mReturns the exclusion_filter. If none has been set, returns an empty hash.[m
 [m
 [m
   [m
 [m
[31m-    Returns the exclusion_filter. If none has been set, returns an empty[m
[31m-hash.[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mReturns the exclusion_filter. If none has been set, returns an empty hash.[m
 [m
 [m
   [m
[36m@@ -20534,8 +20767,8 @@[m [mend[m
 [m
   [m
   [m
[31m-    Returns the exclusion_filter. If none has been set, returns an empty[m
[31m-hash.[m
[32m+[m[41m    [m
[32m+[m[32mReturns the exclusion_filter. If none has been set, returns an empty hash.[m
 [m
 [m
   [m
[36m@@ -20567,37 +20800,32 @@[m [mend[m
       [m
   [m
   [m
[31m-    Clears and reassigns the exclusion_filterexclusion_filter. Set to nilnil if you don't[m
[31m-want any exclusion filter at all.Clears and reassigns the exclusion_filter. Set to nil if you don't[m
[31m-want any exclusion filter at all.[m
[32m+[m[41m    [m
[32m+[m[32mClears and reassigns the exclusion_filterexclusion_filter. Set to nilnil if you don't want any exclusion filter at all.Clears and reassigns the exclusion_filter. Set to nil if you don't want any exclusion filter at all.[m
 [m
 WarningWarning[m
 [m
[31m-This overrides any exclusion filters/tags set on the command line or in[m
[31m-configuration files.This overrides any exclusion filters/tags set on the command line or in[m
[31m-configuration files.[m
[32m+[m[32mThis overrides any exclusion filters/tags set on the command line or in configuration files.This overrides any exclusion filters/tags set on the command line or in configuration files.[m
[32m+[m
 [m
   [m
     [m
[31m-    Clears and reassigns the exclusion_filter. Set to nil if you don't[m
[31m-want any exclusion filter at all.[m
[32m+[m[32mClears and reassigns the exclusion_filter. Set to nil if you don't want any exclusion filter at all.[m
 [m
 Warning[m
 [m
[31m-This overrides any exclusion filters/tags set on the command line or in[m
[31m-configuration files.[m
[32m+[m[32mThis overrides any exclusion filters/tags set on the command line or in configuration files.[m
 [m
 [m
   [m
 [m
   [m
[31m-    Clears and reassigns the exclusion_filter. Set to nil if you don't[m
[31m-want any exclusion filter at all.[m
[32m+[m[41m    [m
[32m+[m[32mClears and reassigns the exclusion_filter. Set to nil if you don't want any exclusion filter at all.[m
 [m
 Warning[m
 [m
[31m-This overrides any exclusion filters/tags set on the command line or in[m
[31m-configuration files.[m
[32m+[m[32mThis overrides any exclusion filters/tags set on the command line or in configuration files.[m
 [m
 [m
   [m
[36m@@ -20696,13 +20924,12 @@[m [mend[m
 [m
   [m
   [m
[31m-    Clears and reassigns the exclusion_filter. Set to nil if you don't[m
[31m-want any exclusion filter at all.[m
[32m+[m[41m    [m
[32m+[m[32mClears and reassigns the exclusion_filter. Set to nil if you don't want any exclusion filter at all.[m
 [m
 Warning[m
 [m
[31m-This overrides any exclusion filters/tags set on the command line or in[m
[31m-configuration files.[m
[32m+[m[32mThis overrides any exclusion filters/tags set on the command line or in configuration files.[m
 [m
 [m
   [m
[36m@@ -20736,13 +20963,10 @@[m [mend[m
       [m
   [m
   [m
[31m-    Sets the expectation framework module(s) to be included in each example[m
[31m-group.Sets the expectation framework module(s) to be included in each example[m
[31m-group.[m
     [m
[31m-frameworks can be :rspec, :test_unit, :minitest, a custom[m
[31m-module, or any combination thereof:.frameworks can be :rspec, :test_unit, :minitest, a custom[m
[31m-module, or any combination thereof:.[m
[32m+[m[32mSets the expectation framework module(s) to be included in each example group.Sets the expectation framework module(s) to be included in each example group.[m
[32m+[m
[32m+[m[32mframeworks can be :rspec, :test_unit, :minitest, a custom module, or any combination thereof:.frameworks can be :rspec, :test_unit, :minitest, a custom module, or any combination thereof:.[m
 [m
 configconfig..expect_withexpect_with :rspec:rspec[m
 configconfig..expect_withexpect_with :test_unit:test_unit[m
[36m@@ -20761,15 +20985,11 @@[m [mconfig.expect_with :rspec, :minitest[m
 config.expect_with OtherExpectationFramework[m
 [m
 [m
[31m-RSpec will translate :rspec:rspec, :minitest:minitest, and :test_unit:test_unit into the[m
[31m-appropriate modules.RSpec will translate :rspec, :minitest, and :test_unit into the[m
[31m-appropriate modules.[m
[32m+[m[32mRSpec will translate :rspec:rspec, :minitest:minitest, and :test_unit:test_unit into the appropriate modules.RSpec will translate :rspec, :minitest, and :test_unit into the appropriate modules.[m
 [m
 ConfigurationConfiguration[m
 [m
[31m-If the module responds to configuration, expect_with will[m
[31m-yield the configuration object if given a block:.If the module responds to configuration, expect_with will[m
[31m-yield the configuration object if given a block:.[m
[32m+[m[32mIf the module responds to configuration, expect_with will yield the configuration object if given a block:.If the module responds to configuration, expect_with will yield the configuration object if given a block:.[m
 [m
 configconfig..expect_withexpect_with OtherExpectationFrameworkOtherExpectationFramework dodo ||custom_configcustom_config||[m
   custom_configcustom_config..custom_settingcustom_setting == truetrue[m
[36m@@ -20784,11 +21004,10 @@[m [mend[m
 [m
 [m
   [m
[31m-    Sets the expectation framework module(s) to be included in each example[m
[31m-group.[m
     [m
[31m-frameworks can be :rspec, :test_unit, :minitest, a custom[m
[31m-module, or any combination thereof:.[m
[32m+[m[32mSets the expectation framework module(s) to be included in each example group.[m
[32m+[m
[32m+[m[32mframeworks can be :rspec, :test_unit, :minitest, a custom module, or any combination thereof:.[m
 [m
 config.expect_with :rspec[m
 config.expect_with :test_unit[m
[36m@@ -20797,13 +21016,11 @@[m [mconfig.expect_with :rspec, :minitest[m
 config.expect_with OtherExpectationFramework[m
 [m
 [m
[31m-RSpec will translate :rspec, :minitest, and :test_unit into the[m
[31m-appropriate modules.[m
[32m+[m[32mRSpec will translate :rspec, :minitest, and :test_unit into the appropriate modules.[m
 [m
 Configuration[m
 [m
[31m-If the module responds to configuration, expect_with will[m
[31m-yield the configuration object if given a block:.[m
[32m+[m[32mIf the module responds to configuration, expect_with will yield the configuration object if given a block:.[m
 [m
 config.expect_with OtherExpectationFramework do |custom_config|[m
   custom_config.custom_setting = true[m
[36m@@ -20814,11 +21031,10 @@[m [mend[m
   [m
 [m
   [m
[31m-    Sets the expectation framework module(s) to be included in each example[m
[31m-group.[m
     [m
[31m-frameworks can be :rspec, :test_unit, :minitest, a custom[m
[31m-module, or any combination thereof:.[m
[32m+[m[32mSets the expectation framework module(s) to be included in each example group.[m
[32m+[m
[32m+[m[32mframeworks can be :rspec, :test_unit, :minitest, a custom module, or any combination thereof:.[m
 [m
 config.expect_with :rspec[m
 config.expect_with :test_unit[m
[36m@@ -20827,13 +21043,11 @@[m [mconfig.expect_with :rspec, :minitest[m
 config.expect_with OtherExpectationFramework[m
 [m
 [m
[31m-RSpec will translate :rspec, :minitest, and :test_unit into the[m
[31m-appropriate modules.[m
[32m+[m[32mRSpec will translate :rspec, :minitest, and :test_unit into the appropriate modules.[m
 [m
 Configuration[m
 [m
[31m-If the module responds to configuration, expect_with will[m
[31m-yield the configuration object if given a block:.[m
[32m+[m[32mIf the module responds to configuration, expect_with will yield the configuration object if given a block:.[m
 [m
 config.expect_with OtherExpectationFramework do |custom_config|[m
   custom_config.custom_setting = true[m
[36m@@ -21309,11 +21523,10 @@[m [mend[m
 [m
   [m
   [m
[31m-    Sets the expectation framework module(s) to be included in each example[m
[31m-group.[m
     [m
[31m-frameworks can be :rspec, :test_unit, :minitest, a custom[m
[31m-module, or any combination thereof:.[m
[32m+[m[32mSets the expectation framework module(s) to be included in each example group.[m
[32m+[m
[32m+[m[32mframeworks can be :rspec, :test_unit, :minitest, a custom module, or any combination thereof:.[m
 [m
 config.expect_with :rspec[m
 config.expect_with :test_unit[m
[36m@@ -21322,13 +21535,11 @@[m [mconfig.expect_with :rspec, :minitest[m
 config.expect_with OtherExpectationFramework[m
 [m
 [m
[31m-RSpec will translate :rspec, :minitest, and :test_unit into the[m
[31m-appropriate modules.[m
[32m+[m[32mRSpec will translate :rspec, :minitest, and :test_unit into the appropriate modules.[m
 [m
 Configuration[m
 [m
[31m-If the module responds to configuration, expect_with will[m
[31m-yield the configuration object if given a block:.[m
[32m+[m[32mIf the module responds to configuration, expect_with will yield the configuration object if given a block:.[m
 [m
 config.expect_with OtherExpectationFramework do |custom_config|[m
   custom_config.custom_setting = true[m
[36m@@ -21787,41 +22006,20 @@[m [mend[m
       [m
   [m
   [m
[31m-    Exposes the current running example via the named[m
[31m-helper method. RSpec 2.x exposed this via exampleexample,[m
[31m-but in RSpec 3.0, the example is instead exposed via[m
[31m-an arg yielded to itit, beforebefore, letlet, etc. However,[m
[31m-some extension gems (such as Capybara) depend on the[m
[31m-RSpec 2.x's exampleexample method, so this config option[m
[31m-can be used to maintain compatibility.Exposes the current running example via the named[m
[31m-helper method. RSpec 2.x exposed this via example,[m
[31m-but in RSpec 3.0, the example is instead exposed via[m
[31m-an arg yielded to it, before, let, etc. However,[m
[31m-some extension gems (such as Capybara) depend on the[m
[31m-RSpec 2.x's example method, so this config option[m
[31m-can be used to maintain compatibility.[m
     [m
[32m+[m[32mExposes the current running example via the named helper method. RSpec 2.x exposed this via exampleexample, but in RSpec 3.0, the example is instead exposed via an arg yielded to itit, beforebefore, letlet, etc. However, some extension gems (such as Capybara) depend on the RSpec 2.x's exampleexample method, so this config option can be used to maintain compatibility.Exposes the current running example via the named helper method. RSpec 2.x exposed this via example, but in RSpec 3.0, the example is instead exposed via an arg yielded to it, before, let, etc. However, some extension gems (such as Capybara) depend on the RSpec 2.x's example method, so this config option can be used to maintain compatibility.[m
 [m
 [m
[31m-    Exposes the current running example via the named[m
[31m-helper method. RSpec 2.x exposed this via example,[m
[31m-but in RSpec 3.0, the example is instead exposed via[m
[31m-an arg yielded to it, before, let, etc. However,[m
[31m-some extension gems (such as Capybara) depend on the[m
[31m-RSpec 2.x's example method, so this config option[m
[31m-can be used to maintain compatibility.[m
   [m
     [m
[32m+[m[32mExposes the current running example via the named helper method. RSpec 2.x exposed this via example, but in RSpec 3.0, the example is instead exposed via an arg yielded to it, before, let, etc. However, some extension gems (such as Capybara) depend on the RSpec 2.x's example method, so this config option can be used to maintain compatibility.[m
 [m
 [m
   [m
[31m-    Exposes the current running example via the named[m
[31m-helper method. RSpec 2.x exposed this via example,[m
[31m-but in RSpec 3.0, the example is instead exposed via[m
[31m-an arg yielded to it, before, let, etc. However,[m
[31m-some extension gems (such as Capybara) depend on the[m
[31m-RSpec 2.x's example method, so this config option[m
[31m-can be used to maintain compatibility.[m
[32m+[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mExposes the current running example via the named helper method. RSpec 2.x exposed this via example, but in RSpec 3.0, the example is instead exposed via an arg yielded to it, before, let, etc. However, some extension gems (such as Capybara) depend on the RSpec 2.x's example method, so this config option can be used to maintain compatibility.[m
 [m
 [m
   [m
[36m@@ -22102,13 +22305,8 @@[m [mend[m
 [m
   [m
   [m
[31m-    Exposes the current running example via the named[m
[31m-helper method. RSpec 2.x exposed this via example,[m
[31m-but in RSpec 3.0, the example is instead exposed via[m
[31m-an arg yielded to it, before, let, etc. However,[m
[31m-some extension gems (such as Capybara) depend on the[m
[31m-RSpec 2.x's example method, so this config option[m
[31m-can be used to maintain compatibility.[m
[32m+[m[41m    [m
[32m+[m[32mExposes the current running example via the named helper method. RSpec 2.x exposed this via example, but in RSpec 3.0, the example is instead exposed via an arg yielded to it, before, let, etc. However, some extension gems (such as Capybara) depend on the RSpec 2.x's example method, so this config option can be used to maintain compatibility.[m
 [m
 [m
   [m
[36m@@ -22187,29 +22386,20 @@[m [mend[m
       [m
   [m
   [m
[31m-    Use this to expose the core RSpec DSL via Module and the main[m
[31m-object. It will be set automatically but you can override it to[m
[31m-remove the DSL.[m
[31m-Default: true.Use this to expose the core RSpec DSL via Module and the main[m
[31m-object. It will be set automatically but you can override it to[m
[31m-remove the DSL.[m
[31m-Default: true.[m
[32m+[m[41m    [m
[32m+[m[32mUse this to expose the core RSpec DSL via Module and the main object. It will be set automatically but you can override it to remove the DSL. Default: true.Use this to expose the core RSpec DSL via Module and the main object. It will be set automatically but you can override it to remove the DSL. Default: true.[m
[32m+[m
[32m+[m
   [m
     [m
[32m+[m[32mUse this to expose the core RSpec DSL via Module and the main object. It will be set automatically but you can override it to remove the DSL. Default: true.[m
 [m
[31m-    Use this to expose the core RSpec DSL via Module and the main[m
[31m-object. It will be set automatically but you can override it to[m
[31m-remove the DSL.[m
[31m-Default: true.[m
 [m
   [m
 [m
   [m
     [m
[31m-    Use this to expose the core RSpec DSL via Module and the main[m
[31m-object. It will be set automatically but you can override it to[m
[31m-remove the DSL.[m
[31m-Default: true.[m
[32m+[m[32mUse this to expose the core RSpec DSL via Module and the main object. It will be set automatically but you can override it to remove the DSL. Default: true.[m
 [m
 [m
   [m
[36m@@ -22358,10 +22548,8 @@[m [mend[m
 [m
   [m
   [m
[31m-    Use this to expose the core RSpec DSL via Module and the main[m
[31m-object. It will be set automatically but you can override it to[m
[31m-remove the DSL.[m
[31m-Default: true.[m
[32m+[m[41m    [m
[32m+[m[32mUse this to expose the core RSpec DSL via Module and the main object. It will be set automatically but you can override it to remove the DSL. Default: true.[m
 [m
 [m
   [m
[36m@@ -22405,21 +22593,20 @@[m [mend[m
       [m
   [m
   [m
[31m-    Indicates if the DSL has been exposed off of modules and main.[m
[31m-Default: true.Indicates if the DSL has been exposed off of modules and main.[m
[31m-Default: true.[m
     [m
[32m+[m[32mIndicates if the DSL has been exposed off of modules and main. Default: true.Indicates if the DSL has been exposed off of modules and main. Default: true.[m
 [m
 [m
[31m-    Indicates if the DSL has been exposed off of modules and main.[m
[31m-Default: true.[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mIndicates if the DSL has been exposed off of modules and main. Default: true.[m
 [m
 [m
   [m
 [m
   [m
[31m-    Indicates if the DSL has been exposed off of modules and main.[m
[31m-Default: true.[m
[32m+[m[41m    [m
[32m+[m[32mIndicates if the DSL has been exposed off of modules and main. Default: true.[m
 [m
 [m
   [m
[36m@@ -22552,8 +22739,8 @@[m [mend[m
 [m
   [m
   [m
[31m-    Indicates if the DSL has been exposed off of modules and main.[m
[31m-Default: true.[m
[32m+[m[41m    [m
[32m+[m[32mIndicates if the DSL has been exposed off of modules and main. Default: true.[m
 [m
 [m
   [m
[36m@@ -22598,39 +22785,26 @@[m [mend[m
       [m
   [m
   [m
[31m-    Tells RSpec to extend example groups with modmod. Methods defined in[m
[31m-modmod are exposed to example groups (not examples). Use filtersfilters to[m
[31m-constrain the groups to extend.Tells RSpec to extend example groups with mod. Methods defined in[m
[31m-mod are exposed to example groups (not examples). Use filters to[m
[31m-constrain the groups to extend.[m
     [m
[31m-Similar to includeinclude, but behavior is added to example groups, which[m
[31m-are classes, rather than the examples, which are instances of those[m
[31m-classes.Similar to include, but behavior is added to example groups, which[m
[31m-are classes, rather than the examples, which are instances of those[m
[31m-classes.[m
[32m+[m[32mTells RSpec to extend example groups with modmod. Methods defined in modmod are exposed to example groups (not examples). Use filtersfilters to constrain the groups to extend.Tells RSpec to extend example groups with mod. Methods defined in mod are exposed to example groups (not examples). Use filters to constrain the groups to extend.[m
[32m+[m
[32m+[m[32mSimilar to includeinclude, but behavior is added to example groups, which are classes, rather than the examples, which are instances of those classes.Similar to include, but behavior is added to example groups, which are classes, rather than the examples, which are instances of those classes.[m
 [m
 [m
   [m
[31m-    Tells RSpec to extend example groups with mod. Methods defined in[m
[31m-mod are exposed to example groups (not examples). Use filters to[m
[31m-constrain the groups to extend.[m
     [m
[31m-Similar to include, but behavior is added to example groups, which[m
[31m-are classes, rather than the examples, which are instances of those[m
[31m-classes.[m
[32m+[m[32mTells RSpec to extend example groups with mod. Methods defined in mod are exposed to example groups (not examples). Use filters to constrain the groups to extend.[m
 [m
[32m+[m[32mSimilar to include, but behavior is added to example groups, which are classes, rather than the examples, which are instances of those classes.[m
 [m
 [m
   [m
 [m
[31m-    Tells RSpec to extend example groups with mod. Methods defined in[m
[31m-mod are exposed to example groups (not examples). Use filters to[m
[31m-constrain the groups to extend.[m
   [m
[31m-Similar to include, but behavior is added to example groups, which[m
[31m-are classes, rather than the examples, which are instances of those[m
[31m-classes.[m
[32m+[m[41m    [m
[32m+[m[32mTells RSpec to extend example groups with mod. Methods defined in mod are exposed to example groups (not examples). Use filters to constrain the groups to extend.[m
[32m+[m
[32m+[m[32mSimilar to include, but behavior is added to example groups, which are classes, rather than the examples, which are instances of those classes.[m
 [m
 [m
   [m
[36m@@ -22643,9 +22817,9 @@[m [mclasses.[m
       [m
       [m
 modulemodule UiHelpersUiHelpers[m
[31m-  defdef run_in_browserrun_in_browser[m
[31m-    # ...[m
[32m+[m[32m  defdef run_in_browserrun_in_browser    # ...[m
 # ...[m
[32m+[m
   endend[m
 endend[m
 [m
[36m@@ -22662,8 +22836,8 @@[m [mdescribedescribe ""edit profileedit profile"""edit profile",, :type:type =>=> :r[m
   endend[m
 endend[m
 module UiHelpers[m
[31m-  def run_in_browser[m
[31m-    # ...[m
[32m+[m[32m  def run_in_browser    # ...[m
[32m+[m
   end[m
 end[m
 [m
[36m@@ -22679,8 +22853,8 @@[m [mdescribe "edit profile", :type => :request do[m
   end[m
 end[m
 module UiHelpers[m
[31m-  def run_in_browser[m
[31m-    # ...[m
[32m+[m[32m  def run_in_browser    # ...[m
[32m+[m
   end[m
 end[m
 [m
[36m@@ -22702,8 +22876,8 @@[m [mend[m
       [m
       [m
 module UiHelpers[m
[31m-  def run_in_browser[m
[31m-    # ...[m
[32m+[m[32m  def run_in_browser    # ...[m
[32m+[m
   end[m
 end[m
 [m
[36m@@ -22745,8 +22919,8 @@[m [mend[m
       [m
       [m
 module UiHelpers[m
[31m-  def run_in_browser[m
[31m-    # ...[m
[32m+[m[32m  def run_in_browser    # ...[m
[32m+[m
   end[m
 end[m
 [m
[36m@@ -22872,13 +23046,10 @@[m [mend[m
 [m
   [m
   [m
[31m-    Tells RSpec to extend example groups with mod. Methods defined in[m
[31m-mod are exposed to example groups (not examples). Use filters to[m
[31m-constrain the groups to extend.[m
     [m
[31m-Similar to include, but behavior is added to example groups, which[m
[31m-are classes, rather than the examples, which are instances of those[m
[31m-classes.[m
[32m+[m[32mTells RSpec to extend example groups with mod. Methods defined in mod are exposed to example groups (not examples). Use filters to constrain the groups to extend.[m
[32m+[m
[32m+[m[32mSimilar to include, but behavior is added to example groups, which are classes, rather than the examples, which are instances of those classes.[m
 [m
 [m
   [m
[36m@@ -22891,8 +23062,8 @@[m [mclasses.[m
       [m
       [m
 module UiHelpers[m
[31m-  def run_in_browser[m
[31m-    # ...[m
[32m+[m[32m  def run_in_browser    # ...[m
[32m+[m
   end[m
 end[m
 [m
[36m@@ -22951,55 +23122,33 @@[m [mend[m
     [m
   [m
     Note:Note:[m
[31m-    The patterns this adds will match the named gems in their common[m
[31m-locations (e.g. system gems, vendored with bundler, installed as a[m
[31m-:git dependency with bundler, etc) but is not guaranteed to work for[m
[31m-all possible gem locations. For example, if you have the gem source[m
[31m-in a directory with a completely unrelated name, and use bundler's[m
[31m-:path option, this will not filter it.The patterns this adds will match the named gems in their common[m
[31m-locations (e.g. system gems, vendored with bundler, installed as a[m
[31m-:git dependency with bundler, etc) but is not guaranteed to work for[m
[31m-all possible gem locations. For example, if you have the gem source[m
[31m-in a directory with a completely unrelated name, and use bundler's[m
[31m-:path option, this will not filter it.[m
[31m-The patterns this adds will match the named gems in their common[m
[31m-locations (e.g. system gems, vendored with bundler, installed as a[m
[31m-:git dependency with bundler, etc) but is not guaranteed to work for[m
[31m-all possible gem locations. For example, if you have the gem source[m
[31m-in a directory with a completely unrelated name, and use bundler's[m
[31m-:path option, this will not filter it.[m
[32m+[m[41m    [m
[32m+[m[32mThe patterns this adds will match the named gems in their common locations (e.g. system gems, vendored with bundler, installed as a :git dependency with bundler, etc) but is not guaranteed to work for all possible gem locations. For example, if you have the gem source in a directory with a completely unrelated name, and use bundler's :path option, this will not filter it.The patterns this adds will match the named gems in their common locations (e.g. system gems, vendored with bundler, installed as a :git dependency with bundler, etc) but is not guaranteed to work for all possible gem locations. For example, if you have the gem source in a directory with a completely unrelated name, and use bundler's :path option, this will not filter it.[m
[32m+[m
[32m+[m[32mThe patterns this adds will match the named gems in their common locations (e.g. system gems, vendored with bundler, installed as a :git dependency with bundler, etc) but is not guaranteed to work for all possible gem locations. For example, if you have the gem source in a directory with a completely unrelated name, and use bundler's :path option, this will not filter it.[m
 [m
   [m
     Note:[m
[31m-    The patterns this adds will match the named gems in their common[m
[31m-locations (e.g. system gems, vendored with bundler, installed as a[m
[31m-:git dependency with bundler, etc) but is not guaranteed to work for[m
[31m-all possible gem locations. For example, if you have the gem source[m
[31m-in a directory with a completely unrelated name, and use bundler's[m
[31m-:path option, this will not filter it.[m
     [m
[32m+[m[32mThe patterns this adds will match the named gems in their common locations (e.g. system gems, vendored with bundler, installed as a :git dependency with bundler, etc) but is not guaranteed to work for all possible gem locations. For example, if you have the gem source in a directory with a completely unrelated name, and use bundler's :path option, this will not filter it.[m
 [m
   [m
[31m-Adds #backtrace_exclusion_patterns that will filter lines from[m
[31m-the named gems from backtraces.Adds #backtrace_exclusion_patterns that will filter lines from[m
[31m-the named gems from backtraces.[m
[32m+[m
[32m+[m
[32m+[m[32mAdds #backtrace_exclusion_patterns that will filter lines from the named gems from backtraces.Adds #backtrace_exclusion_patterns that will filter lines from the named gems from backtraces.[m
 [m
 [m
   [m
     [m
   [m
     Note:[m
[31m-    The patterns this adds will match the named gems in their common[m
[31m-locations (e.g. system gems, vendored with bundler, installed as a[m
[31m-:git dependency with bundler, etc) but is not guaranteed to work for[m
[31m-all possible gem locations. For example, if you have the gem source[m
[31m-in a directory with a completely unrelated name, and use bundler's[m
[31m-:path option, this will not filter it.[m
     [m
[32m+[m[32mThe patterns this adds will match the named gems in their common locations (e.g. system gems, vendored with bundler, installed as a :git dependency with bundler, etc) but is not guaranteed to work for all possible gem locations. For example, if you have the gem source in a directory with a completely unrelated name, and use bundler's :path option, this will not filter it.[m
 [m
   [m
[31m-Adds #backtrace_exclusion_patterns that will filter lines from[m
[31m-the named gems from backtraces.[m
[32m+[m
[32m+[m
[32m+[m[32mAdds #backtrace_exclusion_patterns that will filter lines from the named gems from backtraces.[m
 [m
 [m
   [m
[36m@@ -23008,17 +23157,13 @@[m [mthe named gems from backtraces.[m
     [m
   [m
     Note:[m
[31m-    The patterns this adds will match the named gems in their common[m
[31m-locations (e.g. system gems, vendored with bundler, installed as a[m
[31m-:git dependency with bundler, etc) but is not guaranteed to work for[m
[31m-all possible gem locations. For example, if you have the gem source[m
[31m-in a directory with a completely unrelated name, and use bundler's[m
[31m-:path option, this will not filter it.[m
     [m
[32m+[m[32mThe patterns this adds will match the named gems in their common locations (e.g. system gems, vendored with bundler, installed as a :git dependency with bundler, etc) but is not guaranteed to work for all possible gem locations. For example, if you have the gem source in a directory with a completely unrelated name, and use bundler's :path option, this will not filter it.[m
 [m
   [m
[31m-Adds #backtrace_exclusion_patterns that will filter lines from[m
[31m-the named gems from backtraces.[m
[32m+[m
[32m+[m
[32m+[m[32mAdds #backtrace_exclusion_patterns that will filter lines from the named gems from backtraces.[m
 [m
 [m
   [m
[36m@@ -23231,17 +23381,13 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    The patterns this adds will match the named gems in their common[m
[31m-locations (e.g. system gems, vendored with bundler, installed as a[m
[31m-:git dependency with bundler, etc) but is not guaranteed to work for[m
[31m-all possible gem locations. For example, if you have the gem source[m
[31m-in a directory with a completely unrelated name, and use bundler's[m
[31m-:path option, this will not filter it.[m
[32m+[m[41m    [m
[32m+[m[32mThe patterns this adds will match the named gems in their common locations (e.g. system gems, vendored with bundler, installed as a :git dependency with bundler, etc) but is not guaranteed to work for all possible gem locations. For example, if you have the gem source in a directory with a completely unrelated name, and use bundler's :path option, this will not filter it.[m
[32m+[m
   [m
 [m
 [m
[31m-Adds #backtrace_exclusion_patterns that will filter lines from[m
[31m-the named gems from backtraces.[m
[32m+[m[32mAdds #backtrace_exclusion_patterns that will filter lines from the named gems from backtraces.[m
 [m
 [m
   [m
[36m@@ -23306,41 +23453,32 @@[m [mend[m
       [m
   [m
   [m
[31m-    Adds key/value pairs to the exclusion_filterexclusion_filter. If argsargs[m
[31m-includes any symbols that are not part of the hash, each symbol[m
[31m-is treated as a key in the hash with the value truetrue.Adds key/value pairs to the exclusion_filter. If args[m
[31m-includes any symbols that are not part of the hash, each symbol[m
[31m-is treated as a key in the hash with the value true.[m
[32m+[m[41m    [m
[32m+[m[32mAdds key/value pairs to the exclusion_filterexclusion_filter. If argsargs includes any symbols that are not part of the hash, each symbol is treated as a key in the hash with the value truetrue.Adds key/value pairs to the exclusion_filter. If args includes any symbols that are not part of the hash, each symbol is treated as a key in the hash with the value true.[m
 [m
 NoteNote[m
 [m
[31m-Filters set using this method can be overridden from the command line[m
[31m-or config files (e.g. .rspec.rspec).Filters set using this method can be overridden from the command line[m
[31m-or config files (e.g. .rspec).[m
[32m+[m[32mFilters set using this method can be overridden from the command line or config files (e.g. .rspec.rspec).Filters set using this method can be overridden from the command line or config files (e.g. .rspec).[m
 [m
 [m
   [m
[31m-    Adds key/value pairs to the exclusion_filter. If args[m
[31m-includes any symbols that are not part of the hash, each symbol[m
[31m-is treated as a key in the hash with the value true.[m
[32m+[m[41m    [m
[32m+[m[32mAdds key/value pairs to the exclusion_filter. If args includes any symbols that are not part of the hash, each symbol is treated as a key in the hash with the value true.[m
 [m
 Note[m
 [m
[31m-Filters set using this method can be overridden from the command line[m
[31m-or config files (e.g. .rspec).[m
[32m+[m[32mFilters set using this method can be overridden from the command line or config files (e.g. .rspec).[m
[32m+[m
 [m
   [m
 [m
   [m
     [m
[31m-    Adds key/value pairs to the exclusion_filter. If args[m
[31m-includes any symbols that are not part of the hash, each symbol[m
[31m-is treated as a key in the hash with the value true.[m
[32m+[m[32mAdds key/value pairs to the exclusion_filter. If args includes any symbols that are not part of the hash, each symbol is treated as a key in the hash with the value true.[m
 [m
 Note[m
 [m
[31m-Filters set using this method can be overridden from the command line[m
[31m-or config files (e.g. .rspec).[m
[32m+[m[32mFilters set using this method can be overridden from the command line or config files (e.g. .rspec).[m
 [m
 [m
   [m
[36m@@ -23573,14 +23711,12 @@[m [mend[m
 [m
   [m
   [m
[31m-    Adds key/value pairs to the exclusion_filter. If args[m
[31m-includes any symbols that are not part of the hash, each symbol[m
[31m-is treated as a key in the hash with the value true.[m
[32m+[m[41m    [m
[32m+[m[32mAdds key/value pairs to the exclusion_filter. If args includes any symbols that are not part of the hash, each symbol is treated as a key in the hash with the value true.[m
 [m
 Note[m
 [m
[31m-Filters set using this method can be overridden from the command line[m
[31m-or config files (e.g. .rspec).[m
[32m+[m[32mFilters set using this method can be overridden from the command line or config files (e.g. .rspec).[m
 [m
 [m
   [m
[36m@@ -23643,41 +23779,32 @@[m [mend[m
       [m
   [m
   [m
[31m-    Adds key/value pairs to the inclusion_filterinclusion_filter. If argsargs[m
[31m-includes any symbols that are not part of the hash, each symbol[m
[31m-is treated as a key in the hash with the value truetrue.Adds key/value pairs to the inclusion_filter. If args[m
[31m-includes any symbols that are not part of the hash, each symbol[m
[31m-is treated as a key in the hash with the value true.[m
[32m+[m[41m    [m
[32m+[m[32mAdds key/value pairs to the inclusion_filterinclusion_filter. If argsargs includes any symbols that are not part of the hash, each symbol is treated as a key in the hash with the value truetrue.Adds key/value pairs to the inclusion_filter. If args includes any symbols that are not part of the hash, each symbol is treated as a key in the hash with the value true.[m
 [m
 NoteNote[m
 [m
[31m-Filters set using this method can be overridden from the command line[m
[31m-or config files (e.g. .rspec.rspec).Filters set using this method can be overridden from the command line[m
[31m-or config files (e.g. .rspec).[m
[32m+[m[32mFilters set using this method can be overridden from the command line or config files (e.g. .rspec.rspec).Filters set using this method can be overridden from the command line or config files (e.g. .rspec).[m
 [m
 [m
   [m
[31m-    Adds key/value pairs to the inclusion_filter. If args[m
[31m-includes any symbols that are not part of the hash, each symbol[m
[31m-is treated as a key in the hash with the value true.[m
[32m+[m[41m    [m
[32m+[m[32mAdds key/value pairs to the inclusion_filter. If args includes any symbols that are not part of the hash, each symbol is treated as a key in the hash with the value true.[m
 [m
 Note[m
 [m
[31m-Filters set using this method can be overridden from the command line[m
[31m-or config files (e.g. .rspec).[m
[32m+[m[32mFilters set using this method can be overridden from the command line or config files (e.g. .rspec).[m
[32m+[m
 [m
   [m
 [m
   [m
     [m
[31m-    Adds key/value pairs to the inclusion_filter. If args[m
[31m-includes any symbols that are not part of the hash, each symbol[m
[31m-is treated as a key in the hash with the value true.[m
[32m+[m[32mAdds key/value pairs to the inclusion_filter. If args includes any symbols that are not part of the hash, each symbol is treated as a key in the hash with the value true.[m
 [m
 Note[m
 [m
[31m-Filters set using this method can be overridden from the command line[m
[31m-or config files (e.g. .rspec).[m
[32m+[m[32mFilters set using this method can be overridden from the command line or config files (e.g. .rspec).[m
 [m
 [m
   [m
[36m@@ -23910,14 +24037,12 @@[m [mend[m
 [m
   [m
   [m
[31m-    Adds key/value pairs to the inclusion_filter. If args[m
[31m-includes any symbols that are not part of the hash, each symbol[m
[31m-is treated as a key in the hash with the value true.[m
[32m+[m[41m    [m
[32m+[m[32mAdds key/value pairs to the inclusion_filter. If args includes any symbols that are not part of the hash, each symbol is treated as a key in the hash with the value true.[m
 [m
 Note[m
 [m
[31m-Filters set using this method can be overridden from the command line[m
[31m-or config files (e.g. .rspec).[m
[32m+[m[32mFilters set using this method can be overridden from the command line or config files (e.g. .rspec).[m
 [m
 [m
   [m
[36m@@ -23980,49 +24105,20 @@[m [mend[m
       [m
   [m
   [m
[31m-    Applies the provided filter only if any of examples match, in constrast[m
[31m-to #filter_run, which always applies even if no examples match, in[m
[31m-which case no examples will be run. This allows you to leave configured[m
[31m-filters in place that are intended only for temporary use. The most common[m
[31m-example is focus filtering: config.filter_run_when_matching :focusconfig.filter_run_when_matching :focus.[m
[31m-With that configured, you can temporarily focus an example or group[m
[31m-by tagging it with :focus:focus metadata, or prefixing it with an ff[m
[31m-(as in fdescribefdescribe, fcontextfcontext and fitfit) since those are aliases for[m
[31m-describedescribe/contextcontext/itit with :focus:focus metadata.Applies the provided filter only if any of examples match, in constrast[m
[31m-to #filter_run, which always applies even if no examples match, in[m
[31m-which case no examples will be run. This allows you to leave configured[m
[31m-filters in place that are intended only for temporary use. The most common[m
[31m-example is focus filtering: config.filter_run_when_matching :focus.[m
[31m-With that configured, you can temporarily focus an example or group[m
[31m-by tagging it with :focus metadata, or prefixing it with an f[m
[31m-(as in fdescribe, fcontext and fit) since those are aliases for[m
[31m-describe/context/it with :focus metadata.[m
     [m
[32m+[m[32mApplies the provided filter only if any of examples match, in constrast to #filter_run, which always applies even if no examples match, in which case no examples will be run. This allows you to leave configured filters in place that are intended only for temporary use. The most common example is focus filtering: config.filter_run_when_matching :focusconfig.filter_run_when_matching :focus. With that configured, you can temporarily focus an example or group by tagging it with :focus:focus metadata, or prefixing it with an ff (as in fdescribefdescribe, fcontextfcontext and fitfit) since those are aliases for describedescribe/contextcontext/itit with :focus:focus metadata.Applies the provided filter only if any of examples match, in constrast to #filter_run, which always applies even if no examples match, in which case no examples will be run. This allows you to leave configured filters in place that are intended only for temporary use. The most common example is focus filtering: config.filter_run_when_matching :focus. With that configured, you can temporarily focus an example or group by tagging it with :focus metadata, or prefixing it with an f (as in fdescribe, fcontext and fit) since those are aliases for describe/context/it with :focus metadata.[m
 [m
 [m
[31m-    Applies the provided filter only if any of examples match, in constrast[m
[31m-to #filter_run, which always applies even if no examples match, in[m
[31m-which case no examples will be run. This allows you to leave configured[m
[31m-filters in place that are intended only for temporary use. The most common[m
[31m-example is focus filtering: config.filter_run_when_matching :focus.[m
[31m-With that configured, you can temporarily focus an example or group[m
[31m-by tagging it with :focus metadata, or prefixing it with an f[m
[31m-(as in fdescribe, fcontext and fit) since those are aliases for[m
[31m-describe/context/it with :focus metadata.[m
   [m
     [m
[32m+[m[32mApplies the provided filter only if any of examples match, in constrast to #filter_run, which always applies even if no examples match, in which case no examples will be run. This allows you to leave configured filters in place that are intended only for temporary use. The most common example is focus filtering: config.filter_run_when_matching :focus. With that configured, you can temporarily focus an example or group by tagging it with :focus metadata, or prefixing it with an f (as in fdescribe, fcontext and fit) since those are aliases for describe/context/it with :focus metadata.[m
 [m
 [m
   [m
[31m-    Applies the provided filter only if any of examples match, in constrast[m
[31m-to #filter_run, which always applies even if no examples match, in[m
[31m-which case no examples will be run. This allows you to leave configured[m
[31m-filters in place that are intended only for temporary use. The most common[m
[31m-example is focus filtering: config.filter_run_when_matching :focus.[m
[31m-With that configured, you can temporarily focus an example or group[m
[31m-by tagging it with :focus metadata, or prefixing it with an f[m
[31m-(as in fdescribe, fcontext and fit) since those are aliases for[m
[31m-describe/context/it with :focus metadata.[m
[32m+[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mApplies the provided filter only if any of examples match, in constrast to #filter_run, which always applies even if no examples match, in which case no examples will be run. This allows you to leave configured filters in place that are intended only for temporary use. The most common example is focus filtering: config.filter_run_when_matching :focus. With that configured, you can temporarily focus an example or group by tagging it with :focus metadata, or prefixing it with an f (as in fdescribe, fcontext and fit) since those are aliases for describe/context/it with :focus metadata.[m
 [m
 [m
   [m
[36m@@ -24131,15 +24227,8 @@[m [mend[m
 [m
   [m
   [m
[31m-    Applies the provided filter only if any of examples match, in constrast[m
[31m-to #filter_run, which always applies even if no examples match, in[m
[31m-which case no examples will be run. This allows you to leave configured[m
[31m-filters in place that are intended only for temporary use. The most common[m
[31m-example is focus filtering: config.filter_run_when_matching :focus.[m
[31m-With that configured, you can temporarily focus an example or group[m
[31m-by tagging it with :focus metadata, or prefixing it with an f[m
[31m-(as in fdescribe, fcontext and fit) since those are aliases for[m
[31m-describe/context/it with :focus metadata.[m
[32m+[m[41m    [m
[32m+[m[32mApplies the provided filter only if any of examples match, in constrast to #filter_run, which always applies even if no examples match, in which case no examples will be run. This allows you to leave configured filters in place that are intended only for temporary use. The most common example is focus filtering: config.filter_run_when_matching :focus. With that configured, you can temporarily focus an example or group by tagging it with :focus metadata, or prefixing it with an f (as in fdescribe, fcontext and fit) since those are aliases for describe/context/it with :focus metadata.[m
 [m
 [m
   [m
[36m@@ -24362,16 +24455,15 @@[m [mend[m
       [m
   [m
   [m
[31m-    Returns a duplicate of the formatters currently loaded in[m
[31m-the FormatterLoaderFormatterLoader for introspection.Returns a duplicate of the formatters currently loaded in[m
[31m-the FormatterLoader for introspection.[m
[32m+[m[41m    [m
[32m+[m[32mReturns a duplicate of the formatters currently loaded in the FormatterLoaderFormatterLoader for introspection.Returns a duplicate of the formatters currently loaded in the FormatterLoader for introspection.[m
 [m
 Note as this is a duplicate, any mutations will be disregarded.Note as this is a duplicate, any mutations will be disregarded.[m
 [m
 [m
   [m
[31m-    Returns a duplicate of the formatters currently loaded in[m
[31m-the FormatterLoader for introspection.[m
[32m+[m[41m    [m
[32m+[m[32mReturns a duplicate of the formatters currently loaded in the FormatterLoader for introspection.[m
 [m
 Note as this is a duplicate, any mutations will be disregarded.[m
 [m
[36m@@ -24379,8 +24471,8 @@[m [mNote as this is a duplicate, any mutations will be disregarded.[m
   [m
 [m
   [m
[31m-    Returns a duplicate of the formatters currently loaded in[m
[31m-the FormatterLoader for introspection.[m
[32m+[m[41m    [m
[32m+[m[32mReturns a duplicate of the formatters currently loaded in the FormatterLoader for introspection.[m
 [m
 Note as this is a duplicate, any mutations will be disregarded.[m
 [m
[36m@@ -24532,8 +24629,8 @@[m [mend[m
 [m
   [m
   [m
[31m-    Returns a duplicate of the formatters currently loaded in[m
[31m-the FormatterLoader for introspection.[m
[32m+[m[41m    [m
[32m+[m[32mReturns a duplicate of the formatters currently loaded in the FormatterLoader for introspection.[m
 [m
 Note as this is a duplicate, any mutations will be disregarded.[m
 [m
[36m@@ -25355,58 +25487,33 @@[m [mend[m
     [m
   [m
     Note:Note:[m
[31m-    Filtered module inclusions can also be applied to[m
[31m-individual examples that have matching metadata. Just like[m
[31m-Ruby's object model is that every object has a singleton class[m
[31m-which has only a single instance, RSpec's model is that every[m
[31m-example has a singleton example group containing just the one[m
[31m-example.Filtered module inclusions can also be applied to[m
[31m-individual examples that have matching metadata. Just like[m
[31m-Ruby's object model is that every object has a singleton class[m
[31m-which has only a single instance, RSpec's model is that every[m
[31m-example has a singleton example group containing just the one[m
[31m-example.[m
[31m-Filtered module inclusions can also be applied to[m
[31m-individual examples that have matching metadata. Just like[m
[31m-Ruby's object model is that every object has a singleton class[m
[31m-which has only a single instance, RSpec's model is that every[m
[31m-example has a singleton example group containing just the one[m
[31m-example.[m
[32m+[m[41m    [m
[32m+[m[32mFiltered module inclusions can also be applied to individual examples that have matching metadata. Just like Ruby's object model is that every object has a singleton class which has only a single instance, RSpec's model is that every example has a singleton example group containing just the one example.Filtered module inclusions can also be applied to individual examples that have matching metadata. Just like Ruby's object model is that every object has a singleton class which has only a single instance, RSpec's model is that every example has a singleton example group containing just the one example.[m
[32m+[m
[32m+[m[32mFiltered module inclusions can also be applied to individual examples that have matching metadata. Just like Ruby's object model is that every object has a singleton class which has only a single instance, RSpec's model is that every example has a singleton example group containing just the one example.[m
 [m
   [m
     Note:[m
[31m-    Filtered module inclusions can also be applied to[m
[31m-individual examples that have matching metadata. Just like[m
[31m-Ruby's object model is that every object has a singleton class[m
[31m-which has only a single instance, RSpec's model is that every[m
[31m-example has a singleton example group containing just the one[m
[31m-example.[m
[32m+[m[41m    [m
[32m+[m[32mFiltered module inclusions can also be applied to individual examples that have matching metadata. Just like Ruby's object model is that every object has a singleton class which has only a single instance, RSpec's model is that every example has a singleton example group containing just the one example.[m
 [m
   [m
 [m
[31m-Tells RSpec to include modmod in example groups. Methods defined in[m
[31m-modmod are exposed to examples (not example groups). Use filtersfilters to[m
[31m-constrain the groups or examples in which to include the module.Tells RSpec to include mod in example groups. Methods defined in[m
[31m-mod are exposed to examples (not example groups). Use filters to[m
[31m-constrain the groups or examples in which to include the module.[m
[32m+[m
[32m+[m[32mTells RSpec to include modmod in example groups. Methods defined in modmod are exposed to examples (not example groups). Use filtersfilters to constrain the groups or examples in which to include the module.Tells RSpec to include mod in example groups. Methods defined in mod are exposed to examples (not example groups). Use filters to constrain the groups or examples in which to include the module.[m
 [m
 [m
   [m
     [m
   [m
     Note:[m
[31m-    Filtered module inclusions can also be applied to[m
[31m-individual examples that have matching metadata. Just like[m
[31m-Ruby's object model is that every object has a singleton class[m
[31m-which has only a single instance, RSpec's model is that every[m
[31m-example has a singleton example group containing just the one[m
[31m-example.[m
[32m+[m[41m    [m
[32m+[m[32mFiltered module inclusions can also be applied to individual examples that have matching metadata. Just like Ruby's object model is that every object has a singleton class which has only a single instance, RSpec's model is that every example has a singleton example group containing just the one example.[m
 [m
   [m
 [m
[31m-Tells RSpec to include mod in example groups. Methods defined in[m
[31m-mod are exposed to examples (not example groups). Use filters to[m
[31m-constrain the groups or examples in which to include the module.[m
[32m+[m
[32m+[m[32mTells RSpec to include mod in example groups. Methods defined in mod are exposed to examples (not example groups). Use filters to constrain the groups or examples in which to include the module.[m
 [m
 [m
   [m
[36m@@ -25415,18 +25522,13 @@[m [mconstrain the groups or examples in which to include the module.[m
     [m
   [m
     Note:[m
[31m-    Filtered module inclusions can also be applied to[m
[31m-individual examples that have matching metadata. Just like[m
[31m-Ruby's object model is that every object has a singleton class[m
[31m-which has only a single instance, RSpec's model is that every[m
[31m-example has a singleton example group containing just the one[m
[31m-example.[m
[32m+[m[41m    [m
[32m+[m[32mFiltered module inclusions can also be applied to individual examples that have matching metadata. Just like Ruby's object model is that every object has a singleton class which has only a single instance, RSpec's model is that every example has a singleton example group containing just the one example.[m
 [m
   [m
 [m
[31m-Tells RSpec to include mod in example groups. Methods defined in[m
[31m-mod are exposed to examples (not example groups). Use filters to[m
[31m-constrain the groups or examples in which to include the module.[m
[32m+[m
[32m+[m[32mTells RSpec to include mod in example groups. Methods defined in mod are exposed to examples (not example groups). Use filters to constrain the groups or examples in which to include the module.[m
 [m
 [m
   [m
[36m@@ -25713,18 +25815,13 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    Filtered module inclusions can also be applied to[m
[31m-individual examples that have matching metadata. Just like[m
[31m-Ruby's object model is that every object has a singleton class[m
[31m-which has only a single instance, RSpec's model is that every[m
[31m-example has a singleton example group containing just the one[m
[31m-example.[m
[32m+[m[41m    [m
[32m+[m[32mFiltered module inclusions can also be applied to individual examples that have matching metadata. Just like Ruby's object model is that every object has a singleton class which has only a single instance, RSpec's model is that every example has a singleton example group containing just the one example.[m
[32m+[m
   [m
 [m
 [m
[31m-Tells RSpec to include mod in example groups. Methods defined in[m
[31m-mod are exposed to examples (not example groups). Use filters to[m
[31m-constrain the groups or examples in which to include the module.[m
[32m+[m[32mTells RSpec to include mod in example groups. Methods defined in mod are exposed to examples (not example groups). Use filters to constrain the groups or examples in which to include the module.[m
 [m
 [m
   [m
[36m@@ -25806,58 +25903,33 @@[m [mend[m
     [m
   [m
     Note:Note:[m
[31m-    Filtered context inclusions can also be applied to[m
[31m-individual examples that have matching metadata. Just like[m
[31m-Ruby's object model is that every object has a singleton class[m
[31m-which has only a single instance, RSpec's model is that every[m
[31m-example has a singleton example group containing just the one[m
[31m-example.Filtered context inclusions can also be applied to[m
[31m-individual examples that have matching metadata. Just like[m
[31m-Ruby's object model is that every object has a singleton class[m
[31m-which has only a single instance, RSpec's model is that every[m
[31m-example has a singleton example group containing just the one[m
[31m-example.[m
[31m-Filtered context inclusions can also be applied to[m
[31m-individual examples that have matching metadata. Just like[m
[31m-Ruby's object model is that every object has a singleton class[m
[31m-which has only a single instance, RSpec's model is that every[m
[31m-example has a singleton example group containing just the one[m
[31m-example.[m
[32m+[m[41m    [m
[32m+[m[32mFiltered context inclusions can also be applied to individual examples that have matching metadata. Just like Ruby's object model is that every object has a singleton class which has only a single instance, RSpec's model is that every example has a singleton example group containing just the one example.Filtered context inclusions can also be applied to individual examples that have matching metadata. Just like Ruby's object model is that every object has a singleton class which has only a single instance, RSpec's model is that every example has a singleton example group containing just the one example.[m
[32m+[m
[32m+[m[32mFiltered context inclusions can also be applied to individual examples that have matching metadata. Just like Ruby's object model is that every object has a singleton class which has only a single instance, RSpec's model is that every example has a singleton example group containing just the one example.[m
 [m
   [m
     Note:[m
[31m-    Filtered context inclusions can also be applied to[m
[31m-individual examples that have matching metadata. Just like[m
[31m-Ruby's object model is that every object has a singleton class[m
[31m-which has only a single instance, RSpec's model is that every[m
[31m-example has a singleton example group containing just the one[m
[31m-example.[m
[32m+[m[41m    [m
[32m+[m[32mFiltered context inclusions can also be applied to individual examples that have matching metadata. Just like Ruby's object model is that every object has a singleton class which has only a single instance, RSpec's model is that every example has a singleton example group containing just the one example.[m
 [m
   [m
 [m
[31m-Tells RSpec to include the named shared example group in example groups.[m
[31m-Use filtersfilters to constrain the groups or examples in which to include[m
[31m-the example group.Tells RSpec to include the named shared example group in example groups.[m
[31m-Use filters to constrain the groups or examples in which to include[m
[31m-the example group.[m
[32m+[m
[32m+[m[32mTells RSpec to include the named shared example group in example groups. Use filtersfilters to constrain the groups or examples in which to include the example group.Tells RSpec to include the named shared example group in example groups. Use filters to constrain the groups or examples in which to include the example group.[m
 [m
 [m
   [m
     [m
   [m
     Note:[m
[31m-    Filtered context inclusions can also be applied to[m
[31m-individual examples that have matching metadata. Just like[m
[31m-Ruby's object model is that every object has a singleton class[m
[31m-which has only a single instance, RSpec's model is that every[m
[31m-example has a singleton example group containing just the one[m
[31m-example.[m
[32m+[m[41m    [m
[32m+[m[32mFiltered context inclusions can also be applied to individual examples that have matching metadata. Just like Ruby's object model is that every object has a singleton class which has only a single instance, RSpec's model is that every example has a singleton example group containing just the one example.[m
 [m
   [m
 [m
[31m-Tells RSpec to include the named shared example group in example groups.[m
[31m-Use filters to constrain the groups or examples in which to include[m
[31m-the example group.[m
[32m+[m
[32m+[m[32mTells RSpec to include the named shared example group in example groups. Use filters to constrain the groups or examples in which to include the example group.[m
 [m
 [m
   [m
[36m@@ -25866,18 +25938,13 @@[m [mthe example group.[m
     [m
   [m
     Note:[m
[31m-    Filtered context inclusions can also be applied to[m
[31m-individual examples that have matching metadata. Just like[m
[31m-Ruby's object model is that every object has a singleton class[m
[31m-which has only a single instance, RSpec's model is that every[m
[31m-example has a singleton example group containing just the one[m
[31m-example.[m
     [m
[32m+[m[32mFiltered context inclusions can also be applied to individual examples that have matching metadata. Just like Ruby's object model is that every object has a singleton class which has only a single instance, RSpec's model is that every example has a singleton example group containing just the one example.[m
 [m
   [m
[31m-Tells RSpec to include the named shared example group in example groups.[m
[31m-Use filters to constrain the groups or examples in which to include[m
[31m-the example group.[m
[32m+[m
[32m+[m
[32m+[m[32mTells RSpec to include the named shared example group in example groups. Use filters to constrain the groups or examples in which to include the example group.[m
 [m
 [m
   [m
[36m@@ -26129,18 +26196,13 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    Filtered context inclusions can also be applied to[m
[31m-individual examples that have matching metadata. Just like[m
[31m-Ruby's object model is that every object has a singleton class[m
[31m-which has only a single instance, RSpec's model is that every[m
[31m-example has a singleton example group containing just the one[m
[31m-example.[m
     [m
[32m+[m[32mFiltered context inclusions can also be applied to individual examples that have matching metadata. Just like Ruby's object model is that every object has a singleton class which has only a single instance, RSpec's model is that every example has a singleton example group containing just the one example.[m
 [m
   [m
[31m-Tells RSpec to include the named shared example group in example groups.[m
[31m-Use filters to constrain the groups or examples in which to include[m
[31m-the example group.[m
[32m+[m
[32m+[m
[32m+[m[32mTells RSpec to include the named shared example group in example groups. Use filters to constrain the groups or examples in which to include the example group.[m
 [m
 [m
   [m
[36m@@ -26211,21 +26273,20 @@[m [mend[m
       [m
   [m
   [m
[31m-    Returns the inclusion_filterinclusion_filter. If none has been set, returns an empty[m
[31m-hash.Returns the inclusion_filter. If none has been set, returns an empty[m
[31m-hash.[m
     [m
[32m+[m[32mReturns the inclusion_filterinclusion_filter. If none has been set, returns an empty hash.Returns the inclusion_filter. If none has been set, returns an empty hash.[m
 [m
 [m
[31m-    Returns the inclusion_filter. If none has been set, returns an empty[m
[31m-hash.[m
   [m
     [m
[32m+[m[32mReturns the inclusion_filter. If none has been set, returns an empty hash.[m
 [m
 [m
   [m
[31m-    Returns the inclusion_filter. If none has been set, returns an empty[m
[31m-hash.[m
[32m+[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mReturns the inclusion_filter. If none has been set, returns an empty hash.[m
 [m
 [m
   [m
[36m@@ -26314,8 +26375,8 @@[m [mend[m
 [m
   [m
   [m
[31m-    Returns the inclusion_filter. If none has been set, returns an empty[m
[31m-hash.[m
[32m+[m[41m    [m
[32m+[m[32mReturns the inclusion_filter. If none has been set, returns an empty hash.[m
 [m
 [m
   [m
[36m@@ -26347,37 +26408,32 @@[m [mend[m
       [m
   [m
   [m
[31m-    Clears and reassigns the inclusion_filterinclusion_filter. Set to nilnil if you don't[m
[31m-want any inclusion filter at all.Clears and reassigns the inclusion_filter. Set to nil if you don't[m
[31m-want any inclusion filter at all.[m
[32m+[m[41m    [m
[32m+[m[32mClears and reassigns the inclusion_filterinclusion_filter. Set to nilnil if you don't want any inclusion filter at all.Clears and reassigns the inclusion_filter. Set to nil if you don't want any inclusion filter at all.[m
 [m
 WarningWarning[m
 [m
[31m-This overrides any inclusion filters/tags set on the command line or in[m
[31m-configuration files.This overrides any inclusion filters/tags set on the command line or in[m
[31m-configuration files.[m
[32m+[m[32mThis overrides any inclusion filters/tags set on the command line or in configuration files.This overrides any inclusion filters/tags set on the command line or in configuration files.[m
[32m+[m
 [m
   [m
     [m
[31m-    Clears and reassigns the inclusion_filter. Set to nil if you don't[m
[31m-want any inclusion filter at all.[m
[32m+[m[32mClears and reassigns the inclusion_filter. Set to nil if you don't want any inclusion filter at all.[m
 [m
 Warning[m
 [m
[31m-This overrides any inclusion filters/tags set on the command line or in[m
[31m-configuration files.[m
[32m+[m[32mThis overrides any inclusion filters/tags set on the command line or in configuration files.[m
 [m
 [m
   [m
 [m
   [m
[31m-    Clears and reassigns the inclusion_filter. Set to nil if you don't[m
[31m-want any inclusion filter at all.[m
[32m+[m[41m    [m
[32m+[m[32mClears and reassigns the inclusion_filter. Set to nil if you don't want any inclusion filter at all.[m
 [m
 Warning[m
 [m
[31m-This overrides any inclusion filters/tags set on the command line or in[m
[31m-configuration files.[m
[32m+[m[32mThis overrides any inclusion filters/tags set on the command line or in configuration files.[m
 [m
 [m
   [m
[36m@@ -26476,13 +26532,12 @@[m [mend[m
 [m
   [m
   [m
[31m-    Clears and reassigns the inclusion_filter. Set to nil if you don't[m
[31m-want any inclusion filter at all.[m
[32m+[m[41m    [m
[32m+[m[32mClears and reassigns the inclusion_filter. Set to nil if you don't want any inclusion filter at all.[m
 [m
 Warning[m
 [m
[31m-This overrides any inclusion filters/tags set on the command line or in[m
[31m-configuration files.[m
[32m+[m[32mThis overrides any inclusion filters/tags set on the command line or in configuration files.[m
 [m
 [m
   [m
[36m@@ -26923,17 +26987,11 @@[m [mend[m
 [m
 frameworkframework can be a Symbol or a Module.framework can be a Symbol or a Module.[m
 [m
[31m-Given any of :rspec:rspec, :mocha:mocha, :flexmock:flexmock, or :rr:rr, configures the[m
[31m-named framework.Given any of :rspec, :mocha, :flexmock, or :rr, configures the[m
[31m-named framework.[m
[32m+[m[32mGiven any of :rspec:rspec, :mocha:mocha, :flexmock:flexmock, or :rr:rr, configures the named framework.Given any of :rspec, :mocha, :flexmock, or :rr, configures the named framework.[m
 [m
[31m-Given :nothing:nothing, configures no framework. Use this if you don't use[m
[31m-any mocking framework to save a little bit of overhead.Given :nothing, configures no framework. Use this if you don't use[m
[31m-any mocking framework to save a little bit of overhead.[m
[32m+[m[32mGiven :nothing:nothing, configures no framework. Use this if you don't use any mocking framework to save a little bit of overhead.Given :nothing, configures no framework. Use this if you don't use any mocking framework to save a little bit of overhead.[m
 [m
[31m-Given a Module, includes that module in every example group. The module[m
[31m-should adhere to RSpec's mock framework adapter API:.Given a Module, includes that module in every example group. The module[m
[31m-should adhere to RSpec's mock framework adapter API:.[m
[32m+[m[32mGiven a Module, includes that module in every example group. The module should adhere to RSpec's mock framework adapter API:.Given a Module, includes that module in every example group. The module should adhere to RSpec's mock framework adapter API:.[m
 [m
 setup_mocks_for_rspec[m
   - called before each example[m
[36m@@ -26964,9 +27022,7 @@[m [mteardown_mocks_for_rspec[m
   - called after verify_mocks_for_rspec (even if there are errors)[m
 [m
 [m
[31m-If the module responds to configurationconfiguration and mock_withmock_with receives a[m
[31m-block, it will yield the configuration object to the block e.g.If the module responds to configuration and mock_with receives a[m
[31m-block, it will yield the configuration object to the block e.g.[m
[32m+[m[32mIf the module responds to configurationconfiguration and mock_withmock_with receives a block, it will yield the configuration object to the block e.g.If the module responds to configuration and mock_with receives a block, it will yield the configuration object to the block e.g.[m
 [m
 configconfig..mock_withmock_with OtherMockFrameworkAdapterOtherMockFrameworkAdapter dodo ||mod_configmod_config||[m
   mod_configmod_config..custom_settingcustom_setting == truetrue[m
[36m@@ -26985,14 +27042,11 @@[m [mend[m
 [m
 framework can be a Symbol or a Module.[m
 [m
[31m-Given any of :rspec, :mocha, :flexmock, or :rr, configures the[m
[31m-named framework.[m
[32m+[m[32mGiven any of :rspec, :mocha, :flexmock, or :rr, configures the named framework.[m
 [m
[31m-Given :nothing, configures no framework. Use this if you don't use[m
[31m-any mocking framework to save a little bit of overhead.[m
[32m+[m[32mGiven :nothing, configures no framework. Use this if you don't use any mocking framework to save a little bit of overhead.[m
 [m
[31m-Given a Module, includes that module in every example group. The module[m
[31m-should adhere to RSpec's mock framework adapter API:.[m
[32m+[m[32mGiven a Module, includes that module in every example group. The module should adhere to RSpec's mock framework adapter API:.[m
 [m
 setup_mocks_for_rspec[m
   - called before each example[m
[36m@@ -27005,8 +27059,7 @@[m [mteardown_mocks_for_rspec[m
   - called after verify_mocks_for_rspec (even if there are errors)[m
 [m
 [m
[31m-If the module responds to configuration and mock_with receives a[m
[31m-block, it will yield the configuration object to the block e.g.[m
[32m+[m[32mIf the module responds to configuration and mock_with receives a block, it will yield the configuration object to the block e.g.[m
 [m
 config.mock_with OtherMockFrameworkAdapter do |mod_config|[m
   mod_config.custom_setting = true[m
[36m@@ -27021,14 +27075,11 @@[m [mend[m
 [m
 framework can be a Symbol or a Module.[m
 [m
[31m-Given any of :rspec, :mocha, :flexmock, or :rr, configures the[m
[31m-named framework.[m
[32m+[m[32mGiven any of :rspec, :mocha, :flexmock, or :rr, configures the named framework.[m
 [m
[31m-Given :nothing, configures no framework. Use this if you don't use[m
[31m-any mocking framework to save a little bit of overhead.[m
[32m+[m[32mGiven :nothing, configures no framework. Use this if you don't use any mocking framework to save a little bit of overhead.[m
 [m
[31m-Given a Module, includes that module in every example group. The module[m
[31m-should adhere to RSpec's mock framework adapter API:.[m
[32m+[m[32mGiven a Module, includes that module in every example group. The module should adhere to RSpec's mock framework adapter API:.[m
 [m
 setup_mocks_for_rspec[m
   - called before each example[m
[36m@@ -27041,8 +27092,7 @@[m [mteardown_mocks_for_rspec[m
   - called after verify_mocks_for_rspec (even if there are errors)[m
 [m
 [m
[31m-If the module responds to configuration and mock_with receives a[m
[31m-block, it will yield the configuration object to the block e.g.[m
[32m+[m[32mIf the module responds to configuration and mock_with receives a block, it will yield the configuration object to the block e.g.[m
 [m
 config.mock_with OtherMockFrameworkAdapter do |mod_config|[m
   mod_config.custom_setting = true[m
[36m@@ -27420,14 +27471,11 @@[m [mend[m
 [m
 framework can be a Symbol or a Module.[m
 [m
[31m-Given any of :rspec, :mocha, :flexmock, or :rr, configures the[m
[31m-named framework.[m
[32m+[m[32mGiven any of :rspec, :mocha, :flexmock, or :rr, configures the named framework.[m
 [m
[31m-Given :nothing, configures no framework. Use this if you don't use[m
[31m-any mocking framework to save a little bit of overhead.[m
[32m+[m[32mGiven :nothing, configures no framework. Use this if you don't use any mocking framework to save a little bit of overhead.[m
 [m
[31m-Given a Module, includes that module in every example group. The module[m
[31m-should adhere to RSpec's mock framework adapter API:.[m
[32m+[m[32mGiven a Module, includes that module in every example group. The module should adhere to RSpec's mock framework adapter API:.[m
 [m
 setup_mocks_for_rspec[m
   - called before each example[m
[36m@@ -27440,8 +27488,7 @@[m [mteardown_mocks_for_rspec[m
   - called after verify_mocks_for_rspec (even if there are errors)[m
 [m
 [m
[31m-If the module responds to configuration and mock_with receives a[m
[31m-block, it will yield the configuration object to the block e.g.[m
[32m+[m[32mIf the module responds to configuration and mock_with receives a block, it will yield the configuration object to the block e.g.[m
 [m
 config.mock_with OtherMockFrameworkAdapter do |mod_config|[m
   mod_config.custom_setting = true[m
[36m@@ -27815,29 +27870,20 @@[m [mend[m
       [m
   [m
   [m
[31m-    Sets the default global ordering strategy. By default this can be one[m
[31m-of :defined:defined, :random:random, but is customizable through the[m
[31m-register_orderingregister_ordering API. If order is set to 'rand:<seed>''rand:<seed>',[m
[31m-the seed will also be set.Sets the default global ordering strategy. By default this can be one[m
[31m-of :defined, :random, but is customizable through the[m
[31m-register_ordering API. If order is set to 'rand:<seed>',[m
[31m-the seed will also be set.[m
[32m+[m[41m    [m
[32m+[m[32mSets the default global ordering strategy. By default this can be one of :defined:defined, :random:random, but is customizable through the register_orderingregister_ordering API. If order is set to 'rand:<seed>''rand:<seed>', the seed will also be set.Sets the default global ordering strategy. By default this can be one of :defined, :random, but is customizable through the register_ordering API. If order is set to 'rand:<seed>', the seed will also be set.[m
 [m
 [m
   [m
[31m-    Sets the default global ordering strategy. By default this can be one[m
[31m-of :defined, :random, but is customizable through the[m
[31m-register_ordering API. If order is set to 'rand:<seed>',[m
[31m-the seed will also be set.[m
     [m
[32m+[m[32mSets the default global ordering strategy. By default this can be one of :defined, :random, but is customizable through the register_ordering API. If order is set to 'rand:<seed>', the seed will also be set.[m
 [m
 [m
   [m
 [m
[31m-    Sets the default global ordering strategy. By default this can be one[m
[31m-of :defined, :random, but is customizable through the[m
[31m-register_ordering API. If order is set to 'rand:<seed>',[m
[31m-the seed will also be set.[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mSets the default global ordering strategy. By default this can be one of :defined, :random, but is customizable through the register_ordering API. If order is set to 'rand:<seed>', the seed will also be set.[m
 [m
 [m
   [m
[36m@@ -27924,10 +27970,8 @@[m [mdelegate_to_ordering_manager :order=[m
 [m
   [m
   [m
[31m-    Sets the default global ordering strategy. By default this can be one[m
[31m-of :defined, :random, but is customizable through the[m
[31m-register_ordering API. If order is set to 'rand:<seed>',[m
[31m-the seed will also be set.[m
[32m+[m[41m    [m
[32m+[m[32mSets the default global ordering strategy. By default this can be one of :defined, :random, but is customizable through the register_ordering API. If order is set to 'rand:<seed>', the seed will also be set.[m
 [m
 [m
   [m
[36m@@ -27962,35 +28006,26 @@[m [mdelegate_to_ordering_manager :order=[m
       [m
   [m
   [m
[31m-    Tells RSpec to prepend example groups with modmod. Methods defined in[m
[31m-modmod are exposed to examples (not example groups). Use filtersfilters to[m
[31m-constrain the groups in which to prepend the module.Tells RSpec to prepend example groups with mod. Methods defined in[m
[31m-mod are exposed to examples (not example groups). Use filters to[m
[31m-constrain the groups in which to prepend the module.[m
     [m
[31m-Similar to includeinclude, but module is included before the example group's class[m
[31m-in the ancestor chain.Similar to include, but module is included before the example group's class[m
[31m-in the ancestor chain.[m
[32m+[m[32mTells RSpec to prepend example groups with modmod. Methods defined in modmod are exposed to examples (not example groups). Use filtersfilters to constrain the groups in which to prepend the module.Tells RSpec to prepend example groups with mod. Methods defined in mod are exposed to examples (not example groups). Use filters to constrain the groups in which to prepend the module.[m
 [m
[32m+[m[32mSimilar to includeinclude, but module is included before the example group's class in the ancestor chain.Similar to include, but module is included before the example group's class in the ancestor chain.[m
 [m
 [m
[31m-    Tells RSpec to prepend example groups with mod. Methods defined in[m
[31m-mod are exposed to examples (not example groups). Use filters to[m
[31m-constrain the groups in which to prepend the module.[m
   [m
[31m-Similar to include, but module is included before the example group's class[m
[31m-in the ancestor chain.[m
     [m
[32m+[m[32mTells RSpec to prepend example groups with mod. Methods defined in mod are exposed to examples (not example groups). Use filters to constrain the groups in which to prepend the module.[m
 [m
[32m+[m[32mSimilar to include, but module is included before the example group's class in the ancestor chain.[m
 [m
 [m
   [m
[31m-    Tells RSpec to prepend example groups with mod. Methods defined in[m
[31m-mod are exposed to examples (not example groups). Use filters to[m
[31m-constrain the groups in which to prepend the module.[m
 [m
[31m-Similar to include, but module is included before the example group's class[m
[31m-in the ancestor chain.[m
[32m+[m[41m  [m
[32m+[m[41m    [m
[32m+[m[32mTells RSpec to prepend example groups with mod. Methods defined in mod are exposed to examples (not example groups). Use filters to constrain the groups in which to prepend the module.[m
[32m+[m
[32m+[m[32mSimilar to include, but module is included before the example group's class in the ancestor chain.[m
 [m
 [m
   [m
[36m@@ -28247,12 +28282,10 @@[m [mend[m
 [m
   [m
   [m
[31m-    Tells RSpec to prepend example groups with mod. Methods defined in[m
[31m-mod are exposed to examples (not example groups). Use filters to[m
[31m-constrain the groups in which to prepend the module.[m
     [m
[31m-Similar to include, but module is included before the example group's class[m
[31m-in the ancestor chain.[m
[32m+[m[32mTells RSpec to prepend example groups with mod. Methods defined in mod are exposed to examples (not example groups). Use filters to constrain the groups in which to prepend the module.[m
[32m+[m
[32m+[m[32mSimilar to include, but module is included before the example group's class in the ancestor chain.[m
 [m
 [m
   [m
[36m@@ -28325,45 +28358,32 @@[m [mend[m
       [m
   [m
   [m
[31m-    Adds blockblock to the start of the list of beforebefore blocks in the same[m
[31m-scope (:example:example, :context:context, or :suite:suite), in contrast to #before,[m
[31m-which adds the hook to the end of the list.Adds block to the start of the list of before blocks in the same[m
[31m-scope (:example, :context, or :suite), in contrast to #before,[m
[31m-which adds the hook to the end of the list.[m
[32m+[m[41m    [m
[32m+[m[32mAdds blockblock to the start of the list of beforebefore blocks in the same scope (:example:example, :context:context, or :suite:suite), in contrast to #before, which adds the hook to the end of the list.Adds block to the start of the list of before blocks in the same scope (:example, :context, or :suite), in contrast to #before, which adds the hook to the end of the list.[m
 [m
 See Hooks#before for full beforebefore hook docs.See Hooks#before for full before hook docs.[m
 [m
[31m-This method differs from Hooks#prepend_before in only one way: it supports[m
[31m-the :suite:suite scope. Hooks with the :suite:suite scope will be run once before[m
[31m-the first example of the entire suite is executed.This method differs from Hooks#prepend_before in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once before[m
[31m-the first example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#prepend_before in only one way: it supports the :suite:suite scope. Hooks with the :suite:suite scope will be run once before the first example of the entire suite is executed.This method differs from Hooks#prepend_before in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once before the first example of the entire suite is executed.[m
[32m+[m
 [m
   [m
     [m
[31m-    Adds block to the start of the list of before blocks in the same[m
[31m-scope (:example, :context, or :suite), in contrast to #before,[m
[31m-which adds the hook to the end of the list.[m
[32m+[m[32mAdds block to the start of the list of before blocks in the same scope (:example, :context, or :suite), in contrast to #before, which adds the hook to the end of the list.[m
 [m
 See Hooks#before for full before hook docs.[m
 [m
[31m-This method differs from Hooks#prepend_before in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once before[m
[31m-the first example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#prepend_before in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once before the first example of the entire suite is executed.[m
[32m+[m
 [m
   [m
 [m
   [m
     [m
[31m-    Adds block to the start of the list of before blocks in the same[m
[31m-scope (:example, :context, or :suite), in contrast to #before,[m
[31m-which adds the hook to the end of the list.[m
[32m+[m[32mAdds block to the start of the list of before blocks in the same scope (:example, :context, or :suite), in contrast to #before, which adds the hook to the end of the list.[m
 [m
 See Hooks#before for full before hook docs.[m
 [m
[31m-This method differs from Hooks#prepend_before in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once before[m
[31m-the first example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#prepend_before in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once before the first example of the entire suite is executed.[m
 [m
 [m
   [m
[36m@@ -28596,15 +28616,12 @@[m [mend[m
 [m
   [m
   [m
[31m-    Adds block to the start of the list of before blocks in the same[m
[31m-scope (:example, :context, or :suite), in contrast to #before,[m
[31m-which adds the hook to the end of the list.[m
[32m+[m[41m    [m
[32m+[m[32mAdds block to the start of the list of before blocks in the same scope (:example, :context, or :suite), in contrast to #before, which adds the hook to the end of the list.[m
 [m
 See Hooks#before for full before hook docs.[m
 [m
[31m-This method differs from Hooks#prepend_before in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once before[m
[31m-the first example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#prepend_before in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once before the first example of the entire suite is executed.[m
 [m
 [m
   [m
[36m@@ -28669,29 +28686,20 @@[m [mend[m
       [m
   [m
   [m
[31m-    Turns deprecation warnings into errors, in order to surface[m
[31m-the full backtrace of the call site. This can be useful when[m
[31m-you need more context to address a deprecation than the[m
[31m-single-line call site normally provided.Turns deprecation warnings into errors, in order to surface[m
[31m-the full backtrace of the call site. This can be useful when[m
[31m-you need more context to address a deprecation than the[m
[31m-single-line call site normally provided.[m
[32m+[m[41m    [m
[32m+[m[32mTurns deprecation warnings into errors, in order to surface the full backtrace of the call site. This can be useful when you need more context to address a deprecation than the single-line call site normally provided.Turns deprecation warnings into errors, in order to surface the full backtrace of the call site. This can be useful when you need more context to address a deprecation than the single-line call site normally provided.[m
[32m+[m
 [m
   [m
     [m
[31m-    Turns deprecation warnings into errors, in order to surface[m
[31m-the full backtrace of the call site. This can be useful when[m
[31m-you need more context to address a deprecation than the[m
[31m-single-line call site normally provided.[m
[32m+[m[32mTurns deprecation warnings into errors, in order to surface the full backtrace of the call site. This can be useful when you need more context to address a deprecation than the single-line call site normally provided.[m
 [m
 [m
   [m
 [m
   [m
[31m-    Turns deprecation warnings into errors, in order to surface[m
[31m-the full backtrace of the call site. This can be useful when[m
[31m-you need more context to address a deprecation than the[m
[31m-single-line call site normally provided.[m
[32m+[m[41m    [m
[32m+[m[32mTurns deprecation warnings into errors, in order to surface the full backtrace of the call site. This can be useful when you need more context to address a deprecation than the single-line call site normally provided.[m
 [m
 [m
   [m
[36m@@ -28817,10 +28825,8 @@[m [mend[m
 [m
   [m
   [m
[31m-    Turns deprecation warnings into errors, in order to surface[m
[31m-the full backtrace of the call site. This can be useful when[m
[31m-you need more context to address a deprecation than the[m
[31m-single-line call site normally provided.[m
[32m+[m[41m    [m
[32m+[m[32mTurns deprecation warnings into errors, in order to surface the full backtrace of the call site. This can be useful when you need more context to address a deprecation than the single-line call site normally provided.[m
 [m
 [m
   [m
[36m@@ -28863,21 +28869,20 @@[m [mend[m
       [m
   [m
   [m
[31m-    Turns warnings into errors. This can be useful when[m
[31m-you want RSpec to run in a 'strict' no warning situation.Turns warnings into errors. This can be useful when[m
[31m-you want RSpec to run in a 'strict' no warning situation.[m
[32m+[m[41m    [m
[32m+[m[32mTurns warnings into errors. This can be useful when you want RSpec to run in a 'strict' no warning situation.Turns warnings into errors. This can be useful when you want RSpec to run in a 'strict' no warning situation.[m
[32m+[m
 [m
   [m
     [m
[31m-    Turns warnings into errors. This can be useful when[m
[31m-you want RSpec to run in a 'strict' no warning situation.[m
[32m+[m[32mTurns warnings into errors. This can be useful when you want RSpec to run in a 'strict' no warning situation.[m
 [m
 [m
   [m
 [m
   [m
[31m-    Turns warnings into errors. This can be useful when[m
[31m-you want RSpec to run in a 'strict' no warning situation.[m
[32m+[m[41m    [m
[32m+[m[32mTurns warnings into errors. This can be useful when you want RSpec to run in a 'strict' no warning situation.[m
 [m
 [m
   [m
[36m@@ -29043,8 +29048,8 @@[m [mend[m
 [m
   [m
   [m
[31m-    Turns warnings into errors. This can be useful when[m
[31m-you want RSpec to run in a 'strict' no warning situation.[m
[32m+[m[41m    [m
[32m+[m[32mTurns warnings into errors. This can be useful when you want RSpec to run in a 'strict' no warning situation.[m
 [m
 [m
   [m
[36m@@ -29098,43 +29103,33 @@[m [mend[m
     [m
   [m
     Note:Note:[m
[31m-    Pass the symbol :global:global to set the ordering strategy that[m
[31m-will be used to order the top-level example groups and any example[m
[31m-groups that do not have declared :order:order metadata.Pass the symbol :global to set the ordering strategy that[m
[31m-will be used to order the top-level example groups and any example[m
[31m-groups that do not have declared :order metadata.[m
[31m-Pass the symbol :global to set the ordering strategy that[m
[31m-will be used to order the top-level example groups and any example[m
[31m-groups that do not have declared :order metadata.[m
[32m+[m[41m    [m
[32m+[m[32mPass the symbol :global:global to set the ordering strategy that will be used to order the top-level example groups and any example groups that do not have declared :order:order metadata.Pass the symbol :global to set the ordering strategy that will be used to order the top-level example groups and any example groups that do not have declared :order metadata.[m
[32m+[m
[32m+[m[32mPass the symbol :global to set the ordering strategy that will be used to order the top-level example groups and any example groups that do not have declared :order metadata.[m
 [m
   [m
     Note:[m
[31m-    Pass the symbol :global to set the ordering strategy that[m
[31m-will be used to order the top-level example groups and any example[m
[31m-groups that do not have declared :order metadata.[m
[32m+[m[41m    [m
[32m+[m[32mPass the symbol :global to set the ordering strategy that will be used to order the top-level example groups and any example groups that do not have declared :order metadata.[m
[32m+[m
   [m
 [m
 [m
[31m-Registers a named ordering strategy that can later be[m
[31m-used to order an example group's subgroups by adding[m
[31m-:order => <name>:order => <name> metadata to the example group.Registers a named ordering strategy that can later be[m
[31m-used to order an example group's subgroups by adding[m
[31m-:order => <name> metadata to the example group.[m
[32m+[m[32mRegisters a named ordering strategy that can later be used to order an example group's subgroups by adding :order => <name>:order => <name> metadata to the example group.Registers a named ordering strategy that can later be used to order an example group's subgroups by adding :order => <name> metadata to the example group.[m
 [m
 [m
   [m
     [m
   [m
     Note:[m
[31m-    Pass the symbol :global to set the ordering strategy that[m
[31m-will be used to order the top-level example groups and any example[m
[31m-groups that do not have declared :order metadata.[m
[32m+[m[41m    [m
[32m+[m[32mPass the symbol :global to set the ordering strategy that will be used to order the top-level example groups and any example groups that do not have declared :order metadata.[m
 [m
   [m
 [m
[31m-Registers a named ordering strategy that can later be[m
[31m-used to order an example group's subgroups by adding[m
[31m-:order => <name> metadata to the example group.[m
[32m+[m
[32m+[m[32mRegisters a named ordering strategy that can later be used to order an example group's subgroups by adding :order => <name> metadata to the example group.[m
 [m
 [m
   [m
[36m@@ -29143,15 +29138,13 @@[m [mused to order an example group's subgroups by adding[m
     [m
   [m
     Note:[m
[31m-    Pass the symbol :global to set the ordering strategy that[m
[31m-will be used to order the top-level example groups and any example[m
[31m-groups that do not have declared :order metadata.[m
[32m+[m[41m    [m
[32m+[m[32mPass the symbol :global to set the ordering strategy that will be used to order the top-level example groups and any example groups that do not have declared :order metadata.[m
 [m
   [m
 [m
[31m-Registers a named ordering strategy that can later be[m
[31m-used to order an example group's subgroups by adding[m
[31m-:order => <name> metadata to the example group.[m
[32m+[m
[32m+[m[32mRegisters a named ordering strategy that can later be used to order an example group's subgroups by adding :order => <name> metadata to the example group.[m
 [m
 [m
   [m
[36m@@ -29632,15 +29645,13 @@[m [mdelegate_to_ordering_manager :register_ordering[m
     [m
   [m
     Note:[m
[31m-    Pass the symbol :global to set the ordering strategy that[m
[31m-will be used to order the top-level example groups and any example[m
[31m-groups that do not have declared :order metadata.[m
[32m+[m[41m    [m
[32m+[m[32mPass the symbol :global to set the ordering strategy that will be used to order the top-level example groups and any example groups that do not have declared :order metadata.[m
[32m+[m
   [m
 [m
 [m
[31m-Registers a named ordering strategy that can later be[m
[31m-used to order an example group's subgroups by adding[m
[31m-:order => <name> metadata to the example group.[m
[32m+[m[32mRegisters a named ordering strategy that can later be used to order an example group's subgroups by adding :order => <name> metadata to the example group.[m
 [m
 [m
   [m
[36m@@ -29908,8 +29931,7 @@[m [mReturns:[m
     [m
       # File 'lib/rspec/core/configuration.rb', line 1024# File 'lib/rspec/core/configuration.rb', line 1024[m
 [m
[31m-defdef reporterreporter[m
[31m-  # @reporter_buffer should only ever be set in this method to cover[m
[32m+[m[32mdefdef reporterreporter  # @reporter_buffer should only ever be set in this method to cover[m
 # @reporter_buffer should only ever be set in this method to cover[m
   # initialization of @reporter.[m
 # initialization of @reporter.[m
[36m@@ -29923,9 +29946,9 @@[m [mdefdef reporterreporter[m
     endend[m
 endend# File 'lib/rspec/core/configuration.rb', line 1024[m
 [m
[31m-def reporter[m
[31m-  # @reporter_buffer should only ever be set in this method to cover[m
[32m+[m[32mdef reporter  # @reporter_buffer should only ever be set in this method to cover[m
   # initialization of @reporter.[m
[32m+[m
   @reporter_buffer || @reporter ||=[m
     begin[m
       @reporter_buffer = DeprecationReporterBuffer.new[m
[36m@@ -29938,9 +29961,9 @@[m [mend[m
     [m
       # File 'lib/rspec/core/configuration.rb', line 1024[m
 [m
[31m-def reporter[m
[31m-  # @reporter_buffer should only ever be set in this method to cover[m
[32m+[m[32mdef reporter  # @reporter_buffer should only ever be set in this method to cover[m
   # initialization of @reporter.[m
[32m+[m
   @reporter_buffer || @reporter ||=[m
     begin[m
       @reporter_buffer = DeprecationReporterBuffer.new[m
[36m@@ -29972,9 +29995,9 @@[m [mend[m
     [m
       # File 'lib/rspec/core/configuration.rb', line 1024[m
 [m
[31m-def reporter[m
[31m-  # @reporter_buffer should only ever be set in this method to cover[m
[32m+[m[32mdef reporter  # @reporter_buffer should only ever be set in this method to cover[m
   # initialization of @reporter.[m
[32m+[m
   @reporter_buffer || @reporter ||=[m
     begin[m
       @reporter_buffer = DeprecationReporterBuffer.new[m
[36m@@ -30008,9 +30031,9 @@[m [mend[m
     [m
       # File 'lib/rspec/core/configuration.rb', line 1024[m
 [m
[31m-def reporter[m
[31m-  # @reporter_buffer should only ever be set in this method to cover[m
[32m+[m[32mdef reporter  # @reporter_buffer should only ever be set in this method to cover[m
   # initialization of @reporter.[m
[32m+[m
   @reporter_buffer || @reporter ||=[m
     begin[m
       @reporter_buffer = DeprecationReporterBuffer.new[m
[36m@@ -30073,9 +30098,9 @@[m [mReturns:[m
     [m
       # File 'lib/rspec/core/configuration.rb', line 1024[m
 [m
[31m-def reporter[m
[31m-  # @reporter_buffer should only ever be set in this method to cover[m
[32m+[m[32mdef reporter  # @reporter_buffer should only ever be set in this method to cover[m
   # initialization of @reporter.[m
[32m+[m
   @reporter_buffer || @reporter ||=[m
     begin[m
       @reporter_buffer = DeprecationReporterBuffer.new[m
[36m@@ -30093,37 +30118,22 @@[m [mend[m
       [m
   [m
   [m
[32m+[m[41m    [m
 Seed for random ordering (default: generated randomly each run).Seed for random ordering (default: generated randomly each run).[m
 [m
[31m-When you run specs with --order random--order random, RSpec generates a random seed[m
[31m-for the randomization and prints it to the output_streamoutput_stream (assuming[m
[31m-you're using RSpec's built-in formatters). If you discover an ordering[m
[31m-dependency (i.e. examples fail intermittently depending on order), set[m
[31m-this (on Configuration or on the command line with --seed--seed) to run[m
[31m-using the same seed while you debug the issue.When you run specs with --order random, RSpec generates a random seed[m
[31m-for the randomization and prints it to the output_stream (assuming[m
[31m-you're using RSpec's built-in formatters). If you discover an ordering[m
[31m-dependency (i.e. examples fail intermittently depending on order), set[m
[31m-this (on Configuration or on the command line with --seed) to run[m
[31m-using the same seed while you debug the issue.[m
[32m+[m[32mWhen you run specs with --order random--order random, RSpec generates a random seed for the randomization and prints it to the output_streamoutput_stream (assuming you're using RSpec's built-in formatters). If you discover an ordering dependency (i.e. examples fail intermittently depending on order), set this (on Configuration or on the command line with --seed--seed) to run using the same seed while you debug the issue.When you run specs with --order random, RSpec generates a random seed for the randomization and prints it to the output_stream (assuming you're using RSpec's built-in formatters). If you discover an ordering dependency (i.e. examples fail intermittently depending on order), set this (on Configuration or on the command line with --seed) to run using the same seed while you debug the issue.[m
[32m+[m
[32m+[m[32mWe recommend, actually, that you use the command line approach so you don't accidentally leave the seed encoded.We recommend, actually, that you use the command line approach so you don't accidentally leave the seed encoded.[m
 [m
[31m-We recommend, actually, that you use the command line approach so you[m
[31m-don't accidentally leave the seed encoded.We recommend, actually, that you use the command line approach so you[m
[31m-don't accidentally leave the seed encoded.[m
 [m
   [m
     [m
 Seed for random ordering (default: generated randomly each run).[m
 [m
[31m-When you run specs with --order random, RSpec generates a random seed[m
[31m-for the randomization and prints it to the output_stream (assuming[m
[31m-you're using RSpec's built-in formatters). If you discover an ordering[m
[31m-dependency (i.e. examples fail intermittently depending on order), set[m
[31m-this (on Configuration or on the command line with --seed) to run[m
[31m-using the same seed while you debug the issue.[m
[32m+[m[32mWhen you run specs with --order random, RSpec generates a random seed for the randomization and prints it to the output_stream (assuming you're using RSpec's built-in formatters). If you discover an ordering dependency (i.e. examples fail intermittently depending on order), set this (on Configuration or on the command line with --seed) to run using the same seed while you debug the issue.[m
[32m+[m
[32m+[m[32mWe recommend, actually, that you use the command line approach so you don't accidentally leave the seed encoded.[m
 [m
[31m-We recommend, actually, that you use the command line approach so you[m
[31m-don't accidentally leave the seed encoded.[m
 [m
   [m
 [m
[36m@@ -30131,15 +30141,9 @@[m [mdon't accidentally leave the seed encoded.[m
     [m
 Seed for random ordering (default: generated randomly each run).[m
 [m
[31m-When you run specs with --order random, RSpec generates a random seed[m
[31m-for the randomization and prints it to the output_stream (assuming[m
[31m-you're using RSpec's built-in formatters). If you discover an ordering[m
[31m-dependency (i.e. examples fail intermittently depending on order), set[m
[31m-this (on Configuration or on the command line with --seed) to run[m
[31m-using the same seed while you debug the issue.[m
[32m+[m[32mWhen you run specs with --order random, RSpec generates a random seed for the randomization and prints it to the output_stream (assuming you're using RSpec's built-in formatters). If you discover an ordering dependency (i.e. examples fail intermittently depending on order), set this (on Configuration or on the command line with --seed) to run using the same seed while you debug the issue.[m
 [m
[31m-We recommend, actually, that you use the command line approach so you[m
[31m-don't accidentally leave the seed encoded.[m
[32m+[m[32mWe recommend, actually, that you use the command line approach so you don't accidentally leave the seed encoded.[m
 [m
 [m
   [m
[36m@@ -30208,17 +30212,12 @@[m [mdelegate_to_ordering_manager :seed[m
 [m
   [m
   [m
[32m+[m[41m    [m
 Seed for random ordering (default: generated randomly each run).[m
 [m
[31m-When you run specs with --order random, RSpec generates a random seed[m
[31m-for the randomization and prints it to the output_stream (assuming[m
[31m-you're using RSpec's built-in formatters). If you discover an ordering[m
[31m-dependency (i.e. examples fail intermittently depending on order), set[m
[31m-this (on Configuration or on the command line with --seed) to run[m
[31m-using the same seed while you debug the issue.[m
[32m+[m[32mWhen you run specs with --order random, RSpec generates a random seed for the randomization and prints it to the output_stream (assuming you're using RSpec's built-in formatters). If you discover an ordering dependency (i.e. examples fail intermittently depending on order), set this (on Configuration or on the command line with --seed) to run using the same seed while you debug the issue.[m
 [m
[31m-We recommend, actually, that you use the command line approach so you[m
[31m-don't accidentally leave the seed encoded.[m
[32m+[m[32mWe recommend, actually, that you use the command line approach so you don't accidentally leave the seed encoded.[m
 [m
 [m
   [m
[36m@@ -30353,33 +30356,26 @@[m [mdelegate_to_ordering_manager :seed=[m
       [m
   [m
   [m
[31m-    Deprecated.Deprecated. This config option was added in RSpec 2 to pave the way[m
[31m-for this being the default behavior in RSpec 3. Now this option is[m
[31m-a no-op.This config option was added in RSpec 2 to pave the way[m
[31m-for this being the default behavior in RSpec 3. Now this option is[m
[31m-a no-op.[m
[31m-This config option was added in RSpec 2 to pave the way[m
[31m-for this being the default behavior in RSpec 3. Now this option is[m
[31m-a no-op.[m
[31m-Deprecated. This config option was added in RSpec 2 to pave the way[m
[31m-for this being the default behavior in RSpec 3. Now this option is[m
[31m-a no-op.[m
[32m+[m[32m    Deprecated.Deprecated.[m[41m [m
[32m+[m[32mThis config option was added in RSpec 2 to pave the way for this being the default behavior in RSpec 3. Now this option is a no-op.This config option was added in RSpec 2 to pave the way for this being the default behavior in RSpec 3. Now this option is a no-op.[m
[32m+[m
[32m+[m[32mThis config option was added in RSpec 2 to pave the way for this being the default behavior in RSpec 3. Now this option is a no-op.[m
[32m+[m[32mDeprecated.[m[41m [m
[32m+[m[32mThis config option was added in RSpec 2 to pave the way for this being the default behavior in RSpec 3. Now this option is a no-op.[m
 [m
 [m
 [m
   [m
[31m-    Deprecated. This config option was added in RSpec 2 to pave the way[m
[31m-for this being the default behavior in RSpec 3. Now this option is[m
[31m-a no-op.[m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mThis config option was added in RSpec 2 to pave the way for this being the default behavior in RSpec 3. Now this option is a no-op.[m
 [m
 [m
 [m
   [m
 [m
   [m
[31m-    Deprecated. This config option was added in RSpec 2 to pave the way[m
[31m-for this being the default behavior in RSpec 3. Now this option is[m
[31m-a no-op.[m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mThis config option was added in RSpec 2 to pave the way for this being the default behavior in RSpec 3. Now this option is a no-op.[m
 [m
 [m
 [m
[36m@@ -30519,9 +30515,8 @@[m [mend[m
 [m
   [m
   [m
[31m-    Deprecated. This config option was added in RSpec 2 to pave the way[m
[31m-for this being the default behavior in RSpec 3. Now this option is[m
[31m-a no-op.[m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mThis config option was added in RSpec 2 to pave the way for this being the default behavior in RSpec 3. Now this option is a no-op.[m
 [m
 [m
 [m
[36m@@ -30904,43 +30913,26 @@[m [mend[m
       [m
   [m
   [m
[31m-    Defines a callback that runs after the first example with matching[m
[31m-metadata is defined. If no examples are defined with matching metadata,[m
[31m-it will not get called at all.Defines a callback that runs after the first example with matching[m
[31m-metadata is defined. If no examples are defined with matching metadata,[m
[31m-it will not get called at all.[m
     [m
[31m-This can be used to ensure some setup is performed (such as bootstrapping[m
[31m-a DB or loading a specific file that adds significantly to the boot time)[m
[31m-if needed (as indicated by the presence of an example with matching metadata)[m
[31m-but avoided otherwise.This can be used to ensure some setup is performed (such as bootstrapping[m
[31m-a DB or loading a specific file that adds significantly to the boot time)[m
[31m-if needed (as indicated by the presence of an example with matching metadata)[m
[31m-but avoided otherwise.[m
[32m+[m[32mDefines a callback that runs after the first example with matching metadata is defined. If no examples are defined with matching metadata, it will not get called at all.Defines a callback that runs after the first example with matching metadata is defined. If no examples are defined with matching metadata, it will not get called at all.[m
[32m+[m
[32m+[m[32mThis can be used to ensure some setup is performed (such as bootstrapping a DB or loading a specific file that adds significantly to the boot time) if needed (as indicated by the presence of an example with matching metadata) but avoided otherwise.This can be used to ensure some setup is performed (such as bootstrapping a DB or loading a specific file that adds significantly to the boot time) if needed (as indicated by the presence of an example with matching metadata) but avoided otherwise.[m
 [m
 [m
   [m
[31m-    Defines a callback that runs after the first example with matching[m
[31m-metadata is defined. If no examples are defined with matching metadata,[m
[31m-it will not get called at all.[m
     [m
[31m-This can be used to ensure some setup is performed (such as bootstrapping[m
[31m-a DB or loading a specific file that adds significantly to the boot time)[m
[31m-if needed (as indicated by the presence of an example with matching metadata)[m
[31m-but avoided otherwise.[m
[32m+[m[32mDefines a callback that runs after the first example with matching metadata is defined. If no examples are defined with matching metadata, it will not get called at all.[m
 [m
[32m+[m[32mThis can be used to ensure some setup is performed (such as bootstrapping a DB or loading a specific file that adds significantly to the boot time) if needed (as indicated by the presence of an example with matching metadata) but avoided otherwise.[m
 [m
 [m
   [m
 [m
[31m-    Defines a callback that runs after the first example with matching[m
[31m-metadata is defined. If no examples are defined with matching metadata,[m
[31m-it will not get called at all.[m
   [m
[31m-This can be used to ensure some setup is performed (such as bootstrapping[m
[31m-a DB or loading a specific file that adds significantly to the boot time)[m
[31m-if needed (as indicated by the presence of an example with matching metadata)[m
[31m-but avoided otherwise.[m
[32m+[m[41m    [m
[32m+[m[32mDefines a callback that runs after the first example with matching metadata is defined. If no examples are defined with matching metadata, it will not get called at all.[m
[32m+[m
[32m+[m[32mThis can be used to ensure some setup is performed (such as bootstrapping a DB or loading a specific file that adds significantly to the boot time) if needed (as indicated by the presence of an example with matching metadata) but avoided otherwise.[m
 [m
 [m
   [m
[36m@@ -31222,14 +31214,10 @@[m [mend[m
 [m
   [m
   [m
[31m-    Defines a callback that runs after the first example with matching[m
[31m-metadata is defined. If no examples are defined with matching metadata,[m
[31m-it will not get called at all.[m
     [m
[31m-This can be used to ensure some setup is performed (such as bootstrapping[m
[31m-a DB or loading a specific file that adds significantly to the boot time)[m
[31m-if needed (as indicated by the presence of an example with matching metadata)[m
[31m-but avoided otherwise.[m
[32m+[m[32mDefines a callback that runs after the first example with matching metadata is defined. If no examples are defined with matching metadata, it will not get called at all.[m
[32m+[m
[32m+[m[32mThis can be used to ensure some setup is performed (such as bootstrapping a DB or loading a specific file that adds significantly to the boot time) if needed (as indicated by the presence of an example with matching metadata) but avoided otherwise.[m
 [m
 [m
   [m
[36m@@ -31325,11 +31314,8 @@[m [mend[m
       [m
       [m
         —[m
[31m-        formatter to use. Can be any of the[m
[31m-string values supported from the CLI (p/progress,[m
[31m-d/doc/documentation, h/html, or j/json), any[m
[31m-class that implements the formatter protocol and has registered[m
[31m-itself with RSpec as a formatter, or a formatter instance.[m
[32m+[m[41m        [m
[32m+[m[32mformatter to use. Can be any of the string values supported from the CLI (p/progress, d/doc/documentation, h/html, or j/json), any class that implements the formatter protocol and has registered itself with RSpec as a formatter, or a formatter instance.[m
 [m
       [m
     [m
[36m@@ -31346,9 +31332,8 @@[m [mitself with RSpec as a formatter, or a formatter instance.[m
       [m
       [m
         —[m
[31m-        where the formatter will write its output.[m
[31m-Can be an IO object or a string path to a file. If not provided,[m
[31m-the configured output_stream ($stdout, by default) will be used.[m
[32m+[m[41m        [m
[32m+[m[32mwhere the formatter will write its output. Can be an IO object or a string path to a file. If not provided, the configured output_stream ($stdout, by default) will be used.[m
 [m
       [m
     [m
[36m@@ -31392,8 +31378,7 @@[m [mend[m
 RSpec.configuration.add_setting :foo[m
 [m
 [m
[31m-Used internally and by extension frameworks like rspec-rails, so they[m
[31m-can add config settings that are domain specific. For example:.[m
[32m+[m[32mUsed internally and by extension frameworks like rspec-rails, so they can add config settings that are domain specific. For example:.[m
 [m
 RSpec.configure do |c|[m
   c.add_setting :use_transactional_fixtures,[m
[36m@@ -31402,8 +31387,7 @@[m [mRSpec.configure do |c|[m
 end[m
 [m
 [m
[31m-add_setting creates three methods on the configuration object, a[m
[31m-setter, a getter, and a predicate:.[m
[32m+[m[32madd_setting creates three methods on the configuration object, a setter, a getter, and a predicate:.[m
 [m
 RSpec.configuration.foo=(value)[m
 RSpec.configuration.foo[m
[36m@@ -31451,7 +31436,8 @@[m [mRSpec.configuration.foo? # Returns true if foo returns anything but nil or false[m
             [m
           [m
           [m
[31m-            — Set a default value for the generated getter and predicate methods:[m
[32m+[m[32m            —[m[41m [m
[32m+[m[32mSet a default value for the generated getter and predicate methods:[m
 [m
 add_setting(:foo, :default => "default value")[m
 [m
[36m@@ -31466,8 +31452,8 @@[m [madd_setting(:foo, :default => "default value")[m
             [m
           [m
           [m
[31m-            — Use :alias_with to alias the setter, getter, and predicate to[m
[31m-another name, or names:[m
[32m+[m[32m            —[m[41m [m
[32m+[m[32mUse :alias_with to alias the setter, getter, and predicate to another name, or names:[m
 [m
 add_setting(:foo, :alias_with => :bar)[m
 add_setting(:foo, :alias_with => [:bar, :baz])[m
[36m@@ -31512,11 +31498,10 @@[m [mend[m
       [m
   [m
   [m
[32m+[m[41m    [m
 Defines a after hook. See Hooks#after for full docs.[m
 [m
[31m-This method differs from Hooks#after in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once after[m
[31m-the last example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#after in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once after the last example of the entire suite is executed.[m
 [m
 [m
   [m
[36m@@ -31584,14 +31569,13 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    The defined aliased will also be added to the top level[m
[31m-(e.g. main and from within modules) if[m
[31m-expose_dsl_globally is set to true.[m
[32m+[m[41m    [m
[32m+[m[32mThe defined aliased will also be added to the top level (e.g. main and from within modules) if expose_dsl_globally is set to true.[m
[32m+[m
   [m
 [m
 [m
[31m-Creates a method that defines an example group with the provided[m
[31m-metadata. Can be used to define example group/metadata shortcuts.[m
[32m+[m[32mCreates a method that defines an example group with the provided metadata. Can be used to define example group/metadata shortcuts.[m
 [m
 [m
   [m
[36m@@ -31661,22 +31645,20 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    The specific example alias below (pending) is already[m
[31m-defined for you.[m
[32m+[m[41m    [m
[32m+[m[32mThe specific example alias below (pending) is already defined for you.[m
 [m
   [m
 [m
   [m
     Note:[m
[31m-    Use with caution. This extends the language used in your[m
[31m-specs, but does not add any additional documentation. We use this[m
[31m-in RSpec to define methods like focus and xit, but we also add[m
[31m-docs for those methods.[m
[32m+[m[41m    [m
[32m+[m[32mUse with caution. This extends the language used in your specs, but does not add any additional documentation. We use this in RSpec to define methods like focus and xit, but we also add docs for those methods.[m
[32m+[m
   [m
 [m
 [m
[31m-Creates a method that delegates to example including the submitted[m
[31m-args. Used internally to add variants of example like pending:.[m
[32m+[m[32mCreates a method that delegates to example including the submitted args. Used internally to add variants of example like pending:.[m
 [m
 [m
   [m
[36m@@ -31773,16 +31757,13 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    Use with caution. This extends the language used in your[m
[31m-specs, but does not add any additional documentation. We use this[m
[31m-in RSpec to define it_should_behave_like (for backward[m
[31m-compatibility), but we also add docs for that method.[m
[32m+[m[41m    [m
[32m+[m[32mUse with caution. This extends the language used in your specs, but does not add any additional documentation. We use this in RSpec to define it_should_behave_like (for backward compatibility), but we also add docs for that method.[m
[32m+[m
   [m
 [m
 [m
[31m-Define an alias for it_should_behave_like that allows different[m
[31m-language (like "it_has_behavior" or "it_behaves_like") to be[m
[31m-employed when including shared examples.[m
[32m+[m[32mDefine an alias for it_should_behave_like that allows different language (like “it_has_behavior” or “it_behaves_like”) to be employed when including shared examples.[m
 [m
 [m
   [m
[36m@@ -31837,15 +31818,12 @@[m [mend[m
       [m
   [m
   [m
[31m-    Adds block to the end of the list of after blocks in the same[m
[31m-scope (:example, :context, or :suite), in contrast to #after,[m
[31m-which adds the hook to the start of the list.[m
[32m+[m[41m    [m
[32m+[m[32mAdds block to the end of the list of after blocks in the same scope (:example, :context, or :suite), in contrast to #after, which adds the hook to the start of the list.[m
 [m
 See Hooks#after for full after hook docs.[m
 [m
[31m-This method differs from Hooks#append_after in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once after[m
[31m-the last example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#append_after in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once after the last example of the entire suite is executed.[m
 [m
 [m
   [m
[36m@@ -31958,17 +31937,14 @@[m [mend[m
       [m
   [m
   [m
[32m+[m[41m    [m
 Regexps used to exclude lines from backtraces.[m
 [m
[31m-Excludes lines from ruby (and jruby) source, installed gems, anything[m
[31m-in any "bin" directory, and any of the RSpec libs (outside gem[m
[31m-installs) by default.[m
[32m+[m[32mExcludes lines from ruby (and jruby) source, installed gems, anything in any “bin” directory, and any of the RSpec libs (outside gem installs) by default.[m
 [m
 You can modify the list via the getter, or replace it with the setter.[m
 [m
[31m-To override this behaviour and display a full backtrace, use[m
[31m---backtrace on the command line, in a .rspec file, or in the[m
[31m-rspec_options attribute of RSpec's rake task.[m
[32m+[m[32mTo override this behaviour and display a full backtrace, use --backtrace on the command line, in a .rspec file, or in the rspec_options attribute of RSpec's rake task.[m
 [m
 [m
   [m
[36m@@ -32068,8 +32047,7 @@[m [mend[m
 [m
 Defaults to [Regexp.new Dir.getwd].[m
 [m
[31m-Lines that match an exclusion and an inclusion pattern[m
[31m-will be included.[m
[32m+[m[32mLines that match an exclusion and an inclusion pattern will be included.[m
 [m
 You can modify the list via the getter, or replace it with the setter.[m
 [m
[36m@@ -32148,11 +32127,10 @@[m [mend[m
       [m
   [m
   [m
[32m+[m[41m    [m
 Defines a before hook. See Hooks#before for full docs.[m
 [m
[31m-This method differs from Hooks#before in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once before[m
[31m-the first example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#before in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once before the first example of the entire suite is executed.[m
 [m
 [m
   [m
[36m@@ -32238,8 +32217,8 @@[m [mend[m
       [m
       [m
         —[m
[31m-        an output stream to use, defaults to the current[m
[31m-output_stream[m
[32m+[m[41m        [m
[32m+[m[32man output stream to use, defaults to the current output_stream[m
 [m
       [m
     [m
[36m@@ -32294,8 +32273,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    The formatter that will be used if no formatter has been set.[m
[31m-Defaults to 'progress'.[m
[32m+[m[41m    [m
[32m+[m[32mThe formatter that will be used if no formatter has been set. Defaults to 'progress'.[m
 [m
 [m
   [m
[36m@@ -32403,10 +32384,8 @@[m [mParameters:[m
       [m
       [m
         —[m
[31m-        metadata filters that determine[m
[31m-which example or group metadata hashes the callback will be triggered[m
[31m-for. If none are given, the callback will be run against the metadata[m
[31m-hashes of all groups and examples.[m
[32m+[m[41m        [m
[32m+[m[32mmetadata filters that determine which example or group metadata hashes the callback will be triggered for. If none are given, the callback will be run against the metadata hashes of all groups and examples.[m
 [m
       [m
     [m
[36m@@ -32426,8 +32405,8 @@[m [mYield Parameters:[m
       [m
       [m
         —[m
[31m-        original metadata hash from an example or[m
[31m-group. Mutate this in your block as needed.[m
[32m+[m[41m        [m
[32m+[m[32moriginal metadata hash from an example or group. Mutate this in your block as needed.[m
 [m
       [m
     [m
[36m@@ -32460,8 +32439,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Determines where deprecation warnings are printed.[m
[31m-Defaults to $stderr.[m
[32m+[m[41m    [m
[32m+[m[32mDetermines where deprecation warnings are printed. Defaults to $stderr.[m
 [m
 [m
   [m
[36m@@ -32576,27 +32558,20 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    It configures rspec-mocks and rspec-expectations only[m
[31m-if the user is using those (either explicitly or implicitly[m
[31m-by not setting mock_with or expect_with to anything else).[m
[32m+[m[41m    [m
[32m+[m[32mIt configures rspec-mocks and rspec-expectations only if the user is using those (either explicitly or implicitly by not setting mock_with or expect_with to anything else).[m
 [m
   [m
 [m
   [m
     Note:[m
[31m-    If the user uses this options with mock_with :mocha[m
[31m-(or similiar) they will still have monkey patching active[m
[31m-in their test environment from mocha.[m
[32m+[m[41m    [m
[32m+[m[32mIf the user uses this options with mock_with :mocha (or similiar) they will still have monkey patching active in their test environment from mocha.[m
 [m
   [m
 [m
[31m-Enables zero monkey patching mode for RSpec. It removes monkey[m
[31m-patching of the top-level DSL methods (describe,[m
[31m-shared_examples_for, etc) onto main and Module, instead[m
[31m-requiring you to prefix these methods with RSpec.. It enables[m
[31m-expect-only syntax for rspec-mocks and rspec-expectations. It[m
[31m-simply disables monkey patching on whatever pieces of RSpec[m
[31m-the user is using.[m
[32m+[m
[32m+[m[32mEnables zero monkey patching mode for RSpec. It removes monkey patching of the top-level DSL methods (describe, shared_examples_for, etc) onto main and Module, instead requiring you to prefix these methods with RSpec.. It enables expect-only syntax for rspec-mocks and rspec-expectations. It simply disables monkey patching on whatever pieces of RSpec the user is using.[m
 [m
 [m
   [m
[36m@@ -32660,8 +32635,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Returns the exclusion_filter. If none has been set, returns an empty[m
[31m-hash.[m
[32m+[m[41m    [m
[32m+[m[32mReturns the exclusion_filter. If none has been set, returns an empty hash.[m
 [m
 [m
   [m
[36m@@ -32693,13 +32668,12 @@[m [mend[m
       [m
   [m
   [m
[31m-    Clears and reassigns the exclusion_filter. Set to nil if you don't[m
[31m-want any exclusion filter at all.[m
[32m+[m[41m    [m
[32m+[m[32mClears and reassigns the exclusion_filter. Set to nil if you don't want any exclusion filter at all.[m
 [m
 Warning[m
 [m
[31m-This overrides any exclusion filters/tags set on the command line or in[m
[31m-configuration files.[m
[32m+[m[32mThis overrides any exclusion filters/tags set on the command line or in configuration files.[m
 [m
 [m
   [m
[36m@@ -32733,11 +32707,10 @@[m [mend[m
       [m
   [m
   [m
[31m-    Sets the expectation framework module(s) to be included in each example[m
[31m-group.[m
     [m
[31m-frameworks can be :rspec, :test_unit, :minitest, a custom[m
[31m-module, or any combination thereof:.[m
[32m+[m[32mSets the expectation framework module(s) to be included in each example group.[m
[32m+[m
[32m+[m[32mframeworks can be :rspec, :test_unit, :minitest, a custom module, or any combination thereof:.[m
 [m
 config.expect_with :rspec[m
 config.expect_with :test_unit[m
[36m@@ -32746,13 +32719,11 @@[m [mconfig.expect_with :rspec, :minitest[m
 config.expect_with OtherExpectationFramework[m
 [m
 [m
[31m-RSpec will translate :rspec, :minitest, and :test_unit into the[m
[31m-appropriate modules.[m
[32m+[m[32mRSpec will translate :rspec, :minitest, and :test_unit into the appropriate modules.[m
 [m
 Configuration[m
 [m
[31m-If the module responds to configuration, expect_with will[m
[31m-yield the configuration object if given a block:.[m
[32m+[m[32mIf the module responds to configuration, expect_with will yield the configuration object if given a block:.[m
 [m
 config.expect_with OtherExpectationFramework do |custom_config|[m
   custom_config.custom_setting = true[m
[36m@@ -32943,13 +32916,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Exposes the current running example via the named[m
[31m-helper method. RSpec 2.x exposed this via example,[m
[31m-but in RSpec 3.0, the example is instead exposed via[m
[31m-an arg yielded to it, before, let, etc. However,[m
[31m-some extension gems (such as Capybara) depend on the[m
[31m-RSpec 2.x's example method, so this config option[m
[31m-can be used to maintain compatibility.[m
[32m+[m[41m    [m
[32m+[m[32mExposes the current running example via the named helper method. RSpec 2.x exposed this via example, but in RSpec 3.0, the example is instead exposed via an arg yielded to it, before, let, etc. However, some extension gems (such as Capybara) depend on the RSpec 2.x's example method, so this config option can be used to maintain compatibility.[m
 [m
 [m
   [m
[36m@@ -33028,10 +32997,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Use this to expose the core RSpec DSL via Module and the main[m
[31m-object. It will be set automatically but you can override it to[m
[31m-remove the DSL.[m
[31m-Default: true.[m
[32m+[m[41m    [m
[32m+[m[32mUse this to expose the core RSpec DSL via Module and the main object. It will be set automatically but you can override it to remove the DSL. Default: true.[m
 [m
 [m
   [m
[36m@@ -33075,8 +33042,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Indicates if the DSL has been exposed off of modules and main.[m
[31m-Default: true.[m
[32m+[m[41m    [m
[32m+[m[32mIndicates if the DSL has been exposed off of modules and main. Default: true.[m
 [m
 [m
   [m
[36m@@ -33121,13 +33088,10 @@[m [mend[m
       [m
   [m
   [m
[31m-    Tells RSpec to extend example groups with mod. Methods defined in[m
[31m-mod are exposed to example groups (not examples). Use filters to[m
[31m-constrain the groups to extend.[m
     [m
[31m-Similar to include, but behavior is added to example groups, which[m
[31m-are classes, rather than the examples, which are instances of those[m
[31m-classes.[m
[32m+[m[32mTells RSpec to extend example groups with mod. Methods defined in mod are exposed to example groups (not examples). Use filters to constrain the groups to extend.[m
[32m+[m
[32m+[m[32mSimilar to include, but behavior is added to example groups, which are classes, rather than the examples, which are instances of those classes.[m
 [m
 [m
   [m
[36m@@ -33140,8 +33104,8 @@[m [mclasses.[m
       [m
       [m
 module UiHelpers[m
[31m-  def run_in_browser[m
[31m-    # ...[m
[32m+[m[32m  def run_in_browser    # ...[m
[32m+[m
   end[m
 end[m
 [m
[36m@@ -33200,17 +33164,13 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    The patterns this adds will match the named gems in their common[m
[31m-locations (e.g. system gems, vendored with bundler, installed as a[m
[31m-:git dependency with bundler, etc) but is not guaranteed to work for[m
[31m-all possible gem locations. For example, if you have the gem source[m
[31m-in a directory with a completely unrelated name, and use bundler's[m
[31m-:path option, this will not filter it.[m
     [m
[32m+[m[32mThe patterns this adds will match the named gems in their common locations (e.g. system gems, vendored with bundler, installed as a :git dependency with bundler, etc) but is not guaranteed to work for all possible gem locations. For example, if you have the gem source in a directory with a completely unrelated name, and use bundler's :path option, this will not filter it.[m
 [m
   [m
[31m-Adds #backtrace_exclusion_patterns that will filter lines from[m
[31m-the named gems from backtraces.[m
[32m+[m
[32m+[m
[32m+[m[32mAdds #backtrace_exclusion_patterns that will filter lines from the named gems from backtraces.[m
 [m
 [m
   [m
[36m@@ -33275,14 +33236,12 @@[m [mend[m
       [m
   [m
   [m
[31m-    Adds key/value pairs to the exclusion_filter. If args[m
[31m-includes any symbols that are not part of the hash, each symbol[m
[31m-is treated as a key in the hash with the value true.[m
[32m+[m[41m    [m
[32m+[m[32mAdds key/value pairs to the exclusion_filter. If args includes any symbols that are not part of the hash, each symbol is treated as a key in the hash with the value true.[m
 [m
 Note[m
 [m
[31m-Filters set using this method can be overridden from the command line[m
[31m-or config files (e.g. .rspec).[m
[32m+[m[32mFilters set using this method can be overridden from the command line or config files (e.g. .rspec).[m
 [m
 [m
   [m
[36m@@ -33345,14 +33304,12 @@[m [mend[m
       [m
   [m
   [m
[31m-    Adds key/value pairs to the inclusion_filter. If args[m
[31m-includes any symbols that are not part of the hash, each symbol[m
[31m-is treated as a key in the hash with the value true.[m
[32m+[m[41m    [m
[32m+[m[32mAdds key/value pairs to the inclusion_filter. If args includes any symbols that are not part of the hash, each symbol is treated as a key in the hash with the value true.[m
 [m
 Note[m
 [m
[31m-Filters set using this method can be overridden from the command line[m
[31m-or config files (e.g. .rspec).[m
[32m+[m[32mFilters set using this method can be overridden from the command line or config files (e.g. .rspec).[m
 [m
 [m
   [m
[36m@@ -33415,15 +33372,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Applies the provided filter only if any of examples match, in constrast[m
[31m-to #filter_run, which always applies even if no examples match, in[m
[31m-which case no examples will be run. This allows you to leave configured[m
[31m-filters in place that are intended only for temporary use. The most common[m
[31m-example is focus filtering: config.filter_run_when_matching :focus.[m
[31m-With that configured, you can temporarily focus an example or group[m
[31m-by tagging it with :focus metadata, or prefixing it with an f[m
[31m-(as in fdescribe, fcontext and fit) since those are aliases for[m
[31m-describe/context/it with :focus metadata.[m
[32m+[m[41m    [m
[32m+[m[32mApplies the provided filter only if any of examples match, in constrast to #filter_run, which always applies even if no examples match, in which case no examples will be run. This allows you to leave configured filters in place that are intended only for temporary use. The most common example is focus filtering: config.filter_run_when_matching :focus. With that configured, you can temporarily focus an example or group by tagging it with :focus metadata, or prefixing it with an f (as in fdescribe, fcontext and fit) since those are aliases for describe/context/it with :focus metadata.[m
 [m
 [m
   [m
[36m@@ -33503,8 +33454,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Returns a duplicate of the formatters currently loaded in[m
[31m-the FormatterLoader for introspection.[m
[32m+[m[41m    [m
[32m+[m[32mReturns a duplicate of the formatters currently loaded in the FormatterLoader for introspection.[m
 [m
 Note as this is a duplicate, any mutations will be disregarded.[m
 [m
[36m@@ -33739,18 +33698,13 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    Filtered module inclusions can also be applied to[m
[31m-individual examples that have matching metadata. Just like[m
[31m-Ruby's object model is that every object has a singleton class[m
[31m-which has only a single instance, RSpec's model is that every[m
[31m-example has a singleton example group containing just the one[m
[31m-example.[m
[32m+[m[41m    [m
[32m+[m[32mFiltered module inclusions can also be applied to individual examples that have matching metadata. Just like Ruby's object model is that every object has a singleton class which has only a single instance, RSpec's model is that every example has a singleton example group containing just the one example.[m
[32m+[m
   [m
 [m
 [m
[31m-Tells RSpec to include mod in example groups. Methods defined in[m
[31m-mod are exposed to examples (not example groups). Use filters to[m
[31m-constrain the groups or examples in which to include the module.[m
[32m+[m[32mTells RSpec to include mod in example groups. Methods defined in mod are exposed to examples (not example groups). Use filters to constrain the groups or examples in which to include the module.[m
 [m
 [m
   [m
[36m@@ -33832,18 +33786,13 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    Filtered context inclusions can also be applied to[m
[31m-individual examples that have matching metadata. Just like[m
[31m-Ruby's object model is that every object has a singleton class[m
[31m-which has only a single instance, RSpec's model is that every[m
[31m-example has a singleton example group containing just the one[m
[31m-example.[m
     [m
[32m+[m[32mFiltered context inclusions can also be applied to individual examples that have matching metadata. Just like Ruby's object model is that every object has a singleton class which has only a single instance, RSpec's model is that every example has a singleton example group containing just the one example.[m
 [m
   [m
[31m-Tells RSpec to include the named shared example group in example groups.[m
[31m-Use filters to constrain the groups or examples in which to include[m
[31m-the example group.[m
[32m+[m
[32m+[m
[32m+[m[32mTells RSpec to include the named shared example group in example groups. Use filters to constrain the groups or examples in which to include the example group.[m
 [m
 [m
   [m
[36m@@ -33914,8 +33863,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Returns the inclusion_filter. If none has been set, returns an empty[m
[31m-hash.[m
[32m+[m[41m    [m
[32m+[m[32mReturns the inclusion_filter. If none has been set, returns an empty hash.[m
 [m
 [m
   [m
[36m@@ -33947,13 +33896,12 @@[m [mend[m
       [m
   [m
   [m
[31m-    Clears and reassigns the inclusion_filter. Set to nil if you don't[m
[31m-want any inclusion filter at all.[m
[32m+[m[41m    [m
[32m+[m[32mClears and reassigns the inclusion_filter. Set to nil if you don't want any inclusion filter at all.[m
 [m
 Warning[m
 [m
[31m-This overrides any inclusion filters/tags set on the command line or in[m
[31m-configuration files.[m
[32m+[m[32mThis overrides any inclusion filters/tags set on the command line or in configuration files.[m
 [m
 [m
   [m
[36m@@ -34082,14 +34033,11 @@[m [mend[m
 [m
 framework can be a Symbol or a Module.[m
 [m
[31m-Given any of :rspec, :mocha, :flexmock, or :rr, configures the[m
[31m-named framework.[m
[32m+[m[32mGiven any of :rspec, :mocha, :flexmock, or :rr, configures the named framework.[m
 [m
[31m-Given :nothing, configures no framework. Use this if you don't use[m
[31m-any mocking framework to save a little bit of overhead.[m
[32m+[m[32mGiven :nothing, configures no framework. Use this if you don't use any mocking framework to save a little bit of overhead.[m
 [m
[31m-Given a Module, includes that module in every example group. The module[m
[31m-should adhere to RSpec's mock framework adapter API:.[m
[32m+[m[32mGiven a Module, includes that module in every example group. The module should adhere to RSpec's mock framework adapter API:.[m
 [m
 setup_mocks_for_rspec[m
   - called before each example[m
[36m@@ -34102,8 +34050,7 @@[m [mteardown_mocks_for_rspec[m
   - called after verify_mocks_for_rspec (even if there are errors)[m
 [m
 [m
[31m-If the module responds to configuration and mock_with receives a[m
[31m-block, it will yield the configuration object to the block e.g.[m
[32m+[m[32mIf the module responds to configuration and mock_with receives a block, it will yield the configuration object to the block e.g.[m
 [m
 config.mock_with OtherMockFrameworkAdapter do |mod_config|[m
   mod_config.custom_setting = true[m
[36m@@ -34264,10 +34213,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Sets the default global ordering strategy. By default this can be one[m
[31m-of :defined, :random, but is customizable through the[m
[31m-register_ordering API. If order is set to 'rand:<seed>',[m
[31m-the seed will also be set.[m
[32m+[m[41m    [m
[32m+[m[32mSets the default global ordering strategy. By default this can be one of :defined, :random, but is customizable through the register_ordering API. If order is set to 'rand:<seed>', the seed will also be set.[m
 [m
 [m
   [m
[36m@@ -34302,12 +34249,10 @@[m [mdelegate_to_ordering_manager :order=[m
       [m
   [m
   [m
[31m-    Tells RSpec to prepend example groups with mod. Methods defined in[m
[31m-mod are exposed to examples (not example groups). Use filters to[m
[31m-constrain the groups in which to prepend the module.[m
     [m
[31m-Similar to include, but module is included before the example group's class[m
[31m-in the ancestor chain.[m
[32m+[m[32mTells RSpec to prepend example groups with mod. Methods defined in mod are exposed to examples (not example groups). Use filters to constrain the groups in which to prepend the module.[m
[32m+[m
[32m+[m[32mSimilar to include, but module is included before the example group's class in the ancestor chain.[m
 [m
 [m
   [m
[36m@@ -34380,15 +34325,12 @@[m [mend[m
       [m
   [m
   [m
[31m-    Adds block to the start of the list of before blocks in the same[m
[31m-scope (:example, :context, or :suite), in contrast to #before,[m
[31m-which adds the hook to the end of the list.[m
[32m+[m[41m    [m
[32m+[m[32mAdds block to the start of the list of before blocks in the same scope (:example, :context, or :suite), in contrast to #before, which adds the hook to the end of the list.[m
 [m
 See Hooks#before for full before hook docs.[m
 [m
[31m-This method differs from Hooks#prepend_before in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once before[m
[31m-the first example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#prepend_before in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once before the first example of the entire suite is executed.[m
 [m
 [m
   [m
[36m@@ -34453,10 +34395,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Turns deprecation warnings into errors, in order to surface[m
[31m-the full backtrace of the call site. This can be useful when[m
[31m-you need more context to address a deprecation than the[m
[31m-single-line call site normally provided.[m
[32m+[m[41m    [m
[32m+[m[32mTurns deprecation warnings into errors, in order to surface the full backtrace of the call site. This can be useful when you need more context to address a deprecation than the single-line call site normally provided.[m
 [m
 [m
   [m
[36m@@ -34499,8 +34439,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Turns warnings into errors. This can be useful when[m
[31m-you want RSpec to run in a 'strict' no warning situation.[m
[32m+[m[41m    [m
[32m+[m[32mTurns warnings into errors. This can be useful when you want RSpec to run in a 'strict' no warning situation.[m
 [m
 [m
   [m
[36m@@ -34554,15 +34494,13 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    Pass the symbol :global to set the ordering strategy that[m
[31m-will be used to order the top-level example groups and any example[m
[31m-groups that do not have declared :order metadata.[m
     [m
[32m+[m[32mPass the symbol :global to set the ordering strategy that will be used to order the top-level example groups and any example groups that do not have declared :order metadata.[m
 [m
   [m
[31m-Registers a named ordering strategy that can later be[m
[31m-used to order an example group's subgroups by adding[m
[31m-:order => <name> metadata to the example group.[m
[32m+[m
[32m+[m
[32m+[m[32mRegisters a named ordering strategy that can later be used to order an example group's subgroups by adding :order => <name> metadata to the example group.[m
 [m
 [m
   [m
[36m@@ -34743,9 +34687,9 @@[m [mReturns:[m
     [m
       # File 'lib/rspec/core/configuration.rb', line 1024[m
 [m
[31m-def reporter[m
[31m-  # @reporter_buffer should only ever be set in this method to cover[m
[32m+[m[32mdef reporter  # @reporter_buffer should only ever be set in this method to cover[m
   # initialization of @reporter.[m
[32m+[m
   @reporter_buffer || @reporter ||=[m
     begin[m
       @reporter_buffer = DeprecationReporterBuffer.new[m
[36m@@ -34763,17 +34707,12 @@[m [mend[m
       [m
   [m
   [m
[32m+[m[41m    [m
 Seed for random ordering (default: generated randomly each run).[m
 [m
[31m-When you run specs with --order random, RSpec generates a random seed[m
[31m-for the randomization and prints it to the output_stream (assuming[m
[31m-you're using RSpec's built-in formatters). If you discover an ordering[m
[31m-dependency (i.e. examples fail intermittently depending on order), set[m
[31m-this (on Configuration or on the command line with --seed) to run[m
[31m-using the same seed while you debug the issue.[m
[32m+[m[32mWhen you run specs with --order random, RSpec generates a random seed for the randomization and prints it to the output_stream (assuming you're using RSpec's built-in formatters). If you discover an ordering dependency (i.e. examples fail intermittently depending on order), set this (on Configuration or on the command line with --seed) to run using the same seed while you debug the issue.[m
 [m
[31m-We recommend, actually, that you use the command line approach so you[m
[31m-don't accidentally leave the seed encoded.[m
[32m+[m[32mWe recommend, actually, that you use the command line approach so you don't accidentally leave the seed encoded.[m
 [m
 [m
   [m
[36m@@ -34829,9 +34769,8 @@[m [mdelegate_to_ordering_manager :seed=[m
       [m
   [m
   [m
[31m-    Deprecated. This config option was added in RSpec 2 to pave the way[m
[31m-for this being the default behavior in RSpec 3. Now this option is[m
[31m-a no-op.[m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mThis config option was added in RSpec 2 to pave the way for this being the default behavior in RSpec 3. Now this option is a no-op.[m
 [m
 [m
 [m
[36m@@ -34955,14 +34897,10 @@[m [mend[m
       [m
   [m
   [m
[31m-    Defines a callback that runs after the first example with matching[m
[31m-metadata is defined. If no examples are defined with matching metadata,[m
[31m-it will not get called at all.[m
     [m
[31m-This can be used to ensure some setup is performed (such as bootstrapping[m
[31m-a DB or loading a specific file that adds significantly to the boot time)[m
[31m-if needed (as indicated by the presence of an example with matching metadata)[m
[31m-but avoided otherwise.[m
[32m+[m[32mDefines a callback that runs after the first example with matching metadata is defined. If no examples are defined with matching metadata, it will not get called at all.[m
[32m+[m
[32m+[m[32mThis can be used to ensure some setup is performed (such as bootstrapping a DB or loading a specific file that adds significantly to the boot time) if needed (as indicated by the presence of an example with matching metadata) but avoided otherwise.[m
 [m
 [m
   [m
[36m@@ -35081,28 +35019,27 @@[m [mClass: RSpec::Core::Configuration[m
 [m
 Overview[m
   [m
[32m+[m[41m    [m
 Stores runtime configuration information.[m
 [m
[31m-Configuration options are loaded from multiple files and joined together[m
[31m-with command-line switches and the SPEC_OPTS environment variable.[m
[32m+[m[32mConfiguration options are loaded from multiple files and joined together with command-line switches and the SPEC_OPTS environment variable.[m
[32m+[m
[32m+[m[32mPrecedence order (where later entries overwrite earlier entries on conflicts):.[m
[32m+[m
[32m+[m[32mGlobal ($XDG_CONFIG_HOME/rspec/options, or ~/.rspec if it does not exist).[m
 [m
[31m-Precedence order (where later entries overwrite earlier entries on[m
[31m-conflicts):.[m
[32m+[m[32mProject-specific (./.rspec).[m
 [m
[32m+[m[32mLocal (./.rspec-local).[m
 [m
[31m-Global ($XDG_CONFIG_HOME/rspec/options, or ~/.rspec if it does[m
[31m-not exist)[m
[31m-Project-specific (./.rspec)[m
[31m-Local (./.rspec-local)[m
[31m-Command-line options[m
[31m-SPEC_OPTS[m
[32m+[m[32mCommand-line options.[m
 [m
[32m+[m[32mSPEC_OPTS.[m
 [m
[31m-For example, an option set in the local file will override an option set[m
[31m-in your global file.[m
 [m
[31m-The global, project-specific and local files can all be overridden with a[m
[31m-separate custom file using the --options command-line parameter.[m
[32m+[m[32mFor example, an option set in the local file will override an option set in your global file.[m
[32m+[m
[32m+[m[32mThe global, project-specific and local files can all be overridden with a separate custom file using the –options command-line parameter.[m
 [m
 [m
   [m
[36m@@ -35185,9 +35125,9 @@[m [mend[m
   [m
 [m
   [m
[31m-    Deprecated. No longer recommended because of complex behavior. Instead,[m
[31m-rely on the fact that TTYs will display color by default, or set[m
[31m-#color_mode to :on to display color on a non-TTY output.[m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mNo longer recommended because of complex behavior. Instead, rely on the fact that TTYs will display color by default, or set #color_mode to :on to display color on a non-TTY output.[m
[32m+[m
   [m
 [m
 [m
[36m@@ -35613,7 +35572,9 @@[m [mrely on the fact that TTYs will display color by default, or set[m
   [m
 [m
   [m
[31m-    Load files matching this pattern (default: '**{,/*/**}/*_spec.rb').[m
[32m+[m[41m    [m
[32m+[m[32mLoad files matching this pattern (default: '**{,//*}/*_spec.rb').[m
[32m+[m
   [m
 [m
 [m
[36m@@ -35715,8 +35679,9 @@[m [mrely on the fact that TTYs will display color by default, or set[m
   [m
 [m
   [m
[31m-    Deprecated. Use #filter_run_when_matching instead for the specific[m
[31m-filters that you want to be ignored if none match.[m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mUse #filter_run_when_matching instead for the specific filters that you want to be ignored if none match.[m
[32m+[m
   [m
 [m
 [m
[36m@@ -35911,7 +35884,9 @@[m [mfilters that you want to be ignored if none match.[m
   [m
 [m
   [m
[31m-    Define an alias for it_should_behave_like that allows different language (like "it_has_behavior" or "it_behaves_like") to be employed when including shared examples.[m
[32m+[m[41m    [m
[32m+[m[32mDefine an alias for it_should_behave_like that allows different language (like “it_has_behavior” or “it_behaves_like”) to be employed when including shared examples.[m
[32m+[m
   [m
 [m
 [m
[36m@@ -36814,9 +36840,9 @@[m [mfilters that you want to be ignored if none match.[m
   [m
 [m
   [m
[31m-    Deprecated. This config option was added in RSpec 2 to pave the way[m
[31m-for this being the default behavior in RSpec 3. Now this option is[m
[31m-a no-op.[m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mThis config option was added in RSpec 2 to pave the way for this being the default behavior in RSpec 3. Now this option is a no-op.[m
[32m+[m
   [m
 [m
 [m
[36m@@ -36962,10 +36991,10 @@[m [ma no-op.[m
     [m
       # File 'lib/rspec/core/configuration.rb', line 500[m
 [m
[31m-def initialize[m
[31m-  # rubocop:disable Style/GlobalVars[m
[31m-  @start_time = $_rspec_core_load_started_at || ::RSpec::Core::Time.now[m
[31m-  # rubocop:enable Style/GlobalVars[m
[32m+[m[32mdef initialize  # rubocop:disable Style/GlobalVars[m
[32m+[m
[32m+[m[32m  @start_time = $_rspec_core_load_started_at || ::RSpec::Core::Time.now  # rubocop:enable Style/GlobalVars[m
[32m+[m
   @expectation_frameworks = [][m
   @include_modules = FilterableItemRepository::QueryOptimized.new(:any?)[m
   @extend_modules  = FilterableItemRepository::QueryOptimized.new(:any?)[m
[36m@@ -37035,26 +37064,17 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    This option will only be used by --bisect if you set it in a file[m
[31m-loaded via --require.[m
     [m
[32m+[m[32mThis option will only be used by --bisect if you set it in a file loaded via --require.[m
 [m
   [m
[31m-Determines which bisect runner implementation gets used to run subsets[m
[31m-of the suite during a bisection. Your choices are:.[m
 [m
 [m
[31m-:shell: Performs a spec run by shelling out, booting RSpec and your[m
[31m-application environment each time. This runner is the most widely[m
[31m-compatible runner, but is not as fast. On platforms that do not[m
[31m-support forking, this is the default.[m
[31m-:fork: Pre-boots RSpec and your application environment in a parent[m
[31m-process, and then forks a child process for each spec run. This runner[m
[31m-tends to be significantly faster than the :shell runner but cannot[m
[31m-be used in some situations. On platforms that support forking, this[m
[31m-is the default. If you use this runner, you should ensure that all[m
[31m-of your one-time setup logic goes in a before(:suite) hook instead[m
[31m-of getting run at the top-level of a file loaded by --require.[m
[32m+[m[32mDetermines which bisect runner implementation gets used to run subsets of the suite during a bisection. Your choices are:.[m
[32m+[m
[32m+[m[32m:shell: Performs a spec run by shelling out, booting RSpec and your application environment each time. This runner is the most widely compatible runner, but is not as fast. On platforms that do not support forking, this is the default.[m
[32m+[m
[32m+[m[32m:fork: Pre-boots RSpec and your application environment in a parent process, and then forks a child process for each spec run. This runner tends to be significantly faster than the :shell runner but cannot be used in some situations. On platforms that support forking, this is the default. If you use this runner, you should ensure that all of your one-time setup logic goes in a before(:suite) hook instead of getting run at the top-level of a file loaded by --require.[m
 [m
 [m
 [m
[36m@@ -37102,13 +37122,11 @@[m [mend[m
       [m
   [m
   [m
[31m-    Deprecated. No longer recommended because of complex behavior. Instead,[m
[31m-rely on the fact that TTYs will display color by default, or set[m
[31m-#color_mode to :on to display color on a non-TTY output.[m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mNo longer recommended because of complex behavior. Instead, rely on the fact that TTYs will display color by default, or set #color_mode to :on to display color on a non-TTY output.[m
 [m
[31m-Enables color output if the output is a TTY.  As of RSpec 3.6, this is[m
[31m-the default behavior and this option is retained only for backwards[m
[31m-compatibility.[m
[32m+[m
[32m+[m[32mEnables color output if the output is a TTY. As of RSpec 3.6, this is the default behavior and this option is retained only for backwards compatibility.[m
 [m
 [m
   [m
[36m@@ -37164,13 +37182,14 @@[m [mend[m
       [m
   [m
   [m
[32m+[m[41m    [m
 The mode for determining whether to display output in color. One of:.[m
 [m
[32m+[m[32m:automatic - the output will be in color if the output is a TTY (the  default).[m
 [m
[31m-:automatic - the output will be in color if the output is a TTY (the[m
[31m-default)[m
[31m-:on - the output will be in color, whether or not the output is a TTY[m
[31m-:off - the output will not be in color[m
[32m+[m[32m:on - the output will be in color, whether or not the output is a TTY.[m
[32m+[m
[32m+[m[32m:off - the output will not be in color.[m
 [m
 [m
 [m
[36m@@ -37225,9 +37244,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    The default output color. Defaults to :white but can be set to one of[m
[31m-the following: [:black, :white, :red, :green, :yellow, :blue,[m
[31m-:magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mThe default output color. Defaults to :white but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -37273,14 +37291,13 @@[m [madd_setting :default_color[m
     [m
   [m
     Note:[m
[31m-    Other scripts invoking rspec indirectly will ignore this[m
[31m-setting.[m
[32m+[m[41m    [m
[32m+[m[32mOther scripts invoking rspec indirectly will ignore this setting.[m
 [m
   [m
 [m
[31m-Path to use if no path is provided to the rspec command (default:[m
[31m-"spec"). Allows you to just type rspec instead of rspec spec to[m
[31m-run all the examples in the spec directory.[m
[32m+[m
[32m+[m[32mPath to use if no path is provided to the rspec command (default: "spec"). Allows you to just type rspec instead of rspec spec to run all the examples in the spec directory.[m
 [m
 [m
   [m
[36m@@ -37323,9 +37340,8 @@[m [madd_read_only_setting :default_path[m
       [m
   [m
   [m
[31m-    Color used to print details.  Defaults to :cyan but can be set to one[m
[31m-of the following: [:black, :white, :red, :green, :yellow, :blue,[m
[31m-:magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mColor used to print details. Defaults to :cyan but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -37368,8 +37384,8 @@[m [madd_setting :detail_color[m
       [m
   [m
   [m
[31m-    Run examples over DRb (default: false). RSpec doesn't supply the DRb[m
[31m-server, but you can use tools like spork.[m
[32m+[m[41m    [m
[32m+[m[32mRun examples over DRb (default: false). RSpec doesn't supply the DRb server, but you can use tools like spork.[m
 [m
 [m
   [m
[36m@@ -37442,8 +37459,8 @@[m [madd_setting :drb_port[m
       [m
   [m
   [m
[31m-    Prints the formatter output of your suite without running any[m
[31m-examples or hooks.[m
[32m+[m[41m    [m
[32m+[m[32mPrints the formatter output of your suite without running any examples or hooks.[m
 [m
 [m
   [m
[36m@@ -37503,8 +37521,8 @@[m [madd_setting :error_stream[m
       [m
   [m
   [m
[31m-    The file path to use for persisting example statuses. Necessary for the[m
[31m---only-failures and --next-failure CLI options.[m
[32m+[m[41m    [m
[32m+[m[32mThe file path to use for persisting example statuses. Necessary for the --only-failures and --next-failure CLI options.[m
 [m
 [m
   [m
[36m@@ -37649,9 +37671,8 @@[m [mdefine_reader :exclude_pattern[m
       [m
   [m
   [m
[31m-    If specified, indicates the number of failures required before cleaning[m
[31m-up and exit (default: nil). Can also be true to fail and exit on first[m
[31m-failure.[m
[32m+[m[41m    [m
[32m+[m[32mIf specified, indicates the number of failures required before cleaning up and exit (default: nil). Can also be true to fail and exit on first failure.[m
 [m
 [m
   [m
[36m@@ -37724,9 +37746,8 @@[m [madd_setting :fail_if_no_examples[m
       [m
   [m
   [m
[31m-    Color to use to indicate failure.  Defaults to :red but can be set to[m
[31m-one of the following: [:black, :white, :red, :green, :yellow, :blue,[m
[31m-:magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mColor to use to indicate failure. Defaults to :red but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -37863,9 +37887,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Color used when a pending example is fixed. Defaults to :blue but can[m
[31m-be set to one of the following: [:black, :white, :red, :green,[m
[31m-:yellow, :blue, :magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mColor used when a pending example is fixed. Defaults to :blue but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -37908,8 +37931,8 @@[m [madd_setting :fixed_color[m
       [m
   [m
   [m
[31m-    Returns dirs that have been prepended to the load path by the -I[m
[31m-command line option.[m
[32m+[m[41m    [m
[32m+[m[32mReturns dirs that have been prepended to the load path by the -I command line option.[m
 [m
 [m
   [m
[36m@@ -37952,9 +37975,8 @@[m [mdefine_reader :libs[m
       [m
   [m
   [m
[31m-    Maximum count of failed source lines to display in the failure reports.[m
[31m-(default 10).[m
[31m-return [Integer].[m
[32m+[m[41m    [m
[32m+[m[32mMaximum count of failed source lines to display in the failure reports. (default 10). return [Integer].[m
 [m
 [m
   [m
[36m@@ -38014,8 +38037,8 @@[m [mdefine_reader :only_failures[m
       [m
   [m
   [m
[31m-    Determines where RSpec will send its output.[m
[31m-Default: $stdout.[m
[32m+[m[41m    [m
[32m+[m[32mDetermines where RSpec will send its output. Default: $stdout.[m
 [m
 [m
   [m
[36m@@ -38058,7 +38081,8 @@[m [mdefine_reader :output_stream[m
       [m
   [m
   [m
[31m-    Load files matching this pattern (default: '**{,/*/**}/*_spec.rb').[m
[32m+[m[41m    [m
[32m+[m[32mLoad files matching this pattern (default: '**{,//*}/*_spec.rb').[m
 [m
 [m
   [m
[36m@@ -38101,9 +38125,8 @@[m [mdefine_reader :pattern[m
       [m
   [m
   [m
[31m-    Color to use to print pending examples.  Defaults to :yellow but can[m
[31m-be set to one of the following: [:black, :white, :red, :green,[m
[31m-:yellow, :blue, :magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mColor to use to print pending examples. Defaults to :yellow but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -38150,8 +38173,8 @@[m [madd_setting :pending_color[m
   This method is part of a private API.[m
   You should avoid using this method if possible, as it may be removed or be changed in the future.[m
 [m
[31m-Defaults profile_examples to 10 examples when @profile_examples is[m
[31m-true.[m
[32m+[m
[32m+[m[32mDefaults profile_examples to 10 examples when @profile_examples is true.[m
 [m
 [m
   [m
[36m@@ -38181,12 +38204,8 @@[m [madd_setting :profile_examples[m
       [m
   [m
   [m
[31m-    Specifies which directories contain the source code for your project.[m
[31m-When a failure occurs, RSpec looks through the backtrace to find a[m
[31m-a line of source to print. It first looks for a line coming from[m
[31m-one of the project source directories so that, for example, it prints[m
[31m-the expectation or assertion call rather than the source code from[m
[31m-the expectation or assertion framework.[m
[32m+[m[41m    [m
[32m+[m[32mSpecifies which directories contain the source code for your project. When a failure occurs, RSpec looks through the backtrace to find a a line of source to print. It first looks for a line coming from one of the project source directories so that, for example, it prints the expectation or assertion call rather than the source code from the expectation or assertion framework.[m
 [m
 [m
   [m
[36m@@ -38272,11 +38292,11 @@[m [mdefine_reader :requires[m
       [m
   [m
   [m
[31m-    Deprecated. Use #filter_run_when_matching instead for the specific[m
[31m-filters that you want to be ignored if none match.[m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mUse #filter_run_when_matching instead for the specific filters that you want to be ignored if none match.[m
 [m
[31m-Run all examples if none match the configured filters[m
[31m-(default: false).[m
[32m+[m
[32m+[m[32mRun all examples if none match the configured filters (default: false).[m
 [m
 [m
   [m
[36m@@ -38306,8 +38326,8 @@[m [madd_setting :run_all_when_everything_filtered[m
       [m
   [m
   [m
[31m-    Configures how RSpec treats metadata passed as part of a shared example[m
[31m-group definition. For example, given this shared example group definition:.[m
[32m+[m[41m    [m
[32m+[m[32mConfigures how RSpec treats metadata passed as part of a shared example group definition. For example, given this shared example group definition:.[m
 [m
 RSpec.shared_context "uses DB", :db => true do[m
   around(:example) do |ex|[m
[36m@@ -38316,35 +38336,28 @@[m [mRSpec.shared_context "uses DB", :db => true do[m
 end[m
 [m
 [m
[31m-...there are two ways RSpec can treat the :db => true metadata, each[m
[31m-of which has a corresponding config option:.[m
[32m+[m[32m…there are two ways RSpec can treat the :db => true metadata, each of which has a corresponding config option:.[m
 [m
[32m+[m[32m:trigger_inclusion: this shared context will be implicitly included  in any groups (or examples) that have :db => true metadata.[m
 [m
[31m-:trigger_inclusion: this shared context will be implicitly included[m
[31m-in any groups (or examples) that have :db => true metadata.[m
[31m-:apply_to_host_groups: the metadata will be inherited by the metadata[m
[31m-hash of all host groups and examples.[m
[32m+[m[32m:apply_to_host_groups: the metadata will be inherited by the metadata  hash of all host groups and examples.[m
 [m
 [m
[31m-:trigger_inclusion is the legacy behavior from before RSpec 3.5 but should[m
[31m-be considered deprecated. Instead, you can explicitly include a group with[m
[31m-include_context:.[m
[32m+[m[32m:trigger_inclusion is the legacy behavior from before RSpec 3.5 but should be considered deprecated. Instead, you can explicitly include a group with include_context:.[m
 [m
 RSpec.describe "My model" do[m
   include_context "uses DB"[m
 end[m
 [m
 [m
[31m-...or you can configure RSpec to include the context based on matching metadata[m
[31m-using an API that mirrors configured module inclusion:.[m
[32m+[m[32m…or you can configure RSpec to include the context based on matching metadata using an API that mirrors configured module inclusion:.[m
 [m
 RSpec.configure do |rspec|[m
   rspec.include_context "uses DB", :db => true[m
 end[m
 [m
 [m
[31m-:apply_to_host_groups is a new feature of RSpec 3.5 and will be the only[m
[31m-supported behavior in RSpec 4.[m
[32m+[m[32m:apply_to_host_groups is a new feature of RSpec 3.5 and will be the only supported behavior in RSpec 4.[m
 [m
 [m
   [m
[36m@@ -38446,9 +38462,8 @@[m [mdefine_reader :shared_context_metadata_behavior[m
       [m
   [m
   [m
[31m-    Don't print filter info i.e. "Run options: include :focus=>true"[m
[31m-(default false).[m
[31m-return [Boolean].[m
[32m+[m[41m    [m
[32m+[m[32mDon't print filter info i.e. “Run options: include :focus=>true” (default false). return [Boolean].[m
 [m
 [m
   [m
[36m@@ -38478,9 +38493,8 @@[m [madd_setting :silence_filter_announcements[m
       [m
   [m
   [m
[31m-    Color to use to indicate success.  Defaults to :green but can be set[m
[31m-to one of the following: [:black, :white, :red, :green, :yellow,[m
[31m-:blue, :magenta, :cyan].[m
[32m+[m[41m    [m
[32m+[m[32mColor to use to indicate success. Defaults to :green but can be set to one of the following: [:black, :white, :red, :green, :yellow, :blue, :magenta, :cyan].[m
 [m
 [m
   [m
[36m@@ -38523,9 +38537,8 @@[m [madd_setting :success_color[m
       [m
   [m
   [m
[31m-    Use threadsafe options where available.[m
[31m-Currently this will place a mutex around memoized values such as let blocks.[m
[31m-return [Boolean].[m
[32m+[m[41m    [m
[32m+[m[32mUse threadsafe options where available. Currently this will place a mutex around memoized values such as let blocks. return [Boolean].[m
 [m
 [m
   [m
[36m@@ -38579,11 +38593,8 @@[m [madd_setting :threadsafe[m
       [m
       [m
         —[m
[31m-        formatter to use. Can be any of the[m
[31m-string values supported from the CLI (p/progress,[m
[31m-d/doc/documentation, h/html, or j/json), any[m
[31m-class that implements the formatter protocol and has registered[m
[31m-itself with RSpec as a formatter, or a formatter instance.[m
[32m+[m[41m        [m
[32m+[m[32mformatter to use. Can be any of the string values supported from the CLI (p/progress, d/doc/documentation, h/html, or j/json), any class that implements the formatter protocol and has registered itself with RSpec as a formatter, or a formatter instance.[m
 [m
       [m
     [m
[36m@@ -38600,9 +38611,8 @@[m [mitself with RSpec as a formatter, or a formatter instance.[m
       [m
       [m
         —[m
[31m-        where the formatter will write its output.[m
[31m-Can be an IO object or a string path to a file. If not provided,[m
[31m-the configured output_stream ($stdout, by default) will be used.[m
[32m+[m[41m        [m
[32m+[m[32mwhere the formatter will write its output. Can be an IO object or a string path to a file. If not provided, the configured output_stream ($stdout, by default) will be used.[m
 [m
       [m
     [m
[36m@@ -38646,8 +38657,7 @@[m [mend[m
 RSpec.configuration.add_setting :foo[m
 [m
 [m
[31m-Used internally and by extension frameworks like rspec-rails, so they[m
[31m-can add config settings that are domain specific. For example:.[m
[32m+[m[32mUsed internally and by extension frameworks like rspec-rails, so they can add config settings that are domain specific. For example:.[m
 [m
 RSpec.configure do |c|[m
   c.add_setting :use_transactional_fixtures,[m
[36m@@ -38656,8 +38666,7 @@[m [mRSpec.configure do |c|[m
 end[m
 [m
 [m
[31m-add_setting creates three methods on the configuration object, a[m
[31m-setter, a getter, and a predicate:.[m
[32m+[m[32madd_setting creates three methods on the configuration object, a setter, a getter, and a predicate:.[m
 [m
 RSpec.configuration.foo=(value)[m
 RSpec.configuration.foo[m
[36m@@ -38705,7 +38715,8 @@[m [mRSpec.configuration.foo? # Returns true if foo returns anything but nil or false[m
             [m
           [m
           [m
[31m-            — Set a default value for the generated getter and predicate methods:[m
[32m+[m[32m            —[m[41m [m
[32m+[m[32mSet a default value for the generated getter and predicate methods:[m
 [m
 add_setting(:foo, :default => "default value")[m
 [m
[36m@@ -38720,8 +38731,8 @@[m [madd_setting(:foo, :default => "default value")[m
             [m
           [m
           [m
[31m-            — Use :alias_with to alias the setter, getter, and predicate to[m
[31m-another name, or names:[m
[32m+[m[32m            —[m[41m [m
[32m+[m[32mUse :alias_with to alias the setter, getter, and predicate to another name, or names:[m
 [m
 add_setting(:foo, :alias_with => :bar)[m
 add_setting(:foo, :alias_with => [:bar, :baz])[m
[36m@@ -38766,11 +38777,10 @@[m [mend[m
       [m
   [m
   [m
[32m+[m[41m    [m
 Defines a after hook. See Hooks#after for full docs.[m
 [m
[31m-This method differs from Hooks#after in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once after[m
[31m-the last example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#after in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once after the last example of the entire suite is executed.[m
 [m
 [m
   [m
[36m@@ -38838,14 +38848,13 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    The defined aliased will also be added to the top level[m
[31m-(e.g. main and from within modules) if[m
[31m-expose_dsl_globally is set to true.[m
[32m+[m[41m    [m
[32m+[m[32mThe defined aliased will also be added to the top level (e.g. main and from within modules) if expose_dsl_globally is set to true.[m
[32m+[m
   [m
 [m
 [m
[31m-Creates a method that defines an example group with the provided[m
[31m-metadata. Can be used to define example group/metadata shortcuts.[m
[32m+[m[32mCreates a method that defines an example group with the provided metadata. Can be used to define example group/metadata shortcuts.[m
 [m
 [m
   [m
[36m@@ -38915,22 +38924,20 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    The specific example alias below (pending) is already[m
[31m-defined for you.[m
[32m+[m[41m    [m
[32m+[m[32mThe specific example alias below (pending) is already defined for you.[m
 [m
   [m
 [m
   [m
     Note:[m
[31m-    Use with caution. This extends the language used in your[m
[31m-specs, but does not add any additional documentation. We use this[m
[31m-in RSpec to define methods like focus and xit, but we also add[m
[31m-docs for those methods.[m
[32m+[m[41m    [m
[32m+[m[32mUse with caution. This extends the language used in your specs, but does not add any additional documentation. We use this in RSpec to define methods like focus and xit, but we also add docs for those methods.[m
 [m
   [m
 [m
[31m-Creates a method that delegates to example including the submitted[m
[31m-args. Used internally to add variants of example like pending:.[m
[32m+[m
[32m+[m[32mCreates a method that delegates to example including the submitted args. Used internally to add variants of example like pending:.[m
 [m
 [m
   [m
[36m@@ -39027,16 +39036,13 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    Use with caution. This extends the language used in your[m
[31m-specs, but does not add any additional documentation. We use this[m
[31m-in RSpec to define it_should_behave_like (for backward[m
[31m-compatibility), but we also add docs for that method.[m
[32m+[m[41m    [m
[32m+[m[32mUse with caution. This extends the language used in your specs, but does not add any additional documentation. We use this in RSpec to define it_should_behave_like (for backward compatibility), but we also add docs for that method.[m
[32m+[m
   [m
 [m
 [m
[31m-Define an alias for it_should_behave_like that allows different[m
[31m-language (like "it_has_behavior" or "it_behaves_like") to be[m
[31m-employed when including shared examples.[m
[32m+[m[32mDefine an alias for it_should_behave_like that allows different language (like “it_has_behavior” or “it_behaves_like”) to be employed when including shared examples.[m
 [m
 [m
   [m
[36m@@ -39091,15 +39097,12 @@[m [mend[m
       [m
   [m
   [m
[31m-    Adds block to the end of the list of after blocks in the same[m
[31m-scope (:example, :context, or :suite), in contrast to #after,[m
[31m-which adds the hook to the start of the list.[m
[32m+[m[41m    [m
[32m+[m[32mAdds block to the end of the list of after blocks in the same scope (:example, :context, or :suite), in contrast to #after, which adds the hook to the start of the list.[m
 [m
 See Hooks#after for full after hook docs.[m
 [m
[31m-This method differs from Hooks#append_after in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once after[m
[31m-the last example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#append_after in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once after the last example of the entire suite is executed.[m
 [m
 [m
   [m
[36m@@ -39212,17 +39216,14 @@[m [mend[m
       [m
   [m
   [m
[32m+[m[41m    [m
 Regexps used to exclude lines from backtraces.[m
 [m
[31m-Excludes lines from ruby (and jruby) source, installed gems, anything[m
[31m-in any "bin" directory, and any of the RSpec libs (outside gem[m
[31m-installs) by default.[m
[32m+[m[32mExcludes lines from ruby (and jruby) source, installed gems, anything in any “bin” directory, and any of the RSpec libs (outside gem installs) by default.[m
 [m
 You can modify the list via the getter, or replace it with the setter.[m
 [m
[31m-To override this behaviour and display a full backtrace, use[m
[31m---backtrace on the command line, in a .rspec file, or in the[m
[31m-rspec_options attribute of RSpec's rake task.[m
[32m+[m[32mTo override this behaviour and display a full backtrace, use --backtrace on the command line, in a .rspec file, or in the rspec_options attribute of RSpec's rake task.[m
 [m
 [m
   [m
[36m@@ -39322,8 +39326,7 @@[m [mend[m
 [m
 Defaults to [Regexp.new Dir.getwd].[m
 [m
[31m-Lines that match an exclusion and an inclusion pattern[m
[31m-will be included.[m
[32m+[m[32mLines that match an exclusion and an inclusion pattern will be included.[m
 [m
 You can modify the list via the getter, or replace it with the setter.[m
 [m
[36m@@ -39402,11 +39406,10 @@[m [mend[m
       [m
   [m
   [m
[32m+[m[41m    [m
 Defines a before hook. See Hooks#before for full docs.[m
 [m
[31m-This method differs from Hooks#before in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once before[m
[31m-the first example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#before in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once before the first example of the entire suite is executed.[m
 [m
 [m
   [m
[36m@@ -39492,8 +39496,8 @@[m [mend[m
       [m
       [m
         —[m
[31m-        an output stream to use, defaults to the current[m
[31m-output_stream[m
[32m+[m[41m        [m
[32m+[m[32man output stream to use, defaults to the current output_stream[m
 [m
       [m
     [m
[36m@@ -39548,8 +39552,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    The formatter that will be used if no formatter has been set.[m
[31m-Defaults to 'progress'.[m
[32m+[m[41m    [m
[32m+[m[32mThe formatter that will be used if no formatter has been set. Defaults to 'progress'.[m
 [m
 [m
   [m
[36m@@ -39657,10 +39663,8 @@[m [mParameters:[m
       [m
       [m
         —[m
[31m-        metadata filters that determine[m
[31m-which example or group metadata hashes the callback will be triggered[m
[31m-for. If none are given, the callback will be run against the metadata[m
[31m-hashes of all groups and examples.[m
[32m+[m[41m        [m
[32m+[m[32mmetadata filters that determine which example or group metadata hashes the callback will be triggered for. If none are given, the callback will be run against the metadata hashes of all groups and examples.[m
 [m
       [m
     [m
[36m@@ -39680,8 +39684,8 @@[m [mYield Parameters:[m
       [m
       [m
         —[m
[31m-        original metadata hash from an example or[m
[31m-group. Mutate this in your block as needed.[m
[32m+[m[41m        [m
[32m+[m[32moriginal metadata hash from an example or group. Mutate this in your block as needed.[m
 [m
       [m
     [m
[36m@@ -39714,8 +39718,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Determines where deprecation warnings are printed.[m
[31m-Defaults to $stderr.[m
[32m+[m[41m    [m
[32m+[m[32mDetermines where deprecation warnings are printed. Defaults to $stderr.[m
 [m
 [m
   [m
[36m@@ -39830,27 +39837,20 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    It configures rspec-mocks and rspec-expectations only[m
[31m-if the user is using those (either explicitly or implicitly[m
[31m-by not setting mock_with or expect_with to anything else).[m
[32m+[m[41m    [m
[32m+[m[32mIt configures rspec-mocks and rspec-expectations only if the user is using those (either explicitly or implicitly by not setting mock_with or expect_with to anything else).[m
 [m
   [m
 [m
   [m
     Note:[m
[31m-    If the user uses this options with mock_with :mocha[m
[31m-(or similiar) they will still have monkey patching active[m
[31m-in their test environment from mocha.[m
     [m
[32m+[m[32mIf the user uses this options with mock_with :mocha (or similiar) they will still have monkey patching active in their test environment from mocha.[m
 [m
   [m
[31m-Enables zero monkey patching mode for RSpec. It removes monkey[m
[31m-patching of the top-level DSL methods (describe,[m
[31m-shared_examples_for, etc) onto main and Module, instead[m
[31m-requiring you to prefix these methods with RSpec.. It enables[m
[31m-expect-only syntax for rspec-mocks and rspec-expectations. It[m
[31m-simply disables monkey patching on whatever pieces of RSpec[m
[31m-the user is using.[m
[32m+[m
[32m+[m
[32m+[m[32mEnables zero monkey patching mode for RSpec. It removes monkey patching of the top-level DSL methods (describe, shared_examples_for, etc) onto main and Module, instead requiring you to prefix these methods with RSpec.. It enables expect-only syntax for rspec-mocks and rspec-expectations. It simply disables monkey patching on whatever pieces of RSpec the user is using.[m
 [m
 [m
   [m
[36m@@ -39914,8 +39914,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Returns the exclusion_filter. If none has been set, returns an empty[m
[31m-hash.[m
[32m+[m[41m    [m
[32m+[m[32mReturns the exclusion_filter. If none has been set, returns an empty hash.[m
 [m
 [m
   [m
[36m@@ -39947,13 +39947,12 @@[m [mend[m
       [m
   [m
   [m
[31m-    Clears and reassigns the exclusion_filter. Set to nil if you don't[m
[31m-want any exclusion filter at all.[m
[32m+[m[41m    [m
[32m+[m[32mClears and reassigns the exclusion_filter. Set to nil if you don't want any exclusion filter at all.[m
 [m
 Warning[m
 [m
[31m-This overrides any exclusion filters/tags set on the command line or in[m
[31m-configuration files.[m
[32m+[m[32mThis overrides any exclusion filters/tags set on the command line or in configuration files.[m
 [m
 [m
   [m
[36m@@ -39987,11 +39986,10 @@[m [mend[m
       [m
   [m
   [m
[31m-    Sets the expectation framework module(s) to be included in each example[m
[31m-group.[m
     [m
[31m-frameworks can be :rspec, :test_unit, :minitest, a custom[m
[31m-module, or any combination thereof:.[m
[32m+[m[32mSets the expectation framework module(s) to be included in each example group.[m
[32m+[m
[32m+[m[32mframeworks can be :rspec, :test_unit, :minitest, a custom module, or any combination thereof:.[m
 [m
 config.expect_with :rspec[m
 config.expect_with :test_unit[m
[36m@@ -40000,13 +39998,11 @@[m [mconfig.expect_with :rspec, :minitest[m
 config.expect_with OtherExpectationFramework[m
 [m
 [m
[31m-RSpec will translate :rspec, :minitest, and :test_unit into the[m
[31m-appropriate modules.[m
[32m+[m[32mRSpec will translate :rspec, :minitest, and :test_unit into the appropriate modules.[m
 [m
 Configuration[m
 [m
[31m-If the module responds to configuration, expect_with will[m
[31m-yield the configuration object if given a block:.[m
[32m+[m[32mIf the module responds to configuration, expect_with will yield the configuration object if given a block:.[m
 [m
 config.expect_with OtherExpectationFramework do |custom_config|[m
   custom_config.custom_setting = true[m
[36m@@ -40197,13 +40195,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Exposes the current running example via the named[m
[31m-helper method. RSpec 2.x exposed this via example,[m
[31m-but in RSpec 3.0, the example is instead exposed via[m
[31m-an arg yielded to it, before, let, etc. However,[m
[31m-some extension gems (such as Capybara) depend on the[m
[31m-RSpec 2.x's example method, so this config option[m
[31m-can be used to maintain compatibility.[m
[32m+[m[41m    [m
[32m+[m[32mExposes the current running example via the named helper method. RSpec 2.x exposed this via example, but in RSpec 3.0, the example is instead exposed via an arg yielded to it, before, let, etc. However, some extension gems (such as Capybara) depend on the RSpec 2.x's example method, so this config option can be used to maintain compatibility.[m
 [m
 [m
   [m
[36m@@ -40282,10 +40276,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Use this to expose the core RSpec DSL via Module and the main[m
[31m-object. It will be set automatically but you can override it to[m
[31m-remove the DSL.[m
[31m-Default: true.[m
[32m+[m[41m    [m
[32m+[m[32mUse this to expose the core RSpec DSL via Module and the main object. It will be set automatically but you can override it to remove the DSL. Default: true.[m
 [m
 [m
   [m
[36m@@ -40329,8 +40321,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Indicates if the DSL has been exposed off of modules and main.[m
[31m-Default: true.[m
[32m+[m[41m    [m
[32m+[m[32mIndicates if the DSL has been exposed off of modules and main. Default: true.[m
 [m
 [m
   [m
[36m@@ -40375,13 +40367,10 @@[m [mend[m
       [m
   [m
   [m
[31m-    Tells RSpec to extend example groups with mod. Methods defined in[m
[31m-mod are exposed to example groups (not examples). Use filters to[m
[31m-constrain the groups to extend.[m
     [m
[31m-Similar to include, but behavior is added to example groups, which[m
[31m-are classes, rather than the examples, which are instances of those[m
[31m-classes.[m
[32m+[m[32mTells RSpec to extend example groups with mod. Methods defined in mod are exposed to example groups (not examples). Use filters to constrain the groups to extend.[m
[32m+[m
[32m+[m[32mSimilar to include, but behavior is added to example groups, which are classes, rather than the examples, which are instances of those classes.[m
 [m
 [m
   [m
[36m@@ -40394,8 +40383,8 @@[m [mclasses.[m
       [m
       [m
 module UiHelpers[m
[31m-  def run_in_browser[m
[31m-    # ...[m
[32m+[m[32m  def run_in_browser    # ...[m
[32m+[m
   end[m
 end[m
 [m
[36m@@ -40454,17 +40443,13 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    The patterns this adds will match the named gems in their common[m
[31m-locations (e.g. system gems, vendored with bundler, installed as a[m
[31m-:git dependency with bundler, etc) but is not guaranteed to work for[m
[31m-all possible gem locations. For example, if you have the gem source[m
[31m-in a directory with a completely unrelated name, and use bundler's[m
[31m-:path option, this will not filter it.[m
     [m
[32m+[m[32mThe patterns this adds will match the named gems in their common locations (e.g. system gems, vendored with bundler, installed as a :git dependency with bundler, etc) but is not guaranteed to work for all possible gem locations. For example, if you have the gem source in a directory with a completely unrelated name, and use bundler's :path option, this will not filter it.[m
 [m
   [m
[31m-Adds #backtrace_exclusion_patterns that will filter lines from[m
[31m-the named gems from backtraces.[m
[32m+[m
[32m+[m
[32m+[m[32mAdds #backtrace_exclusion_patterns that will filter lines from the named gems from backtraces.[m
 [m
 [m
   [m
[36m@@ -40529,14 +40515,12 @@[m [mend[m
       [m
   [m
   [m
[31m-    Adds key/value pairs to the exclusion_filter. If args[m
[31m-includes any symbols that are not part of the hash, each symbol[m
[31m-is treated as a key in the hash with the value true.[m
[32m+[m[41m    [m
[32m+[m[32mAdds key/value pairs to the exclusion_filter. If args includes any symbols that are not part of the hash, each symbol is treated as a key in the hash with the value true.[m
 [m
 Note[m
 [m
[31m-Filters set using this method can be overridden from the command line[m
[31m-or config files (e.g. .rspec).[m
[32m+[m[32mFilters set using this method can be overridden from the command line or config files (e.g. .rspec).[m
 [m
 [m
   [m
[36m@@ -40599,14 +40583,12 @@[m [mend[m
       [m
   [m
   [m
[31m-    Adds key/value pairs to the inclusion_filter. If args[m
[31m-includes any symbols that are not part of the hash, each symbol[m
[31m-is treated as a key in the hash with the value true.[m
[32m+[m[41m    [m
[32m+[m[32mAdds key/value pairs to the inclusion_filter. If args includes any symbols that are not part of the hash, each symbol is treated as a key in the hash with the value true.[m
 [m
 Note[m
 [m
[31m-Filters set using this method can be overridden from the command line[m
[31m-or config files (e.g. .rspec).[m
[32m+[m[32mFilters set using this method can be overridden from the command line or config files (e.g. .rspec).[m
 [m
 [m
   [m
[36m@@ -40669,15 +40651,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Applies the provided filter only if any of examples match, in constrast[m
[31m-to #filter_run, which always applies even if no examples match, in[m
[31m-which case no examples will be run. This allows you to leave configured[m
[31m-filters in place that are intended only for temporary use. The most common[m
[31m-example is focus filtering: config.filter_run_when_matching :focus.[m
[31m-With that configured, you can temporarily focus an example or group[m
[31m-by tagging it with :focus metadata, or prefixing it with an f[m
[31m-(as in fdescribe, fcontext and fit) since those are aliases for[m
[31m-describe/context/it with :focus metadata.[m
[32m+[m[41m    [m
[32m+[m[32mApplies the provided filter only if any of examples match, in constrast to #filter_run, which always applies even if no examples match, in which case no examples will be run. This allows you to leave configured filters in place that are intended only for temporary use. The most common example is focus filtering: config.filter_run_when_matching :focus. With that configured, you can temporarily focus an example or group by tagging it with :focus metadata, or prefixing it with an f (as in fdescribe, fcontext and fit) since those are aliases for describe/context/it with :focus metadata.[m
 [m
 [m
   [m
[36m@@ -40757,8 +40733,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Returns a duplicate of the formatters currently loaded in[m
[31m-the FormatterLoader for introspection.[m
[32m+[m[41m    [m
[32m+[m[32mReturns a duplicate of the formatters currently loaded in the FormatterLoader for introspection.[m
 [m
 Note as this is a duplicate, any mutations will be disregarded.[m
 [m
[36m@@ -40993,18 +40977,13 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    Filtered module inclusions can also be applied to[m
[31m-individual examples that have matching metadata. Just like[m
[31m-Ruby's object model is that every object has a singleton class[m
[31m-which has only a single instance, RSpec's model is that every[m
[31m-example has a singleton example group containing just the one[m
[31m-example.[m
     [m
[32m+[m[32mFiltered module inclusions can also be applied to individual examples that have matching metadata. Just like Ruby's object model is that every object has a singleton class which has only a single instance, RSpec's model is that every example has a singleton example group containing just the one example.[m
 [m
   [m
[31m-Tells RSpec to include mod in example groups. Methods defined in[m
[31m-mod are exposed to examples (not example groups). Use filters to[m
[31m-constrain the groups or examples in which to include the module.[m
[32m+[m
[32m+[m
[32m+[m[32mTells RSpec to include mod in example groups. Methods defined in mod are exposed to examples (not example groups). Use filters to constrain the groups or examples in which to include the module.[m
 [m
 [m
   [m
[36m@@ -41086,18 +41065,13 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    Filtered context inclusions can also be applied to[m
[31m-individual examples that have matching metadata. Just like[m
[31m-Ruby's object model is that every object has a singleton class[m
[31m-which has only a single instance, RSpec's model is that every[m
[31m-example has a singleton example group containing just the one[m
[31m-example.[m
     [m
[32m+[m[32mFiltered context inclusions can also be applied to individual examples that have matching metadata. Just like Ruby's object model is that every object has a singleton class which has only a single instance, RSpec's model is that every example has a singleton example group containing just the one example.[m
 [m
   [m
[31m-Tells RSpec to include the named shared example group in example groups.[m
[31m-Use filters to constrain the groups or examples in which to include[m
[31m-the example group.[m
[32m+[m
[32m+[m
[32m+[m[32mTells RSpec to include the named shared example group in example groups. Use filters to constrain the groups or examples in which to include the example group.[m
 [m
 [m
   [m
[36m@@ -41168,8 +41142,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Returns the inclusion_filter. If none has been set, returns an empty[m
[31m-hash.[m
[32m+[m[41m    [m
[32m+[m[32mReturns the inclusion_filter. If none has been set, returns an empty hash.[m
 [m
 [m
   [m
[36m@@ -41201,13 +41175,12 @@[m [mend[m
       [m
   [m
   [m
[31m-    Clears and reassigns the inclusion_filter. Set to nil if you don't[m
[31m-want any inclusion filter at all.[m
[32m+[m[41m    [m
[32m+[m[32mClears and reassigns the inclusion_filter. Set to nil if you don't want any inclusion filter at all.[m
 [m
 Warning[m
 [m
[31m-This overrides any inclusion filters/tags set on the command line or in[m
[31m-configuration files.[m
[32m+[m[32mThis overrides any inclusion filters/tags set on the command line or in configuration files.[m
 [m
 [m
   [m
[36m@@ -41336,14 +41312,11 @@[m [mend[m
 [m
 framework can be a Symbol or a Module.[m
 [m
[31m-Given any of :rspec, :mocha, :flexmock, or :rr, configures the[m
[31m-named framework.[m
[32m+[m[32mGiven any of :rspec, :mocha, :flexmock, or :rr, configures the named framework.[m
 [m
[31m-Given :nothing, configures no framework. Use this if you don't use[m
[31m-any mocking framework to save a little bit of overhead.[m
[32m+[m[32mGiven :nothing, configures no framework. Use this if you don't use any mocking framework to save a little bit of overhead.[m
 [m
[31m-Given a Module, includes that module in every example group. The module[m
[31m-should adhere to RSpec's mock framework adapter API:.[m
[32m+[m[32mGiven a Module, includes that module in every example group. The module should adhere to RSpec's mock framework adapter API:.[m
 [m
 setup_mocks_for_rspec[m
   - called before each example[m
[36m@@ -41356,8 +41329,7 @@[m [mteardown_mocks_for_rspec[m
   - called after verify_mocks_for_rspec (even if there are errors)[m
 [m
 [m
[31m-If the module responds to configuration and mock_with receives a[m
[31m-block, it will yield the configuration object to the block e.g.[m
[32m+[m[32mIf the module responds to configuration and mock_with receives a block, it will yield the configuration object to the block e.g.[m
 [m
 config.mock_with OtherMockFrameworkAdapter do |mod_config|[m
   mod_config.custom_setting = true[m
[36m@@ -41518,10 +41492,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Sets the default global ordering strategy. By default this can be one[m
[31m-of :defined, :random, but is customizable through the[m
[31m-register_ordering API. If order is set to 'rand:<seed>',[m
[31m-the seed will also be set.[m
[32m+[m[41m    [m
[32m+[m[32mSets the default global ordering strategy. By default this can be one of :defined, :random, but is customizable through the register_ordering API. If order is set to 'rand:<seed>', the seed will also be set.[m
 [m
 [m
   [m
[36m@@ -41556,12 +41528,10 @@[m [mdelegate_to_ordering_manager :order=[m
       [m
   [m
   [m
[31m-    Tells RSpec to prepend example groups with mod. Methods defined in[m
[31m-mod are exposed to examples (not example groups). Use filters to[m
[31m-constrain the groups in which to prepend the module.[m
     [m
[31m-Similar to include, but module is included before the example group's class[m
[31m-in the ancestor chain.[m
[32m+[m[32mTells RSpec to prepend example groups with mod. Methods defined in mod are exposed to examples (not example groups). Use filters to constrain the groups in which to prepend the module.[m
[32m+[m
[32m+[m[32mSimilar to include, but module is included before the example group's class in the ancestor chain.[m
 [m
 [m
   [m
[36m@@ -41634,15 +41604,12 @@[m [mend[m
       [m
   [m
   [m
[31m-    Adds block to the start of the list of before blocks in the same[m
[31m-scope (:example, :context, or :suite), in contrast to #before,[m
[31m-which adds the hook to the end of the list.[m
[32m+[m[41m    [m
[32m+[m[32mAdds block to the start of the list of before blocks in the same scope (:example, :context, or :suite), in contrast to #before, which adds the hook to the end of the list.[m
 [m
 See Hooks#before for full before hook docs.[m
 [m
[31m-This method differs from Hooks#prepend_before in only one way: it supports[m
[31m-the :suite scope. Hooks with the :suite scope will be run once before[m
[31m-the first example of the entire suite is executed.[m
[32m+[m[32mThis method differs from Hooks#prepend_before in only one way: it supports the :suite scope. Hooks with the :suite scope will be run once before the first example of the entire suite is executed.[m
 [m
 [m
   [m
[36m@@ -41707,10 +41674,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Turns deprecation warnings into errors, in order to surface[m
[31m-the full backtrace of the call site. This can be useful when[m
[31m-you need more context to address a deprecation than the[m
[31m-single-line call site normally provided.[m
[32m+[m[41m    [m
[32m+[m[32mTurns deprecation warnings into errors, in order to surface the full backtrace of the call site. This can be useful when you need more context to address a deprecation than the single-line call site normally provided.[m
 [m
 [m
   [m
[36m@@ -41753,8 +41718,8 @@[m [mend[m
       [m
   [m
   [m
[31m-    Turns warnings into errors. This can be useful when[m
[31m-you want RSpec to run in a 'strict' no warning situation.[m
[32m+[m[41m    [m
[32m+[m[32mTurns warnings into errors. This can be useful when you want RSpec to run in a 'strict' no warning situation.[m
 [m
 [m
   [m
[36m@@ -41808,15 +41773,13 @@[m [mend[m
     [m
   [m
     Note:[m
[31m-    Pass the symbol :global to set the ordering strategy that[m
[31m-will be used to order the top-level example groups and any example[m
[31m-groups that do not have declared :order metadata.[m
[32m+[m[41m    [m
[32m+[m[32mPass the symbol :global to set the ordering strategy that will be used to order the top-level example groups and any example groups that do not have declared :order metadata.[m
 [m
   [m
 [m
[31m-Registers a named ordering strategy that can later be[m
[31m-used to order an example group's subgroups by adding[m
[31m-:order => <name> metadata to the example group.[m
[32m+[m
[32m+[m[32mRegisters a named ordering strategy that can later be used to order an example group's subgroups by adding :order => <name> metadata to the example group.[m
 [m
 [m
   [m
[36m@@ -41997,9 +41966,9 @@[m [mReturns:[m
     [m
       # File 'lib/rspec/core/configuration.rb', line 1024[m
 [m
[31m-def reporter[m
[31m-  # @reporter_buffer should only ever be set in this method to cover[m
[32m+[m[32mdef reporter  # @reporter_buffer should only ever be set in this method to cover[m
   # initialization of @reporter.[m
[32m+[m
   @reporter_buffer || @reporter ||=[m
     begin[m
       @reporter_buffer = DeprecationReporterBuffer.new[m
[36m@@ -42017,17 +41986,12 @@[m [mend[m
       [m
   [m
   [m
[32m+[m[41m    [m
 Seed for random ordering (default: generated randomly each run).[m
 [m
[31m-When you run specs with --order random, RSpec generates a random seed[m
[31m-for the randomization and prints it to the output_stream (assuming[m
[31m-you're using RSpec's built-in formatters). If you discover an ordering[m
[31m-dependency (i.e. examples fail intermittently depending on order), set[m
[31m-this (on Configuration or on the command line with --seed) to run[m
[31m-using the same seed while you debug the issue.[m
[32m+[m[32mWhen you run specs with --order random, RSpec generates a random seed for the randomization and prints it to the output_stream (assuming you're using RSpec's built-in formatters). If you discover an ordering dependency (i.e. examples fail intermittently depending on order), set this (on Configuration or on the command line with --seed) to run using the same seed while you debug the issue.[m
 [m
[31m-We recommend, actually, that you use the command line approach so you[m
[31m-don't accidentally leave the seed encoded.[m
[32m+[m[32mWe recommend, actually, that you use the command line approach so you don't accidentally leave the seed encoded.[m
 [m
 [m
   [m
[36m@@ -42083,9 +42048,8 @@[m [mdelegate_to_ordering_manager :seed=[m
       [m
   [m
   [m
[31m-    Deprecated. This config option was added in RSpec 2 to pave the way[m
[31m-for this being the default behavior in RSpec 3. Now this option is[m
[31m-a no-op.[m
[32m+[m[32m    Deprecated.[m[41m [m
[32m+[m[32mThis config option was added in RSpec 2 to pave the way for this being the default behavior in RSpec 3. Now this option is a no-op.[m
 [m
 [m
 [m
[36m@@ -42209,14 +42176,10 @@[m [mend[m
       [m
   [m
   [m
[31m-    Defines a callback that runs after the first example with matching[m
[31m-metadata is defined. If no examples are defined with matching metadata,[m
[31m-it will not get called at all.[m
     [m
[31m-This can be used to ensure some setup is performed (such as bootstrapping[m
[31m-a DB or loading a specific file that adds significantly to the boot time)[m
[31m-if needed (as indicated by the presence of an example with matching metadata)[m
[31m-but avoided otherwise.[m
[32m+[m[32mDefines a callback that runs after the first example with matching metadata is defined. If no examples are defined with matching metadata, it will not get called at all.[m
[32m+[m
[32m+[m[32mThis can be used to ensure some setup is performed (such as bootstrapping a DB or loading a specific file that adds significantly to the boot time) if needed (as indicated by the presence of an example with matching metadata) but avoided otherwise.[m
 [m
 [m
   [m
