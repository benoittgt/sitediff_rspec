[1mdiff --git a/export/rspec-core-RSpec-Core-Sandbox.html.current b/export/rspec-core-RSpec-Core-Sandbox.html.new[m
[1mindex bc81dda..e28fb14 100644[m
[1m--- a/export/rspec-core-RSpec-Core-Sandbox.html.current[m
[1m+++ b/export/rspec-core-RSpec-Core-Sandbox.html.new[m
[36m@@ -51,38 +51,33 @@[m [mOverviewOverview[m
     [m
   [m
     Note:Note:[m
[31m-    This module is not normally available. You must require[m
[31m-rspec/core/sandboxrspec/core/sandbox to load it.This module is not normally available. You must require[m
[31m-rspec/core/sandbox to load it.[m
[31m-This module is not normally available. You must require[m
[31m-rspec/core/sandbox to load it.[m
[32m+[m[41m    [m
[32m+[m[32mThis module is not normally available. You must require rspec/core/sandboxrspec/core/sandbox to load it.This module is not normally available. You must require rspec/core/sandbox to load it.[m
[32m+[m
[32m+[m[32mThis module is not normally available. You must require rspec/core/sandbox to load it.[m
 [m
   [m
     Note:[m
[31m-    This module is not normally available. You must require[m
[31m-rspec/core/sandbox to load it.[m
[32m+[m[41m    [m
[32m+[m[32mThis module is not normally available. You must require rspec/core/sandbox to load it.[m
 [m
   [m
 [m
[31m-A sandbox isolates the enclosed code into an environment that looks 'new'[m
[31m-meaning globally accessed objects are reset for the duration of the[m
[31m-sandbox.A sandbox isolates the enclosed code into an environment that looks 'new'[m
[31m-meaning globally accessed objects are reset for the duration of the[m
[31m-sandbox.[m
[32m+[m
[32m+[m[32mA sandbox isolates the enclosed code into an environment that looks 'new' meaning globally accessed objects are reset for the duration of the sandbox.A sandbox isolates the enclosed code into an environment that looks 'new' meaning globally accessed objects are reset for the duration of the sandbox.[m
 [m
 [m
   [m
     [m
   [m
     Note:[m
[31m-    This module is not normally available. You must require[m
[31m-rspec/core/sandbox to load it.[m
[32m+[m[41m    [m
[32m+[m[32mThis module is not normally available. You must require rspec/core/sandbox to load it.[m
[32m+[m
   [m
 [m
 [m
[31m-A sandbox isolates the enclosed code into an environment that looks 'new'[m
[31m-meaning globally accessed objects are reset for the duration of the[m
[31m-sandbox.[m
[32m+[m[32mA sandbox isolates the enclosed code into an environment that looks 'new' meaning globally accessed objects are reset for the duration of the sandbox.[m
 [m
 [m
   [m
[36m@@ -91,14 +86,13 @@[m [msandbox.[m
     [m
   [m
     Note:[m
[31m-    This module is not normally available. You must require[m
[31m-rspec/core/sandbox to load it.[m
     [m
[32m+[m[32mThis module is not normally available. You must require rspec/core/sandbox to load it.[m
 [m
   [m
[31m-A sandbox isolates the enclosed code into an environment that looks 'new'[m
[31m-meaning globally accessed objects are reset for the duration of the[m
[31m-sandbox.[m
[32m+[m
[32m+[m
[32m+[m[32mA sandbox isolates the enclosed code into an environment that looks 'new' meaning globally accessed objects are reset for the duration of the sandbox.[m
 [m
 [m
   [m
[36m@@ -191,13 +190,10 @@[m [mExecute a provided block with RSpec global objects (configuration, world) reset.[m
       [m
   [m
   [m
[31m-    Execute a provided block with RSpec global objects (configuration,[m
[31m-world) reset.  This is used to test RSpec with RSpec.Execute a provided block with RSpec global objects (configuration,[m
[31m-world) reset.  This is used to test RSpec with RSpec.[m
     [m
[31m-When calling this the configuration is passed into the provided block.[m
[31m-Use this to set custom configs for your sandboxed examples.When calling this the configuration is passed into the provided block.[m
[31m-Use this to set custom configs for your sandboxed examples.[m
[32m+[m[32mExecute a provided block with RSpec global objects (configuration, world) reset. This is used to test RSpec with RSpec.Execute a provided block with RSpec global objects (configuration, world) reset. This is used to test RSpec with RSpec.[m
[32m+[m
[32m+[m[32mWhen calling this the configuration is passed into the provided block. Use this to set custom configs for your sandboxed examples.When calling this the configuration is passed into the provided block. Use this to set custom configs for your sandboxed examples.[m
 [m
 SandboxSandbox..sandboxedsandboxed dodo ||configconfig||[m
   configconfig..beforebefore((:context:context)) {{ RSpecRSpec..current_examplecurrent_example == nilnil }}[m
[36m@@ -212,11 +208,10 @@[m [mend[m
 [m
 [m
   [m
[31m-    Execute a provided block with RSpec global objects (configuration,[m
[31m-world) reset.  This is used to test RSpec with RSpec.[m
     [m
[31m-When calling this the configuration is passed into the provided block.[m
[31m-Use this to set custom configs for your sandboxed examples.[m
[32m+[m[32mExecute a provided block with RSpec global objects (configuration, world) reset. This is used to test RSpec with RSpec.[m
[32m+[m
[32m+[m[32mWhen calling this the configuration is passed into the provided block. Use this to set custom configs for your sandboxed examples.[m
 [m
 Sandbox.sandboxed do |config|[m
   config.before(:context) { RSpec.current_example = nil }[m
[36m@@ -227,11 +222,10 @@[m [mend[m
   [m
 [m
   [m
[31m-    Execute a provided block with RSpec global objects (configuration,[m
[31m-world) reset.  This is used to test RSpec with RSpec.[m
     [m
[31m-When calling this the configuration is passed into the provided block.[m
[31m-Use this to set custom configs for your sandboxed examples.[m
[32m+[m[32mExecute a provided block with RSpec global objects (configuration, world) reset. This is used to test RSpec with RSpec.[m
[32m+[m
[32m+[m[32mWhen calling this the configuration is passed into the provided block. Use this to set custom configs for your sandboxed examples.[m
 [m
 Sandbox.sandboxed do |config|[m
   config.before(:context) { RSpec.current_example = nil }[m
[36m@@ -435,11 +429,10 @@[m [mend[m
 [m
   [m
   [m
[31m-    Execute a provided block with RSpec global objects (configuration,[m
[31m-world) reset.  This is used to test RSpec with RSpec.[m
     [m
[31m-When calling this the configuration is passed into the provided block.[m
[31m-Use this to set custom configs for your sandboxed examples.[m
[32m+[m[32mExecute a provided block with RSpec global objects (configuration, world) reset. This is used to test RSpec with RSpec.[m
[32m+[m
[32m+[m[32mWhen calling this the configuration is passed into the provided block. Use this to set custom configs for your sandboxed examples.[m
 [m
 Sandbox.sandboxed do |config|[m
   config.before(:context) { RSpec.current_example = nil }[m
[36m@@ -502,11 +495,10 @@[m [mend[m
       [m
   [m
   [m
[31m-    Execute a provided block with RSpec global objects (configuration,[m
[31m-world) reset.  This is used to test RSpec with RSpec.[m
     [m
[31m-When calling this the configuration is passed into the provided block.[m
[31m-Use this to set custom configs for your sandboxed examples.[m
[32m+[m[32mExecute a provided block with RSpec global objects (configuration, world) reset. This is used to test RSpec with RSpec.[m
[32m+[m
[32m+[m[32mWhen calling this the configuration is passed into the provided block. Use this to set custom configs for your sandboxed examples.[m
 [m
 Sandbox.sandboxed do |config|[m
   config.before(:context) { RSpec.current_example = nil }[m
[36m@@ -593,14 +585,13 @@[m [mOverview[m
     [m
   [m
     Note:[m
[31m-    This module is not normally available. You must require[m
[31m-rspec/core/sandbox to load it.[m
[32m+[m[41m    [m
[32m+[m[32mThis module is not normally available. You must require rspec/core/sandbox to load it.[m
[32m+[m
   [m
 [m
 [m
[31m-A sandbox isolates the enclosed code into an environment that looks 'new'[m
[31m-meaning globally accessed objects are reset for the duration of the[m
[31m-sandbox.[m
[32m+[m[32mA sandbox isolates the enclosed code into an environment that looks 'new' meaning globally accessed objects are reset for the duration of the sandbox.[m
 [m
 [m
   [m
[36m@@ -652,11 +644,10 @@[m [msandbox.[m
       [m
   [m
   [m
[31m-    Execute a provided block with RSpec global objects (configuration,[m
[31m-world) reset.  This is used to test RSpec with RSpec.[m
     [m
[31m-When calling this the configuration is passed into the provided block.[m
[31m-Use this to set custom configs for your sandboxed examples.[m
[32m+[m[32mExecute a provided block with RSpec global objects (configuration, world) reset. This is used to test RSpec with RSpec.[m
[32m+[m
[32m+[m[32mWhen calling this the configuration is passed into the provided block. Use this to set custom configs for your sandboxed examples.[m
 [m
 Sandbox.sandboxed do |config|[m
   config.before(:context) { RSpec.current_example = nil }[m
